# Sean Reid, 2026-07-25 — integrating his fork: what we took, and every decision we made on his behalf

*This report supersedes `docs/60-integrating-the-fork.md`, which it began life as. `docs/60` is deleted:
one integration must not have two audit trails that can disagree.*

**Purpose.** Sean invited us to take his work rather than merge it himself, so we are resolving conflicts in
*his* code without him. This file is the audit trail of that: what came in, what conflicted, and what we
decided and why. **Every entry is a place he should be able to say "no, that's wrong" and be right.**

It exists because a merge resolution is a design decision wearing a chore's clothing. Nobody reviews the
resolution of a conflict the way they review a commit, and the resolutions are exactly where two people's
architectures get silently welded together.

## How his work is structured (and why this was easy)

He pre-split 156 commits into **nine cumulative branches**, `sean/upstream-1-ci` … `upstream-9-process`.
Cumulative, not independent: `upstream-1` is 1 commit, `upstream-9` is all nine. Taken **in order**, each step
is small; taken as one merge of his `main`, it is not:

| taken as | conflicted files |
|---|---|
| his whole `main` in one go | 12 |
| `upstream-1-ci` alone | 3 |
| `upstream-2-docs` on top of 1 | 2 |
| `upstream-3-sph-live-drop` on top of 2 | 3 |

So each step is its own PR, stacked on the previous one (`--base integrate-sean-N`), and the diff a reviewer
sees is only that step's delta.

**Authorship is preserved throughout.** Every step comes in by `git merge`, never squash or cherry-pick, so
the commits stay `Sean Reid <seanreid.mail@gmail.com>` and count on his contribution graph.

## Why the histories diverged in the first place — our fault, worth not repeating

His fork branched from our `main` *before* we merged PR #81, and we merged #81 as a **squash**. Squashing
rewrote 40 commits into one, so his branch still carries ~15 commits authored by Robin that `main` now
contains only *inside* the squash. That is what turned a fast-forward into a conflict-resolution exercise.

**Merge commits keep a fork in sync; squash merges strand it.** Now that a collaborator works from forks, the
default for upstream merges should change.

## Decisions, step by step

### upstream-1-ci — "run the deploy gate and the wasm build on every pull request" (PR #84)

The CI this repo never had. 12 files, 3 conflicts.

| conflict | decision | why |
|---|---|---|
| `.gitignore` | **took his side** (deletes the `.github/workflows/` ignore) | That ignore existed *only* because our local `gh` token lacked the `workflow` scope. Committing CI is precisely what it was blocking, so it goes. It had previously been written as `.github/`, which swallowed the whole directory and is why the repo had no CODEOWNERS either. |
| `CHANGELOG.md` | **union**, ours first | Both sides appended at the top. A journal conflict's resolution is both entries; dropping either deletes somebody's record. |
| `JOURNAL.md` | **union**, ours first | Same. |

Also superseded: a stale untracked `.github/workflows/ci.yml` from 2026-07-08 pointing at
`working-directory: greenfield-engine`, a directory that no longer exists.

### upstream-2-docs — "the public story matches the code" (PR #85)

2 conflicts, both `CHANGELOG.md` / `JOURNAL.md`, both unioned. No judgement calls.

### upstream-3-sph-live-drop — "state the crossing law and retire the CPU debris leftovers"

3 conflicts. Two were the usual journals. The third is the one that needs his eyes:

**`lib.rs` — the scene-resolution const block.** His commit replaces our block
(`SPH_SHOCK_WINDOW_S`, `SCENE_DEBRIS_N`, `SCENE_CAP_N`, `SCENE_IMPACT_N`) with a single
`MOONLET_UNIS_N: usize = 1536`.

- **Decision: keep both.** `SPH_SHOCK_WINDOW_S` is load-bearing on our side — it is the quiescence gate the
  de-resolution merge depends on (docs/44 §6), and it is deliberately one constant shared with the docs/42 dt
  coarsening so "the shock is finished" has one definition. Taking his side outright would delete it.
- **Decision NOT taken, and why it matters:** `1536` is exactly `SCENE_IMPACT_N` (512 + 1024), and his own
  comment says the pool is "sized to the pool the retired CPU debris cloud used". The obvious Law V move is
  `const MOONLET_UNIS_N: usize = SCENE_IMPACT_N;` — one number, one home. **We wrote that, then reverted it.**
  Robin's correction: our side moved the disk to **GPU** SPH, so `SCENE_IMPACT_N` is a *physics particle
  count*, whereas `MOONLET_UNIS_N` is a *render pool* sized against the **CPU** path his commit retires. They
  agree only because the CPU-era pool happened to equal the GPU count we now run. Coupling them would assert
  a dependency that does not exist and would resize his draw pool whenever anyone tuned GPU resolution.
  The literal stays, with the coincidence documented at the call site.
- **Sean: if the pool genuinely should track resolution, say so and we will derive it.** We left it
  independent because we could not establish that it should.

### upstream-4-gpu-gravity — "dispatch the GPU kernels; race-free COM sweep for Metal" (PR #87)

7 files, **2 conflicts**, both `CHANGELOG.md` / `JOURNAL.md`, unioned. **No judgement calls needed — but two
Law checks were run before concluding that, and both are worth recording as things that were NOT wrong.**

1. **Is this a second answer to "what is the gravitational acceleration"?** (Law II) No. He adds a GPU
   Barnes–Hut (`GravityField`, `accelerations_tree`) and wires it into the CPU `aggregate` path, and he pins
   it the way CLAUDE.md rule 3 requires: `the_gpu_tree_matches_the_cpu_tree_within_the_theta_bound` and
   `the_gpu_tree_opened_fully_recovers_the_direct_sum`. That makes it an ACCELERATION of the CPU tree, pinned
   to its exact reference, not a rival implementation. The SPH self-gravity in `sph_step.wgsl` is a different
   container and legitimately specialized (docs/46 §1).
2. **Is `theta` duplicated between Rust and WGSL?** (Law V / `laws::SINGLE_SOURCE`) No, and the design is
   better than ours was: the shader reads `P.theta` from its params, `THETA` exists only as a Rust-side
   default, and `accelerations_tree_with` takes it explicitly. Theta has one home and flows to the GPU as a
   uniform. The worry was real — a kernel hardcoding an opening angle would silently ignore a caller's theta —
   it simply was not what he did.

Also confirmed: his rewritten `bh_gravity.wgsl` still satisfies
`pinned_constant_tests::the_shaders_gravitational_constant_matches_the_engines`, the guard added the same day
for the shader copies of `G`. `379/379`.

### The stack half-landed — and GitHub called it MERGED anyway (recovered here)

**Sean: for about a day, your step-4 was not on `main` even though PR #87 said "Merged".** Nor was the
report you are reading. The cause is worth knowing, because it is silent.

The six PRs were stacked, each based on the one below it. As each landed, the next was retargeted to `main`
— for #83, #84, #85 and #86. **#87 and #88 were not retargeted**, so `#87` merged into the branch
`integrate-sean-3-sph-live-drop` and `#88` into `integrate-sean-4-gpu-gravity`. Both merges succeeded, both
PRs closed as **MERGED**, both branches are still alive on the remote — and `main` never received either.
Nothing failed, nothing warned, and the PR list looked finished.

Two habits catch it, and neither is a code review:

- **Merging a stacked PR is only safe once its base is `main`.** Retarget the next PR *before* merging it,
  or delete each base branch on merge so the retarget is forced.
- **"MERGED" is a statement about a PR, not about `main`.** The check that actually answers the question is
  `git merge-base --is-ancestor <branch> origin/main`. Run it after a stack, per branch.

Recovering it also exposed a **second** casualty that no merge would have flagged. `#88` deleted
`docs/60-integrating-the-fork.md` and replaced it with this file; `#86` had added `docs/60` to `main`. Since
the two changes sat on either side of the split, git's three-way merge saw a file added on one side only and
**kept it** — correct git, wrong outcome: `main` would have carried *two* audit trails of one integration,
free to disagree. `docs/60` is deleted here, after checking line by line that this report carries all of it
(open questions 1 and 2 survive struck through **with** their resolutions, 3 and 4 verbatim).

### upstream-5-ground-ball — "the declared ball is matter and its fate is emergent" — **INTEGRATED**

One commit, 7 conflicts. Four resolved, one is a decision worth conferring on, and the analysis produced the
most valuable finding of the whole integration: **his change fixes a bug in OUR code.**

Resolved without difficulty:

| conflict | decision |
|---|---|
| `CHANGELOG` / `JOURNAL` / `docs/46` | union — both sides appended entries and ledger rows |
| `render.rs` | union. 220 lines ours (`Drawn`, `of_matter`, `MatterField`), 44 his (two camera-pan tests). Both APPEND at end of file; no overlap at all. NOTE: a naive union dropped `MatterField::draw`'s closing braces, which sat outside the conflict region — check brace balance after any union, the compiler catches it but the cause is not obvious |
| `fly_camera.rs` | **collapsed a duplicate.** He added `pub const FOV_Y: f64 = 0.9`; we added `DEFAULT_FOV_Y: f64 = 0.9` — converging fixes for the same problem (the FOV was duplicated into a shader's billboard sizing). Keeping both would have been two names for one number in one file. Kept `DEFAULT_FOV_Y`, because callers can override it via `view_from_pose` so "default" is accurate, and because `ground_scene::FOV_Y` already exists with a DIFFERENT value (π/3) — one name, two values, two scenes is a trap |
| `ground_scene.rs` | **his ball now reports through `Drawn`.** He hand-wrote `GpuParticle` literals in the scene to draw the ball's lattice particles. His own comment says *"No ball-specific renderer exists"* — the same principle as our `crate::Drawn`, written by hand. So `Simulation::drawn()` now reports cohesive bodies and `GpuParticle::of_matter` does the albedo and incandescence. **His intent unchanged, his physics untouched**, one fewer place a scene can disagree with the physics about what matter looks like. Note this is the CPU `Aggregate` path; the GPU SPH containers report through `Flight` |

**The one to confer on — `simulation.rs` hunk 3 (24 lines ours, 61 his).** Both sides rewrote the arrival path.

- **Ours** delegates the whole flight to `flight::Flight::step` — one flight law for a ground patch and a
  planet (Law II) — then excavates each `Arrival` through `matter::impact`.
- **His** keeps the loop in the scene and adds three things, all of them real:
  1. **`forecast_body_contact`** — a meteor meeting a cohesive body, forecast through
     `interaction::detect_swept`, the one collision owner. This is his feature.
  2. **A bisected impact site** — the site is where the TRAJECTORY crosses the surface, found by 16-step
     bisection, not wherever the post-step sample landed.
  3. **Momentum as well as energy** — `deposit_event(site, dir, momentum, energy_j)`.

**Finding: (2) is a bug in our code, not just a difference.** Our `FlightEnvironment::arrival` returns `to`,
the post-step position. At 17 km/s with a 1/60 s step that is ~280 m past the surface, so the impact couples
to matter hundreds of metres underground. `PlanetAir::arrival` in `lib.rs` has the identical flaw. His
bisection is the fix, and it is needed at planetary scale more than at ground scale.

**Resolution — neither side won; the pieces compose.** Your three additions are all in, and the flight law
stayed ours, because keeping your loop would have silently retired the atmosphere: it integrated the meteor
as `vel += g*dt`, a VACUUM ballistic path, while `flight::Flight::step` carries drag, aeroheating, ablation
and the trail. So the trajectory stayed with the engine and your three additions moved to where they can
still see it:

1. **`forecast_body_contact` moved into `GroundAir::arrival`.** It had to move rather than merely being kept:
   once `Flight` owns the trajectory the scene never sees the swept segment, and a swept forecast is the one
   thing that cannot be done from outside. `GroundAir` now carries `bodies` and `materials`, and a declared
   body is asked about BEFORE the terrain — it is in the way first. Your code is unchanged inside; only its
   address is different. **It still calls `interaction::detect_swept` and still takes that door's own
   reduced-mass energy** rather than recomputing one.
2. **The bisection moved into `FlightEnvironment::arrival`** — so it fixes the planet too. See below.
3. **`Arrival` gained a `momentum` field**, and `deposit_event` is now the one door every ground arrival goes
   through. The old path called `matter::impact` directly and had no momentum to give it.

**Your finding was right, and it was worse than the ground.** `arrival` returned `to`, the post-step sample.
`PlanetAir::arrival` in `lib.rs` had the identical line. Now both call one `flight::surface_crossing`, and
`an_arrival_sites_the_impact_on_the_surface_not_where_the_step_ended` pins ground and planet together.

**A second law fell out that neither of us was looking for.** `interaction::detect_swept` already prices
every body-body collision at **½·μ·|Δv|²** — its own comment: *"the energy actually available at the contact
frame, not either body's kinetic energy in an arbitrary frame"*. The terrain path wrote **½·m·v²**
separately. Those are not two rules: as the struck mass grows without bound, μ → m and Δv → v. So there is
now one `flight::delivered(body, target_vel, target_mass_kg)`, where `None` means immovable, and
`an_immovable_target_is_the_reduced_mass_limit` pins the terrain case as the LIMIT of the general one
(convergence checked over 10³…10¹⁵ × the striker's mass). Terrain stopped being a special case of impact.

**★ A decision made in your code, and the measurement behind it — `a_sufficient_meteor_shatters_the_ball`.**
Your assertion was `spread1 > 2.0 * spread0`. It passes on your branch and failed here, and the cause is not
a defect on either side: your meteor flew in vacuum. Through this patch's real air a 17 km/s body arrives
with **1.645e11 J instead of 1.720e11** (2.2% of its speed lost over the last 80 m), and the scatter drops to
**1.51×**. Confirmed by ablation — forcing `air_at` to `None` makes your original 2.0× bar pass again, so the
atmosphere is the whole of the difference. **The physics got more honest and a threshold with ~1% of headroom
went red.**

Retuning the multiple would only park an arbitrary constant on today's number, so the bound now comes from
the body's own geometry: the cloud must have grown by more than **one lattice half-spacing**, the distance
below which parcels are still sitting where their bonds held them. Overrule this if you think the multiple
carried something the spacing does not.

I also tried "and it keeps growing a second later" and **it is false — 1.91 m → 1.91 m**. That is worth
knowing on its own: under real gravity onto real terrain a shattered pile SETTLES rather than dispersing, so
"the parcels are unbound" is not what this event produces and a test must not claim it.

**★★ A bug I introduced and the test caught — worth more than the fix.** Moving the bisection meant choosing
how far to bisect. I derived the tolerance from the GRAIN size, reasoning that `deposit_event` floors its
coupling distance at the grain scale so a finer site changes nothing downstream. **That reasoning is wrong,
and every test but yours still passed.** The site is not only a distance — it is also *where the material is
sampled*. A grain-sized tolerance left the bracket 0.83 m wide, put the site **0.795 m under** the surface,
and `strength_at(site)` then read the material *below* the ground: different yield strength → different
crater radius → different coupling length λ, and λ sits inside an exponential. Measured against your
`an_impact_event_heats_debris_grains_already_in_flight`: the grains' coupling weight fell **284×**
(0.2416 → 0.00085) and the debris took essentially none of the event.

The bisection now resolves to the surface FIELD's precision (f32, ~1e-7 relative — 30 halvings), not to the
resolution of the matter. **Your test is the only thing that caught it**, which is the argument for writing
the ones that assert a quantity rather than a state.

**Not done, and flagged rather than quietly skipped.** The impactor's own matter joining the wreck is still
your IOU, untouched. `docs/60-the-one-deposition-door.md` is yours and lands as-is — note that the number 60
was free only because PR #90 deleted the old `docs/60-integrating-the-fork.md` (this report replaced it); had
the recovery not happened first there would have been two docs/60.

**Steps 7–9 are untouched.** The camera/render architecture overlap (his `display_scale()` and camera-relative
globe against our pose API and matter rendering) is still ahead of us and is a larger question than this one.

### upstream-6-one-earth — "one Earth, the conserved refinement rung, site re-coherence" — **INTEGRATED**

One commit, 23 conflict hunks across 8 files. `refine.rs`, `recohere.rs` and `docs/61` land untouched.

**You closed a row we opened against ourselves.** docs/46 row 16 — *"scenes build their own worlds instead of
using ONE Earth"* — has been open since 2026-07-21, and the docs/58 name-freeness capstone (*"FAIL THE BUILD
on `EARTH_*`/`MOON_*` in a generic path"*) was on our own remaining-work list. You did both, and better than
the plan: `a_scene_module_carries_no_copy_of_a_body_parameter` makes it permanent rather than one-off.
`world_def.rs`'s `default_strata()` — our hand-written list of Earth's materials sitting beside the real
definition — is deleted in favour of inheriting the column from the body. That was exactly the private copy
row 16 named.

**★★ Four places this merge would have silently reverted our work, and why they are not your fault.** Your
branch forked before PRs #81 and #88, so on those lines "take theirs" is a REVERT wearing a merge's clothing:

| what | kept |
|---|---|
| `Mat4::perspective_rh(0.9, …)` at the space band's projection | ours (`SPACE_FOV_Y`) — a bare FOV literal is what #88 removed |
| a **second** bare `0.9` in `metres_per_pixel` (the HUD scale bar) | ours (`SPACE_FOV_Y`) |
| `self.flight_env = PlanetAir::of(…)`, the cached flight environment | ours — without it the planet JSON is deserialized every frame |
| the entire `cam_pose` / `view_from_pose` arm | ours — the observer/universe pose API (docs/59) |

In each case your feature was merged **alongside**, not instead: your pan/target composition
(`self.camera.pan`, `eye_dir()`) now rides on `SPACE_FOV_Y`, and Terra keeps both camera arms.

★ **The scale-bar one is the interesting one**, because our own guard would not have caught it.
`laws::fov_tests::no_projection_is_built_from_a_bare_field_of_view_literal` scans for a literal as the first
argument to `perspective_rh`. `metres_per_pixel_at(dist_m, 0.9, …)` is not a projection call, so it sails
past — and it is the subtler half of the original bug, where the HUD confidently reports metres for a frustum
the scene is not drawing. **The guard covers the half that is easy to see.**

**★ docs/46 nearly lost eight rows.** Your side of that hunk carried row 16 plus NEW rows numbered **17 and
18**; ours carried rows 16 through 24. Taking either side whole drops real content — and I did take yours
first, and caught it only because the row list came back `1…18` instead of `1…24`. Your new rows are
renumbered **25 and 26**. The ledger is 1–26 with nothing lost. **Ledger rows are append-only and numbered
by whoever gets there first; two people numbering independently is a collision waiting to happen.**

**★★ Your step made us build a gate we had been describing in prose for months.** It failed on merge, in the
best possible way. Removing `EARTH_RADIUS_M` left three readers inside `mod app` — which is
`#[cfg(target_arch = "wasm32")]` and therefore **invisible to a native `cargo check`**. Measured: native
`cargo check --all-targets` reported **0 errors** on code that does not build. `CLAUDE.md` has warned about
this in prose since July and it still bit us.

So `scripts/test.sh` now compiles `mod app` for wasm32 on full runs, and CI gains a fast `mod app (wasm32)`
job that fails in ~1 min rather than after the whole wasm-pack + Vite build. **The gate was verified by
breaking `mod app` on purpose**: its first version piped cargo into `grep` inside an `if !`, so it read
grep's status, printed the compiler errors, and exited 0. A gate that reports a failure and passes is worse
than no gate — it teaches you to trust it.

**Mechanical notes for your side.** Two shapes recur because your branch predates step 5: `self.meteors` is
now a method (the field became `flight::Flight`), and `Meteor` is the f64 `FlyingBody` with `id`/`temp_k`/
`skin_m`. Your `laws.rs` test also needed lifting into `single_source_tests`, where `strip` and
`contains_number` live. Your `docs/60-the-one-deposition-door.md` (step 5) and this step's `docs/61` both
land as-is.

**Not reviewed, and said plainly:** `refine.rs` (1,015 lines) and `recohere.rs` (568) merged without conflict
and are covered by your own 18 tests, which pass. I read their interfaces, not their physics. Rows 25 and 26
are your own flagged debts on them, and I have left them exactly as you wrote them.

## Steps 6–9 are PARALLEL TRACKS, not a Law II conflict (Robin, 2026-07-25)

This report previously flagged his camera and render work as a Law II problem — two answers to one question,
needing an architecture conference before integration. **That was wrong, and the correction is worth stating
carefully because it is easy to over-apply Law II and freeze work that should simply proceed.**

Robin: *"Steps 6–9 are genuinely parallel tracks; not law violation because they aren't two answers, they are
two visualization methods that should be available to scenes."*

The question Law II governs is **"where is the camera now?"**, and that must have exactly one answer at any
instant. **"How did it get there?" is not that question.** A fly camera, a scripted arc, a chase rule and a
follow are all *producers* of a pose; they are input methods, not competing accounts of where the camera is.
Offering several is no more a violation than offering both a keyboard and a mouse.

The same applies to the render frame. `vp_rel` (camera-relative) and `vp_abs` (absolute) are not two answers
about where the globe is — they are one geometry expressed in whichever frame has precision where it is
needed, which is exactly the specialization `docs/46` §1 sanctions ("the physics itself differs, so the
numerical treatment differs" — here the *arithmetic* differs). Likewise `display_scale()` vs a constant
`DISPLAY_SCALE`: a scale that varies with viewing regime is docs/13's scale-relative rendering, not a rival
definition of a metre.

**And his approach solves a problem we flagged as unbuilt.** Our own work hit the wall that one f32 depth
range cannot hold an 80 m fragment and a 6,371 km planet — dropping the near plane to the fragment made the
globe stop drawing entirely, and we bounded the ratio and named DEPTH PARTITIONING as the real fix we had not
built. His camera-relative globe, `camera_relative_eye_round_trip_is_submillimetre_at_planet_radius` and
`globe_model_translation_stays_subpixel_where_the_coarse_globe_is_drawn` are that fix. On this axis his work
supersedes ours and should win.

### Scenes SWITCH between camera systems — which is what makes it single-valued

Robin, clarifying: *"IE a switch between two camera handling systems."* That is the cleanest form of it. Only
one system drives at a time, so "where is the camera now" has one answer by construction, and a scene chooses
which mechanism it wants. Nothing needs unifying at the *input* end.

### Robin's condition, and it was NOT met — fixed here

*"As long as we can unify FOV within the engine for rendering, we should be good."* Right, and the check found
the engine failing it in the worst way. **Four projection sites, three built from a hardcoded field of view:**

| site | was | now |
|---|---|---|
| `lib.rs` `view_proj` (space band) | `0.9` literal | `SPACE_FOV_Y` |
| `lib.rs` `meters_per_pixel` (space band HUD) | `0.9` literal **again** | `SPACE_FOV_Y` |
| `ground_scene.rs` projection | `60f32.to_radians()` | named `FOV_Y` |
| `fly_camera.rs` | `DEFAULT_FOV_Y` / parameter | unchanged, already correct |

**The space-band pair is the one that matters.** It wrote `0.9` twice — once building the projection and once
in `meters_per_pixel`, the HUD's scale bar. Change the projection and the bar goes on measuring a frustum the
scene is no longer drawing: confident wrong metres, nothing failing. This is ours, not Sean's.

`ground_scene`'s literal is fixed the same way Sean's `upstream-5-ground-ball` fixes it (a named `FOV_Y` at
`FRAC_PI_3`) — converging fixes, so the merge is trivial.

**`SPACE_FOV_Y` is deliberately NOT shared with `fly_camera::DEFAULT_FOV_Y`**, which is also `0.9`. Two scenes
asking "what field of view do I render at" are two questions with the same answer today, and coupling them
would assert that one scene's framing must follow another's. Exactly the `MOONLET_UNIS_N` trap, avoided on
purpose this time.

**Guarded, not just fixed:** `laws::fov_tests::no_projection_is_built_from_a_bare_field_of_view_literal`
scans every source and requires the first argument to `perspective_rh` not to begin with a digit. A named
constant can be traced, shared and tested; a literal cannot. `380/380`.

### The one remaining constraint that keeps "both available" Law-abiding

Several producers, **one sink**. Every camera driver must end by handing the engine a single pose
(`Terra::set_camera_pose`), and nothing may reach around it to mutate camera state directly. If his `arc_*`
path writes to `fly` while `set_camera_pose` also exists, then "where is the camera" genuinely does have two
answers and they can disagree — that would be the violation, and it is a small structural detail rather than
an architectural argument.

So the earlier suggestion in this report — *"likely resolution: his arc becomes a caller of the pose API"* —
was half right and stated too strongly. His arc should not be *replaced* by the pose API; it should **emit**
through it, and remain a first-class capability a scene can choose.

## Open questions for Sean

1. ~~**Two camera-control APIs.**~~ **RESOLVED as a non-issue (see "parallel tracks" above).** Both are
   producers of a pose and both should exist. The only thing to check when integrating steps 6–9 is that his
   `arc_*` path *emits* a pose rather than mutating camera state behind the pose API's back — several
   producers, one sink.
2. ~~**`display_scale()` vs `DISPLAY_SCALE`.**~~ **Also a non-issue, and his side wins.** A scale that varies
   with viewing regime is docs/13 scale-relative rendering, not a rival definition of a metre; the
   camera-relative globe is the depth-partitioning fix we flagged as unbuilt. The integration work is
   mechanical (the same lines of `Terra::render` carry our matter rendering), not architectural.
3. **He may already have built our next task.** His new tests —
   `camera_relative_eye_round_trip_is_submillimetre_at_planet_radius`,
   `globe_model_translation_stays_subpixel_where_the_coarse_globe_is_drawn`,
   `triplanar_anchor_restores_surface_fixed_texture_phase` — describe model-matrix anchoring, which is exactly
   the mechanism we identified as the blocker for the ground-tier ladder (docs/59, one tier at 45 ms/frame
   because every tier is rebuilt every frame). If so, his work supersedes ours there and should win.
4. **`FOV_Y` twice.** He added `pub const FOV_Y` in `fly_camera`; we added `DEFAULT_FOV_Y` plus `View::fov_y`,
   for the same reason (the FOV was duplicated into a shader's billboard sizing). Converging fixes, one name to
   pick. No physics at stake.

## Standing rule this establishes

**Resolve his conflicts against the Laws, not against the compiler.** The `MOONLET_UNIS_N` case is the
example: the compiler was perfectly happy with the wrong answer *and* with the right one, and with the
false-dependency version in between. `docs/00`'s pre-flight is what separated them, and Robin's knowledge of
which path moved to the GPU is what corrected the pre-flight's first answer.
