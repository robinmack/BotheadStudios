# docs/46 — One physics: the charter, and the conformance ledger

> **The promise.** REAL physics — one law, at every scale, in every scene. A world is a world is a world.
> This is the product, not an aesthetic preference about code structure. An engine that answers the same
> physical question two different ways in two different scenes has broken its promise, however good each
> answer looks on its own.
>
> **The span that promise commits to, stated as the acceptance test:** simulate a **star's photosphere
> and generate a solar flare**, or **a raindrop on a flower petal**, with the *same engine* — without
> reinventing or re-coding anything. It should fall out of the scale a scene is viewed at and the action
> occurring in it. **And the player must be able to SEE both.** That is ~15 orders of magnitude with no
> scene-specific branch on either the physics side or the render side.
>
> Judge every change against it: **if it would need re-coding to work at another scale, it is the wrong
> change** — however good the result looks in the scene it was written for. A new per-scene code path is
> a failure even when it ships something beautiful.

The mechanism for this is `docs/13-scale-relative-simulation.md` (*"both simulation and rendering cost
should scale with what is observable from the current viewpoint … detail emerges continuously: star
field → planet disk → landscape → terrain → the rock → its grains"*), with `docs/44` (resolution
EXTENT — how much to resolve) and `docs/47` §1 (resolution GRANULARITY — how fine). This charter states
the promise; those state how it is met, and the split is deliberate — restating their content here would
itself be two answers to one question. **Neither axis is built yet**, and that is the single largest gap
between this promise and the code: the resolution-EXTENT admission test is built and tested but unwired
(`resolution::admission_depth` — `ResolutionField::update` still hardcodes `necessity_depth = 1.0`; the old
`MATERIALIZE_CAP` clip was DELETED with the terrain scene, not wired), and granularity is design-only — the GPU `Particle` struct
has no size field and `part_half` is a per-dispatch uniform, so two grain scales cannot coexist in one
scene. A tyre contact patch needs centimetre grains in the same world a meteor uses metre grains; until
that lands, the span above is a promise the code cannot yet keep.

This doc exists so the ledger below is **read, not rediscovered**. Every session so far has re-derived
some part of it from scratch. `docs/32 §4` maps the forks structurally; this doc states the *rule* that
makes a fork acceptable or not, and keeps the running list of open violations with their evidence.

---

## 1. The rule: specialization vs violation

Not every difference is a breach. The criterion is sharp:

- **Legitimate specialization** — the *physics itself* differs, so the numerical treatment differs.
  Stiff granular contacts need an implicit/semi-implicit integrator to stay stable; self-gravitating SPH
  needs a symplectic leapfrog to conserve energy over orbits (docs/38 §"what unified GPU path does NOT
  mean"). Forcing one integrator on both is unstable or ruinously slow. Boundary conditions likewise:
  the terrain band models Earth as a rigid heightfield because you are standing on a 96 m patch, not
  simulating a planet's interior.
- **DECLARED (a resolution IOU)** — the resolved physics is real and correct but **unaffordable at the
  timescale humans and models need in order to play with it and learn from it**. So the outcome is
  computed from the material's own data and rendered, instead of being allowed to emerge. This is
  legitimate, and it is where most of the interesting engineering will live for years.
- **Violation (a fudge)** — the *same physical question* gets two different answers. Two implementations
  of one law that can drift. A law implemented completely in one place and partially in another. A
  quantity conserved in one scene and not in the other. Or a number chosen because the result looked right.

### Two things "no fudge" does NOT forbid (Robin, 2026-07-24)

The no-fudge Law is aimed at the ENGINE's physics, and even there it is not an absolute — it is bounded by
the compute we have. Two legitimate things are easy to mistake for fudge and must not be:

1. **A scene declaring initial conditions is not a fudge — it is what scenes are for.** Creating matter at a
   specified position and velocity (a meteor swarm arriving from outside Earth, a moon on a declared orbit)
   is honest ABRIDGEMENT: the scene skips the uninteresting prior history and starts the matter at the point
   that matters, in its true state. That is the ICs-not-consequences Law applied to scenes — a declared
   velocity vector is honest. (The 250 m meteor drop WAS a fudge, but not for being declared: it
   misrepresented the entry — wrong speed, wrong heating, wrong path. A real approach velocity from space is
   the honest version of the same declaration.)
2. **The declared-model layer is REQUIRED, not grudgingly tolerated.** To render math-driven results at the
   compute available, the engine stands in unaffordable resolution with models derived from the real physics
   and convergent to it. That is not a compromise of the Law — it is how the Law is met at all within a
   finite budget (Law VIII's honest bound). A declared model becomes a fudge only when it stops deriving
   from, or converging to, the real math — which is exactly the test below.

### Declared is not fudged — the distinction that keeps it honest

A drive-shaft *should* shear when torque exceeds what its material can carry, and in a perfect world it
would do so because the bonds actually failed. That is far too expensive today. So we compute whether it
shears — real torque against the material's real shear strength — and render that outcome.

**That is not a second answer to the same question.** It is the same physics evaluated analytically
because the resolved evaluation sits below the resolution we can afford. The test that separates it from
a fudge:

> **Can you name the resolved computation this replaces, and would the declared answer converge to it as
> resolution rises?**

If yes, it is a declared model carrying an IOU. If you cannot name it, it is a fudge wearing a physics
coat.

The codebase already does this well, and those are the pattern to copy: `plough_loft` declares an
excavation shock finer than a grain as a **conserved momentum transfer**, says so outright, and notes
that co-motion is the physical maximum so there is no coefficient to tune; `Furrow::ejection` marks its
velocity SCALE honest and its distribution SHAPE an explicit resolution IOU *"to be DELETED once particle
count is high enough for the flow to emerge on its own."*

Compare the fudges: `MAX_EJECT = 0.045`, `steep_drop = 3` (retired 2026-07-19, docs/45), a hardcoded
damping ζ. None names a resolved computation; none would converge to anything.

### The horizon — write IOUs so a descendant can delete them

This layer is a **compute-era limitation, not a design preference**. The purpose is to render real
physics on the timescale and compute humans and LLM models need to enjoy and learn from it. If compute
becomes cheap enough — quantum or otherwise — descendants of this engine should be able to delete the
calculate-and-render step and simply let the drive-shaft shear.

That only works if IOUs are written *for deletion*: each declared model must state the resolved
computation it stands in for, so retiring it is a substitution rather than an archaeology project.
**A declared model with no stated resolved counterpart is a fudge, because nobody can ever retire it.**

The test any change must pass: **does this reduce the number of places where one physical question has
two answers?** A declared model does not add an answer — it defers one, in writing. If a change adds a
genuine second answer, it needs a physics reason, written down, or it is debt.

## 2. What is already unified (do not fork these)

From docs/32 §4, and it is a real achievement worth protecting:

`granular::Contact` + `contact_accel` · the SPH kernel `sph_w`/`sph_dw` (one kernel for `AirField` AND
aggregate vapor) · `Furrow` + `ExcavSurface` + `ejection` + `ejecta_energy_scale` (ONE excavation
primitive — but see the caveat below: the LAW is one, yet neither production path CALLS it today) ·
`plough_loft` · `deposit_resting_grain` (one de-resolution primitive; CPU-only since the GPU debris
readback was deleted with the terrain scene) · `Body` as the universal particle · `Material`/`LayeredBody`
as the universal matter description · `zeta_for_restitution` (bond damping and contact damping from one
derivation).

> **Caveat verified 2026-07-23:** "unified" here means the LAW exists once and must not be re-derived — it
> does NOT mean every scene routes through it today. The `Furrow` excavation primitive is real and tested,
> but the terrain meteor still uses `matter::impact`'s inline voxel loop and the giant impact uses the SPH
> path; neither calls `Furrow`. So "do not fork" is guidance for when these are wired, not a claim that they
> already share one call. Routing `matter::impact` through `Furrow` is a tracked refactor, not done.

## 3. The conformance ledger — open violations

Each entry: what has two answers, the evidence, and where it is tracked. Structural forks 1–4 are
docs/32 §4's; the rest were found since and are recorded here so they are not rediscovered.

The **consumers** column (added 2026-07-22) lists the scenes/modules that actually INSTANTIATE the
physics in question, verified by grep on that date, never assumed. Where it reads **none**, that is
the point: a verified law with zero consumers stays OPEN no matter how green its tests are, because
its tests pass whether or not anything calls it (see §7).

| # | one question, two answers | evidence | tracked | consumers |
|---|---|---|---|---|
| 1 | ~~**Two container universes** — `Aggregate` (`Vec<Body>`, f64) vs voxel `World` + GPU f32~~ **CLOSED 2026-07-23 for the fork this row named**: the orbital scene's impact debris. The CPU `Aggregate` debris path is deleted: `OrbitDemo` no longer holds a `moon_debris` and never calls `impact::build_impact_debris*` (zero grep hits in `lib.rs`); a dropped moon resolves through the same GPU SPH machine as birth. Tests: `gpu_sph::a_dropped_moon_impact_leaves_most_matter_bound_on_the_sph_path` (the ported physics pin: bound fraction + emergent shock heating, at the assembly + CPU-KDK seam), `a_moon_drop_builds_and_strikes_through_the_same_assembly`, `a_live_drop_assembles_n_materials_on_its_own_geometry`. What `Aggregate` still hosts is NOT this row's fork: the ground scene's cohesive body (`simulation.rs:109`, `Aggregate::cohesive`; row 4 / docs/61 IOU 2 territory) and the atmosphere/gravity module tests | docs/32 §4.1 | docs/33 | closed (orbital debris); remaining `Aggregate` production consumer: `simulation::Simulation`'s cohesive body only |
| 2 | **Four integrators over one law** | docs/32 §4.2 | docs/38 (partly legitimate — see §1) | GPU KDK (`gpu_sph::encode_kdk`): `OrbitDemo`, birth AND the live moon-drop (`lib.rs:2246`). CPU KDK block (`Aggregate::step_block`): **none** since 2026-07-23: its consumer was the moon-drop debris, retired with row 1; `Aggregate::step` (per-substep) remains consumed by the ground scene's cohesive body (`simulation.rs:354`). CPU Euler settle (`MatterSim::step`): `Simulation` (Ground). GPU trapezoidal-implicit (`particle_step.wgsl`): `GpuProbe` only. `AirField` SPH relaxation: **none**. CPU KDK + adaptive dt (`hydrostatic::step`/`relax_step`): **none in a scene** (production builds `HydroBody` seeds and relaxes on the GPU via `cs_relax`); it is now the native suite's headless twin of the SPH machine (`a_dropped_moon_impact_leaves_most_matter_bound_on_the_sph_path`) |
| 3 | ~~**Rigid-boundary fork** — in an impact Earth is simultaneously materialized grains AND a rigid boundary~~ **CLOSED 2026-07-23 in the scene that had it**: `OrbitDemo`'s moon-drop arm (the one that set `boundary` + `boundary_hole` + `gravity_source` around a live debris cloud) is deleted with the CPU debris path; every resolved impact in the orbital scene now runs Earth as particles through the SPH machine, no rigid boundary. Tests: same set as row 1. The boundary machinery survives only inside `impact.rs`'s builders (`impact.rs:561-577`), whose remaining callers are that module's own physics tests: measurement fixtures, not a scene | docs/32 §4.3 | docs/33 | closed (no scene instantiates the fork; `impact::build_impact_debris_between`/`_scaled` are test-consumed only, verified by grep 2026-07-23) |
| 4 | **Two rigid-body reps** — `body::Sphere` vs the cohesive-`Aggregate` probe | docs/32 §4.4 | docs/38 4c′ | **none**: `body::Sphere` is built only in tests and `Simulation::step` passes an empty body slice (`simulation.rs:164`); the cohesive-`Aggregate` probe lost its scene with the terrain deletion (docs/50), leaving `PROBE_LATTICE`/`PROBE_STIFFNESS_CAP` (`lib.rs:291`) consumerless |
| 5 | ~~**Slope stability is half a law.**~~ **CLOSED 2026-07-19.** Terrain and grains now read the same `friction_coefficient` through one law, `granular::face_stable`; `steep_drop` is retired | was: `granular.rs:73` vs `matter.rs:538` (`h_crit = c/ρg` alone), non-convergent at 106→622 grains/pass. Now: fixpoint on the second pass, settled slope asserted against the DB μ, pristine terrain a no-op (470→0 grains) | **docs/45 §7** | closed |
| 6 | **The de-resolution ladder stops one rung short.** grain→voxel works; voxel→field is built but never triggered. **NARROWED 2026-07-19:** the mechanism is now SAFE — one authoritative `World::ground_top_voxel` answers "where is the ground" for voxels and field alike, and the GPU heightfield, the CPU bilinear surface and the rendered cap all read it. What remains is the TRIGGER, plus `patch_resolved` being one bool for the whole 96 m patch while demotion is per-column | was: three different answers to one question — GPU heightfield read raw voxels (demoted column ⇒ grains fall through), the cap read raw `terrain_height` (demoted crater renders as untouched ground), probe read `bulk_height`. Measured: 98% of grains return (3,605 → 78) | docs/47 §5, docs/39 item #4 | **none** for the open half: `World::demote_column_to_field`/`demote_patch_to_field` have zero production callers (the only call sites are `mesher.rs` tests, e.g. `mesher.rs:1003`); nothing in `Simulation` or the Ground scene ever triggers voxel→field. **NARROWED 2026-07-23 (docs/61):** the missing TRIGGER now exists for the rung above — demote-on-quiescence is `recohere::recohere_settled` (quiescent speed `sqrt(2gΔ)`, sustained one cell dynamical time `sqrt(2Δ/g)`), consumed in production by `Simulation::step` → `recohere_when_settled` (the Ground scene and `bin/run-definition`), folding a settled particle field back to voxels mass- and material-conserving. The voxel→field rung below it is still untriggered, and the SPH-remnant wiring is docs/61's flagged IOU |
| 7 | **Promotion is gated visually in one doc, physically in another.** docs/30: the trigger must be "a physical error bound … never a visual one". docs/39: gate on "camera-visible ∧ interacting" | the two docs, directly | **docs/44** | the gate that actually runs is docs/39's visual one: `Simulation::step` materialises on `(c - camera).length() < view_r` (`simulation.rs:156`) feeding `ResolutionField::update`, consumed by the Ground scene and `bin/run-definition`; docs/30's physical error bound has no consumer |
| 8 | **The honest footprint is computed, then discarded.** `crater_radius` from `V = E/σ` is derived, then clipped by `MATERIALIZE_CAP = 14.0`, and `resolve_patch` resolves the whole 96 m patch regardless | `lib.rs:858`, `lib.rs:992` (self-flagged) | **docs/44** | the evidence sites are gone: `MATERIALIZE_CAP` and `resolve_patch` were deleted with the terrain scene (docs/50), zero grep hits today. The question stands: `damage::crater_radius` is consumed by `interaction.rs:102` swept-collision effects (`OrbitDemo`), and the ground path clips its materialised crater at `MatterSim::impact`'s flagged `MAX_R = 24` LOD guard (`matter.rs:190`), consumed by `Simulation` (Ground) |
| 9 | **Matter leaks at the seam — and it is a DOMAIN property, now measurable per definition (docs/54).** Grains that leave the patch are culled by `matter::step`; the loss is invisible from a particle count, since de-resolution looks identical. `run-definition` reports created/returned/in-flight/lost: a 96 m patch with gentle events loses **0.0%** (260 created, 260 returned), while a 48 m patch with an energetic impact loses **28.8%** (6,328 created, 1,822 lost). The earlier ~2% figure was the big patch only. Sizing the domain to the event is the open question | measured 2026-07-21 from `definitions/ejecta-ground.json` and `definitions/small-island.json` | **docs/54** — was: ~2% of debris never returns to the field (deposition refused inside a dynamic body; the water branch is a self-flagged static-sea placeholder) | measured: 78 of 3,605 grains stranded per event, monotonic | this doc | `simulation::Simulation` (the Ground scene; `bin/run-definition` is the consumer that measures the loss) |
| 10 | **Vehicle/probe never contacts debris.** The probe is a CPU `Aggregate`; grains live in a GPU buffer. Coupling is a bounding-sphere exclusion, not contact | `lib.rs` settle path; `matter::couple_body` exists but is called only from tests | docs/38 4c′ | **none**: `matter::couple_body`'s only callers are its own tests (`matter.rs:2210`), and `Simulation::step` passes `&[]` for bodies (`simulation.rs:164`) |
| 11 | **An asteroid-era constant still runs in an Earth-g scene.** `MAX_EJECT = 0.045` m/s, capped "below the world's ~7 cm/s escape velocity" — a 0.1 mm ballistic hop at 9.81 m/s² | `matter.rs:41` | this doc | **none**: `MAX_EJECT` (now `matter.rs:63`) lives in `MatterSim::dig`, which no scene calls; its only callers are the `isotropy.rs` regression suite. The constant is unconsumed AND still wrong, in that order |
| 12 | **The render asserts a physical state the simulation does not have.** Every Earth scene draws an honest Rayleigh sky over a **vacuum**: `atmosphere::AirField` — pressure-layered, with verified hydrostatic balance, momentum-conserving drag and hypersonic entry heating — is instantiated in **no scene** | `grep AirField crates/engine/src/lib.rs` → nothing; 11 atmosphere tests pass regardless | **docs/48** | **none**: `AirField::new` appears only in `atmosphere.rs`'s own tests; every reference outside that file is a doc comment |

| 13 | **Incandescence has two curves.** "What colour does matter at temperature T glow?" is answered by `emission::incandescence` (docs/20, natively tested, returns premultiplied `[r,g,b]`) AND by a second copy inside `mod app` for the space band (returns `[r,g,b,intensity]`). They agree only on the 800 K glow threshold | both read directly: `emission.rs:13` ramps intensity `(T−800)/2200` capped at 4 with blue from 2600 K; the space-band copy ramps `x=(T−800)/2400` saturating at 3200 K with blue from `x>0.55`. At 2000 K one gives `[0.545, 0.297, 0]`, the other `[1.0, 0.5, 0.0]×0.6` | this doc — found 2026-07-20 during the docs/33 render-scaffolding lift; NOT unified in that PR because collapsing them changes what the space band looks like, which needs its own rig verification | `emission::incandescence`: the Ground scene only (`ground_scene.rs:764`, `:782`). The `mod app` copy (`lib.rs:3836`): `OrbitDemo` only (`lib.rs:3246`, `:3323`, `:3421`). `Terra` consumes neither |

| 14 | **Scene KINDS are code; scene INSTANCES are already data — except one.** Robin's requirement: a scene should be object/assembly definitions, coordinates and materials and "should NOT require special mods of the engine itself". **NARROWED 2026-07-21 by measurement, after the row was first written too broadly.** `orbit.html` and `twomoons.html` are the SAME script and the SAME `OrbitDemo` differing only by `data-world=…/world.json`; `terra.html` likewise. Those are data. What remains code: (a) the scene KIND — adding a genuinely new kind means a new `#[wasm_bindgen]` struct with its own pipelines and render loop, and deleting terrain cost **1,516 lines of `lib.rs`** plus a public-API symbol and a build entry; (b) **`birth.html` alone still has NO world file** — `data-scene="birth"` selects a hardcoded path whose initial conditions (body radii, materials, proto-Earth spin, impact geometry 1.15·v_esc / b≈R_e / d₀=1.6·contact) are Rust constants | pages measured directly; birth's ICs at `gpu_sph::build_impact_bodies` + `assemble_from_relaxed` | **docs/51** — (b) first, since it is the last scene on a code path | the scene kinds: `OrbitDemo` and `Terra` (`lib.rs`) plus `Ground` (`ground_scene.rs`), each a `#[wasm_bindgen]` struct. The birth ICs: `OrbitDemo` via `gpu_sph::build_impact_bodies_from`/`assemble_from_relaxed_with`; note `web/birth.html:77` now carries `data-world="/worlds/birth/world.json"` and the file exists, so (b) is narrower than first written |

| 15 | ~~**Deleting terrain orphaned three verified systems.**~~ **CLOSED 2026-07-21 (docs/53)** — `crate::simulation::Simulation` builds and steps them from a `"ground"` world DEFINITION (production code: 8 `MatterSim` refs, 4 `ResolutionField`, 1 `world::generate`), so the consumer is a file and no scene's deletion can orphan them again. Verified end to end from `definitions/ejecta-ground.json`: an effect propagates analytically off-camera, materialises 257 grains on entering view, and every one de-resolves back to the world (644,190 → 644,450 voxels, **+260** = 257 grains + 3 impact particles — matter conserved). Was: It was the ONLY production consumer of `matter::MatterSim` (the shared matter path), `resolution::ResolutionField` (docs/49 camera-driven resolution, wired the day before), and the voxel `world::World`+`mesher`; the granular GPU pipeline is now reachable only from `GpuProbe`, a compute-only diagnostic with no canvas | measured 2026-07-21 after the deletion: **0** references to `MatterSim`/`ResolutionField` anywhere in `lib.rs`; all 6 `world::generate` calls are inside `#[cfg(test)]`. Every test still passes, which is why it is easy to miss | **docs/51** — a requirement on the NEXT scene: re-consume them or delete them. This is docs/48's wiring pattern at its sharpest: physics wired into one place, and that place deleted | closed |

| 16 | **Scenes build their own worlds instead of using ONE Earth.** Robin: *"Terra should occur naturally from definitions of material, biomes, etc… a fully materialized object, reusable between scenes. Then this scene would simply be using that planet/solar system."* **NARROWED 2026-07-23 (docs/59 order-of-work 1): the IDENTITY and PARAMETERS are one value now.** All three shipped scenes read `assets/bodies/earth.json` and nothing else: the space band's `EARTH_*`/`MOON_*` constants are retired for cached reads of the definitions (`lib.rs` `EARTH_PARAMS`/`MOON_PARAMS`), Terra inherits its radius from the body the world names (`declared_planet_radius`; `worlds/earth/world.json` carries no `radius_m`/`mass_kg`), and the ground patch declares WHERE it sits (`lat`/`lon` in `worlds/ground/world.json`) and derives g, air pressure AND its material strata from the body at that site (`LayeredBody::surface_strata`; the world file's private strata list is gone). Enforced: `one_earth_tests::the_three_scenes_read_one_earth` asserts digit-identity across the three paths, and the `laws` scans (`DEFINITION_OWNED`, planet-block override check) keep copies from creeping back | measured 2026-07-21 (and still true): the ground patch is 96 m across = **0.00024% of Earth's circumference** - walk ~48 m and you reach the edge of the world. What remains of this row: the patch is still a PROCEDURAL cube that merely *names* its site - its relief does not sample the body's elevation rasters at the declared lat/lon (the `terrain_height` TODO(ETOPO)), no camera path connects Terra's globe to the patch (docs/59 items 2–5: descent camera, materialization, hand-down, re-coherence), and the strata band thicknesses are a declared log2 vertical LOD, flagged in `surface_strata` | **docs/23** (the north star: one Earth, the Moon hits it, zoom to the ball) + **docs/13** (scale-relative) + **docs/43** (worlds as data) + **docs/59** (the zoom - this row's remainder is its order of work) | one Earth's parameters: all three scenes (`lib.rs` shared params + `declared_planet_radius`, `simulation.rs::from_definition` via `GroundDef.planet`+`lat`/`lon`). Still scene-local: the patch's procedural relief (`world::terrain_height_with`) and its placement-blind coordinates |
| 17 | ~~**Four answers to "what is the disk?"**~~ **CLOSED 2026-07-23** — `gpu_sph::DiskView` is the single definition (COM, the 85%-mass remnant, the bound+perigee filter, the `find_clumps` call); `disk_stats_json`, `moonlet_bodies`, `largest_moonlet_orbit` and `disk_moonlets` are thin callers. Was: each carried its own copy of the preamble, and they had already DRIFTED — only `disk_stats_json` tested boundedness BEFORE geometry, so the render spheres, the escape check and the geologic hand-off all counted ESCAPING matter as disk | the fix existed in ONE copy with a comment explaining itself (*"Asking `perigee` for boundedness was safe only while it refused to describe hyperbolic orbits"*) and was never propagated to the other three. Net -34 lines. `escaping_matter_is_not_counted_as_disk` now pins it for every consumer at once; mutation-checked against the pre-fix behaviour | **docs/44** — this is the operator de-resolution consumes, so the drift would have been inherited by the clumping pass that collapses a united disk into a body | closed |
| 18 | ~~**The render overrides the physics for cap impacts — Earth cannot be seen to crater.**~~ **CLOSED 2026-07-23.** The globe now carries a measured crater: `accretion::crater_bowl` sizes the bowl's depth AND radius from the excavated volume (the target material lifted above its pristine radius), and `globe.wgsl::crater_sink` sinks the surface into a paraboloid `depth·(1−(θ/θr)²)`. So the depression shows ON the intact bulk sphere — the sphere is still drawn coherent, but it is no longer the whole story. Diagnosing it also found the real defect: the bowl WAS reaching the shader, but its radius came from a `0.72` dial while only depth was measured, giving d/r≈0.06 (a flat saucer); both now come from the one volume plus the sourced simple-crater shape d≈0.4r (Melosh). Rig-measured after the fix: d/r=0.40. Was: `lib.rs` computed `accretion::representation(...)` then discarded it whenever `sph_cap` was set, drawing a flawless sphere with coherence pinned to 1.0 | the numbers disagreed with the picture: birth (no cap) sheds real Earth material (rig 2026-07-23, disk 0.28 M☾ / 56% Earth) while the cap path rendered the target untouched. Now `a_crater_bowl_keeps_its_shape_at_every_size` pins the geometry (it caught a factor-of-2 volume error a screenshot could not) | **Law VI** — the render now REPORTS the excavation instead of overriding it. NOTE (still open, smaller): a fully unambiguous daylit face-on crater shot needs a camera-aim hook the scene does not expose; and the bulk sphere's own coherence is still 1.0, correct only because the cap depression sits on top | closed |

| 19 | ~~**Universal constants retyped across the engine.**~~ **CLOSED 2026-07-24.** `orbit::G` and `blackbody::SIGMA` are the only definitions; every module delegates, and `laws::SINGLE_SOURCE` now counts both so a sixth copy fails the build. Was: **G written out in five modules** (`gravity`, `planet`, `damage`, `accretion`, `orbit`) plus **two shaders**, and Stefan–Boltzmann in three — one of them the truncated `5.670e-8`, so `aggregate`'s cooling moonlet and `atmosphere`'s ablating meteor radiated by *literally different constants* | found by counting, not by reading: nothing had drifted far enough to fail a test. The WGSL copies were invisible to a Rust-only search and only surfaced once σ was registered in the existing guard | this doc — the shader copies are legitimate (WGSL cannot read a Rust constant) and are PINNED by `pinned_constant_tests::the_shaders_gravitational_constant_matches_the_engines`, the `EARTH_RADIUS_M` treatment: a second copy is honest only when something fails if the two disagree | closed |
| 20 | **Entry heating uses a continuum law across a path that is not all continuum.** `atmosphere::stagnation_heat_flux` is Sutton–Graves, correct in dense air and correct for the ground scene it was built for. The orbit-to-ground swarm (docs/59) flies the *whole* path, and above ~100 km a metre-class body is in transitional and then free-molecular flow, where the classical meteor law `Λ·½ρv³` is the right one. The engine applies one of the two everywhere instead of choosing by regime | the regime is COMPUTABLE, not a judgement call: the mean free path follows from catalogued data — `λ = (μ/ρ)·√(πM/2R_uT)` over air's `dynamic_viscosity` and `molar_mass`, which reproduces the measured 68 nm at sea level — so Knudsen `λ/2r` could select or blend the laws. Magnitudes at 100 km, 1 m body, 20 km/s: Sutton–Graves 1.0e6 W/m² vs free-molecular ~2e5 (Λ=0.1), so the error is bounded and not catastrophic at metre scale — but it grows for small fragments, which is exactly the population that decides what burns up | **docs/59** — the flagship's visible trail forms in this regime. Note `dynamic_viscosity` is in `materials.json` but not yet parsed by `materials.rs` | not surveyed — row added after the 2026-07-22 consumers survey |
| 21 | ~~**A body ablating in air is treated as isothermal.**~~ **CLOSED 2026-07-24.** The heat front now advances at the material's own thermal diffusivity `α = k/(ρc)` (`atmosphere::soak_depth`, `heated_mass`), so only the mass the heat has REACHED takes part in the temperature change — and bulk heating survives as the limit the model reduces to once the skin reaches the radius. `thermal_conductivity` is now sourced in `data/materials.json` for 28 of 29 materials (`hh_plasma` left unknown); `docs/04` had reserved the field and nothing had ever used it. MEASURED before → after, 8 iron fragments at 15 km/s from 200 km: a 30 cm parent went 0% ablated → **4.3%**, 1 m 0% → 0.75%, 3 m 0% → 0.03%, and every size up to 3 m now reaches iron's 3134 K boiling point instead of a few hundred K. The live swarm's ablated mass went 397 kg → **15,871 kg**. Two things fell out that nothing declares: the heated layer SETTLES at ~1.4 cm for iron (ablation strips it as fast as conduction deepens it — the classical `δ = α/2v` boundary layer of an ablating surface), and basalt ablates MORE than iron at every size because its 21×-lower diffusivity keeps the heat at the surface, which is why stony meteorites carry thin fusion crusts. A 10 m body still does not reach boiling, and that is Sutton–Graves being right rather than a limit: flux goes as `√(ρ/R_n)`, so a blunter body is heated less — the reason re-entry capsules are blunt. Was: heating the whole mass at bulk heat capacity made thermal response scale with VOLUME, so a 0.5 m iron body flew a perfectly correct 20 km/s entry and barely warmed | the giveaway was that the model got the OBSERVABLE right (small meteoroids burn up, iron meteorites land) for the wrong mechanism — it was only valid below the thermal skin depth, √(αt) ≈ 1.5 cm for iron over ten seconds | **STILL OPEN, smaller:** only the skin's temperature is tracked, so the interior is unmodelled and the heat that conducts past the front is not accounted anywhere; the resolved computation is a real temperature PROFILE through the body, and the moving-boundary treatment of a receding surface is its refinement | closed for the defect named; the remaining interior-profile half not surveyed |
| 22 | **Ablated mass reaches the atmosphere and stops.** `atmosphere::Trail` closed the conservation hole (shed vapour used to cease to exist) but its end state is `merged_kg`: mass booked into the air, permanently | in reality that vapour condenses to micrometeoritic dust and settles out — ~40,000 t/yr on Earth — so the atmosphere is a staging post, not a sink. Named rather than lost, which is the difference between an IOU and a leak | **docs/59** — the resolved counterpart is `AirField` parcels that condense and fall under the same gravity as everything else. Robin (2026-07-24): booking it is the right first cut, *"understand it will eventually fall to the earth"* | not surveyed — row added after the 2026-07-22 consumers survey |

| 23 | ~~**A second answer to "how steep can this material stand".**~~ **CLOSED 2026-07-24, same day it was opened — by Robin, mid-build.** `surface_detail` needed a roughness ceiling and I wrote a slope rule for it, while `granular::repose_allowance`/`face_stable` (docs/45) had carried Mohr–Coulomb all along *specifically* so "ground and grain answer the slope question with ONE law". Robin: *"shouldn't this material work already be hard baked into the engine? How/why were you able to change it and get it wrong with impugnity?"* — and it was wrong in both ways `granular`'s own doc warns about: friction alone (the table refutes it: dry sand grips HARDER than granite, μ 0.67 vs 0.60, yet only granite stands vertical — sand's cohesion is 0, granite's 28 MPa), then friction+cohesion added into ONE slope, when they are an OR over two different measurements (friction on the slope, cohesion on the bank). FIX: `relief_amplitude_m` holds no slope physics — it asks `granular` and only converts a permitted drop into an amplitude; `repose_allowance_on(mu, r, quantum)` takes the field's quantum as a parameter (`SLOPE_QUANTUM_M` belongs to the voxel field, not to the physics) so delegation is possible at all; `critical_bank_height` gives `h_crit` a home (it was inline in `matter.rs` while its partner term lived in `granular` — half a law in each place) | **the reason it was possible is the ledger entry that matters**: `laws.rs` hardens CONSTANTS (`SINGLE_SOURCE`) and keeps scenes out of collision (`COLLISION_PRIMITIVES`), but a DERIVED LAW had no guard, so a reasonable author who did not find the primitive could contradict it with every test still green | **now guarded by a CONSISTENCY test, not a grep**: `generated_relief_is_stable_by_the_engines_own_slope_law` hands every octave of generated relief back to `granular::face_stable` as the face it implies — 7 materials, 6 orders of wavelength — and requires it to be held up. A name check would not have caught this. **The pattern generalises: any module that derives a quantity another module owns should be pinned by agreement, not by convention.** | closed |
| 24 | ~~**Impact energy had two formulas.**~~ **CLOSED 2026-07-25** during the integration of Sean Reid's `upstream-5-ground-ball`, and neither side went looking for it. `interaction::detect_swept` priced every body-body collision at **½·μ·|Δv|²** with the reduced mass — its own comment: *"the energy actually available at the contact frame, not either body's kinetic energy in an arbitrary frame"* — while the terrain arrival path computed **½·m·v²** inline. They were never rivals: as the struck mass grows without bound, μ → m and Δv → v, so **bare terrain is the IMMOVABLE LIMIT of the general collision, not a case beside it**. One `flight::delivered(body, target_vel, target_mass_kg)` now answers, `None` meaning matter too massive to move. Found only because his ball is a target that CAN recoil, which forced the question "what is the energy when the thing struck is free to move" to be asked of the ground path too | the two expressions read directly: `interaction.rs:196` vs the old `0.5 * b.mass_kg * speed * speed` in `flight::Flight::step_bodies` | this doc — pinned by `flight::an_immovable_target_is_the_reduced_mass_limit`, which asserts the finite-mass case CONVERGES to the immovable one over 10³…10¹⁵ × the striker's mass rather than merely agreeing at one point | `flight::Flight` (Ground + Terra entry) and `interaction::detect_swept` (`OrbitDemo`) now share the one formula |


| 25 | **The re-coherence crossing has no energy sink, and re-cohered rubble is instantly bedrock-competent (docs/61) - three debts.** (a) *Binned heat dropped:* a grain's carried temperature has nowhere to go when it becomes a voxel, because the voxel store holds no thermal state at all. (b) *Settle kinetic energy zeroed:* sub-threshold motion (bounded per grain by `m g Δ`, the quiescence criterion's own quantum) is dissipation, and dissipation becomes heat, not nothing. Both are now MEASURED at the crossing instead of silently zeroed: `Recohered::binned_kinetic_j` / `binned_heat_j` book energy-in minus remainder-out per column (heat only where `Material::specific_heat` is sourced; an unknown c stays unknown), `Simulation` accumulates them (`recohered_kinetic_j()` / `recohered_heat_j()`), and `bin/run-definition` prints them whenever the rung ran. The deferred computation both name: a voxel-side thermal field (temperature or heat density per voxel or per column) that `matter::deposit_grain` deposits into; the closing test is today's conservation test flipped from measuring the loss to asserting the deposit lands in the field, e.g. `the_crossing_deposits_its_energy_into_the_voxel_thermal_field` (today's measuring form: `the_crossing_measures_the_binned_kinetic_energy_and_carried_heat`). `Aggregate::drain_settled` and the per-grain settle path (`deposit_resting_grain` from `matter.rs:1155`) share (a) and (b) and are not yet measured. (c) *Instant consolidation, design only:* ejecta re-coheres at reference density and full material strength - `deposit_grain` writes ONE u16 material index (`matter.rs:137`) and a voxel is nothing but that index (`world.rs:60`), so fresh rubble reads back the intact material's `density` and `fracture_strength`, when real fresh ejecta is porous (bulking 1.2 to 1.5x its source volume) and unconsolidated. Deferred: a consolidation state (porosity and strength fraction) on re-cohered matter, relaxing toward intact over a physical timescale (overburden pressure and time, from the material's own data); the test that would close it: `fresh_recohered_ground_is_porous_and_weak_and_consolidates_toward_intact` (fresh re-cohered gravel occupies more columns per kg and detaches under a tool an intact voxel shrugs off, then converges to intact as consolidation runs) | the voxel store's whole per-cell state is `voxels: Vec<u16>` (`world.rs:60`); `grep temp_k crates/engine/src/world.rs` returns nothing; the measurement is `binned_kinetic_j` in `recohere.rs`, asserted conserving by `the_crossing_measures_the_binned_kinetic_energy_and_carried_heat` | **docs/61** | the measured half: `Simulation::recohere_when_settled` accumulates, `bin/run-definition` reports the `recohered` line. The thermal sink and the consolidation state: **none** exist to consume |

| 26 | **The zoom's upward rung is built, verified, and ~~deliberately unwired~~ WIRED (narrowed 2026-07-23).** The camera-driven trigger landed: `crate::site` derives the view-necessity threshold (one coarse particle's matter share against the docs/49 angular budget, `d* = s/theta`, measured from the live field when one exists), and the space band materializes the Ground Zero world's declared ball and strata patch through this rung on the descending crossing, folds through the docs/61 gauge on the ascending one, with the ledger and every refusal on the HUD. (a) RESOLVED 2026-07-23: a relief stall is a true fixed point of the shifting (measured: the site's relax oscillates around 4.9e-2 to the 5000-iteration cap), caused by the release criterion's own two smoothing scales; the rung now judges a PROVEN stall against the coarse field's measured disagreement across exactly those scales (refine::scale_mismatch) and releases with the residual and the derived bound both stated (site: 4.8e-2 under 9.1e-2; relief lattice: 5.1e-2 under 1.2e-1; the strict 5e-3 remains the criterion for every converging run, and an unguarded truncation still refuses); (b) an ISOLATED sub-resolution body (the ball) has no coarse environment to relax against (measured divergent), so it splits exactly, unrelaxed, stated; (c) NARROWED 2026-07-23: the mid-event hand-down landed as BOUNDARY STATE - the guard band re-samples the coarse field once per coarse step (`site::resample_guards`; the guards ARE the coarse field at the boundary, so the event's energy arrives through state, not a formula), `site::EventWindow` books the window within its stated 1e-6 bound, and the pi-scaling gate has its end-to-end consumer (`refine::measure_crater_rim` at the field's own quantum, prediction frozen from the measured contact state, verdict or stated sub-quantum refusal on the HUD). NARROWED FURTHER 2026-07-23: the released site ENTERS DYNAMICS (site::SiteDynamics) - the fine parcels are cohesive matter through the ground scene's own builder (simulation::CohesiveBody::from_parcels, one law for the site's ball and the ground scene's), stepped under bonds, gravity and the one terrain-contact law, driven during an event by the guard band's booked step-to-step delta through the one door (Aggregate::deposit_impact), fate by damage::classify per parcel, verdict word shared, fold via the docs/61 gauge; the release gate still keeps an UNRELEASED patch out of dynamics. Still open in (c): the N-material upload reaching the guards (the sampled state inherits the coarse field's EOS collapse, the named IOU); site-local excavation of the floor law (the parcels carry the crater's truth, the declared surface stays the floor); and the site clock's real-second rate against the scaled celestial clock (a stated compute bound; verdicts are deposit-driven and land at the coarse step). `crate::refine` is docs/59 item 3, the conserved hand-down: one-shot icosahedral splitting with a mandatory retained center child (stencil constants re-derived by least squares for the engine's OWN kernel, separation 0.3051 h and child smoothing 0.7915 h in the support-radius convention; the literature pair 0.2 h / 0.9 h measures 4.9% L2 density error on this kernel vs 0.7% for the derived pair), relax-then-release against the coarse field's interpolated density with a frozen clock (release is the stated 5e-3 relative density-error bound, never an iteration count), one-rung-per-interface discipline and contamination as refusal, the five-quantity conservation ledger, and the Holsapple-Housen pi-scaling gate (v2.2.1 rows) with its live crater consumer | native tests: exact conservation of mass, momentum, angular momentum, kinetic and internal energy on the split; density blip measured 7.5e-2 released to 5.0e-3 on uniform basalt and 9.5e-2 to 5.0e-3 across a basalt/iron interface; contamination and interface-ratio refusals with stated reasons; the gate reproduces a hand-computed Meteor Crater example; the window book is pinned by a quiet-hot-quiet pulse across the shipped site's boundary (guards carry exactly the field's state, the un-stepped fine children bitwise untouched, mass single-owner) and the rim measurement by synthetic bowls (one-quantum dimple refuses, intact shell refuses); the relief stall's derived-bound release is pinned with the bound recomputed independently in the test; the DYNAMICS are pinned by the quiescent fall-rest-settle-fold arc and by the demo beat at the seam (a moon-magnitude boundary pulse delivered through the door SHATTERS the site's ball by classify alone - measured fate 0 solid / 42 fractured / 171 molten / 60 vapor, mass conserved, mid-event fold refused). Green whether or not anything calls it, which is exactly why this row exists (§7) | **docs/59** (order-of-work items 2 and 3) | `crate::site` (the trigger, the spec, the hand-down, the fold) and `OrbitDemo::update_site` (the per-frame crossing check, the HUD line, the billboard draw); armed by any world carrying a `ground` block (`worlds/ground-zero/world.json`). (a) is closed; the row stays open for the (c) remainder |
| 27 | **Sub-raster relief is scaled by a number measured four orders of magnitude away from what it scales, so the ground is generated nearly flat — and this is why a descent reveals no detail.** `Terra::build_segment` forms `slope_fraction = tier_slope / mu` (it was `build_cap` until the surface collapsed into one segment, docs/63 — the formula moved unchanged), where `tier_slope` is the elevation gradient over a baseline of TWO RASTER TEXELS (39 km on the shipped 2048x1024 Earth) and `mu` is a grain-scale friction coefficient. A 39 km baseline is a REGIONAL TILT; it cannot be steep. Measured IN-SUITE on the shipped raster through the engine's own sampler, 16,384 land points: median 0.00202, p90 0.01149, largest anywhere 0.0667 — and **Everest itself reads 0.0328**, because averaging over 39 km flattens it. So `slope_fraction` is ~0.003 typically and ~0.10 at its most extreme, never the 1.0 the law is written in terms of. Compounding it, `relief_amplitude_m` is `min(drop/2, lambda/4)` and for cohesive rock the `lambda/4` NO-OVERHANG cap binds at every wavelength below ~2 km — a HEIGHTFIELD property, not the rock's — which makes amplitude proportional to wavelength, i.e. Hurst exponent H = 1, the smoothest self-affine surface and the one whose slope is IDENTICAL at every scale. Real topography has H ~ 0.5-0.7 and gets relatively rougher as you close in. The two together are the answer to *"why does the ground flatten on descent"*: below ~20 km altitude the frame fits inside one raster texel, so measured elevation contributes only a smooth ramp and every visible bump has to come from a generator that is both scaled to ~0.003 and structurally incapable of getting rougher as you approach | measured: 1-vs-4 ground tiers is max 4 pixel values different at 100 m altitude (so it is NOT the tier ladder, which was the standing hypothesis; ★ the ladder itself was DELETED 2026-08-02 with the globe/cap collapse, docs/63 — geometry was never the right carrier for sub-cell detail, which is the explanation that measurement lacked at the time); at `slope_fraction = 1.0` the same generator gives RMS slope ~1.0 and 106 m of relief in a 109 m frame, so the amplitude law is not the limiter either; at the measured worst (fraction 0.111) it gives 11.8 m, at median land 0.36 m. ★ The first version of this measurement was taken by a SEPARATE SCRIPT that reimplemented `Raster::coords` with a half-texel offset the engine does not have: invisible across 16k points (median and p90 agreed to three digits) but it read Everest as 0.008 against the engine's 0.033. A reimplementation of the thing under test is a second answer to the same question, and moving the measurement in-suite (a `png` DEV-dependency; the browser decodes rasters at runtime, so nothing reaches the wasm bundle) is what caught it | `surface_detail::generated_relief_has_the_same_slope_at_every_scale_below_two_km` and `surface_detail::a_regional_gradient_cannot_reach_a_material_scale_slope` pin both halves; docs/59 Stage B | **OPEN — diagnosed and now PARAMETERISED, no fix applied.** The fix is NOT a multiplier (that is a dial, Law V): a wrong EXPONENT cannot be corrected by a coefficient. **MEASURED 2026-08-01, `earths_topography_is_self_affine_and_its_exponent_is_not_one`:** the structure function of the shipped raster over land (golden-angle points, great-circle pairs, both ends land, 5,846 land points and ~40k pairs per lag) gives RMS dz of 137.9 m at 19.5 km rising to 905.2 m at 625 km, a log-log slope of **H = 0.483** over 39-625 km. That is the accepted value: Turcotte's spherical-harmonic spectrum gives S(k) ~ k^-2 below 10,000 km and beta = 2H+1, so beta ~ 2 implies H ~ 0.5, and a recent Earth/Venus roughness study reports H ~ 0.55 at beta = -2.1. So the generator's implied H = 1 is off by a factor of **(19550/lambda)^(1-H)** -- **50x too smooth at 10 m wavelength**. The measurement also supplies the ANCHOR a fix needs: RMS dz at one texel = 137.9 m, a real roughness rather than a regional tilt. **Honest caveat:** the local slope drifts from ~0.77 at the shortest pair to ~0.41 at the longest, so a single H over 39-625 km is a fit, not a constant, and extrapolating it four more decades to metre scale is a DECLARED model whose resolved counterpart is finer data. FINER DATA EXISTS and is free -- Copernicus GLO-30 (30 m, TanDEM-X, COG on AWS Open Data), ETOPO 2022 (15 arc-sec ~450 m topo+bathy, NOAA public domain, the drop-in upgrade for this combined raster), AWS Terrain Tiles (global z/x/y **terrarium PNG**, no auth, RGB-packed metres -- the same encoding this engine already reads). But 30 m global is ~1.4e12 samples: unshippable whole, so it is a TILED/streamed source, and even at 30 m the generator is still what covers 30 m down to cm. **CORRECTED 2026-08-01, Robin caught it:** this row first said data was NOT an alternative, which had the emphasis backwards. Finer data is the PRIMARY fix — it shortens the generator's span from 5.29 decades to 2.48 (GLO-30) or 1.68 (z=15 tiles), cutting the H=1 error at 0.1 m from 544x to 19x or 7.4x, AND it repairs the slope_fraction category error BY CONSTRUCTION, because that bug is really that two texels is 39 km; at 30 m texels the baseline is 60 m, where real ground does approach what the material can hold. What data cannot do is reach 10 cm (2.5 decades short at GLO-30) or ship as a file (1.8 TB; a web-budget single file buys only ~5-10 km/texel). So: stream tiles, and keep the exponent for the residual. Robin's call |
| 28 | **The appearance integral is built, correct and has NOTHING TO INTEGRATE — the surface's own description is 19.5 km per texel and INVENTED.** `terra::appearance` integrates a footprint's material mixture and slope variance (docs/63), and at Terra's 469 m mesh cell over the Colorado Rockies it changes the picture by almost nothing. Not because the model is wrong — because the data cannot answer it. The land-cover raster is 2048x1024 (**19.5 km/texel**), so a mesh cell sits INSIDE one texel and the mixture has exactly one constituent; the elevation raster at the same resolution is a bilinear ramp across a cell, i.e. a PLANE, which by construction has no slope variance. Worse, `web/public/bodies/earth/SOURCES.txt` says the land cover is *"DERIVED climate approximation from lat+elevation+coast, NOT a measured land-cover dataset"* — six latitude bands in `tools/bake-earth/bake.py::bake_landcover`. **And the biome map commits a category error: land-cover class 3 ("forest") maps to material `pine`, which is pine TIMBER (albedo [0.68, 0.48, 0.21], a brown), so the Amazon and the Congo are drawn the colour of cut lumber.** ★★ **And the fix is not a better wood — it is the right PART of the plant.** Robin (2026-08-03): *"Pine Timber is always the wrong choice for flora though, we should look for 'pine needles' or 'pine leaves', same with other biomes."* What a surface shows from above is the CANOPY — needles, leaves, litter — and timber is the trunk, which is barely visible from any distance and is a different material with a different albedo, roughness and thermal behaviour. So every vegetated biome wants a FOLIAGE material rather than a structural one: pine needles, broadleaf, grass blade, scrub, litter. **This is the substance-versus-assembly distinction arriving a third time** — leaf tissue and hardwood are both substances, a canopy is an assembly of them, and the map was reaching for the assembly's least visible constituent. MEASURED at Galway 2026-08-03: frame RGB 134/119/64 IS pine timber's [0.68,0.48,0.21], and `gunmetal` is [0.55,0.42,0.24], so a bronze gun standing on "forest" is nearly the same colour as the ground and disappears into it. Robin, on seeing it: *"Currently our information is rather sparse and appears to be extrapolated based on latitude, so the Sahara is green"* and *"the Amazon Rainforest in Brazil/etc looks pretty sparse"* ★★ **HALF CLOSED 2026-08-03, and the half that closed is the MATERIALS.** `conifer_foliage` and `broadleaf_foliage` are catalogued as substances, and **their albedo is not chosen** — it is a mean of 449 measured fresh-leaf spectra (NEON domains + the UCSB HyspIRI California campaign, 98 taxa) convolved against the engine's own CIE 1931 observer under a 5772 K sun, via `blackbody::reflectance_srgb`, with `albedo_derives_from_the_measured_spectrum` re-deriving it every run so the stored triple can only ever be a CACHE. The catalogue's 400-700 nm means land at 0.084 (conifer) and 0.069 (broadleaf) against published measured values of 0.08-0.09 — an independent confirmation nobody tuned toward. The land-cover class that conflated the Amazon with Siberia was also SPLIT (3 broadleaf / 6 needleleaf), because one forest class pointing at one material would have shipped the second foliage substance wired to nothing. **Measured A/B through the engine, same rig, same sun, same camera: Galway 148.3/130.9/68.1 red-brown -> 82.1/111.6/64.4 GREEN; Amazon 148.3/132.3/67.9 -> 78.2/108.9/55.8; Siberia 146.9/124.7/66.9 -> 74.7/101.8/62.3; and the Sahara BIT-IDENTICAL either side**, which is the negative control saying only the forest classes moved. Before the split all three forests were literally the same colour. What is still open is the DATA: the cover is still latitude bands, so Ireland is still called forest when it is pasture, and ESA WorldCover is still the fix. ★ Also found and fixed on the way: `tools/bake-earth/bake.py` wrote to `web/public/worlds/earth/`, which stopped holding Earth's rasters when Earth became a shared body — a rebake would have written a full set of rasters into a directory nothing reads and reported success. | `the_shipped_rasters_cannot_express_a_mixture_at_mesh_cell_scale` measures it THROUGH the engine at the scale ladder's own site (39N, 106W, 469 m cell): mixture length **1**, sigma **< 1e-3 rad**, while the mean slope is real (>1e-4) and the mesh already carries it. The test FAILS when better land cover lands, which is its purpose. Rig A/B at 100 m altitude, main vs branch: both frames a flat green fill | **docs/63** — and it re-orders that doc's plan: the data expansion is not "later", it is PREREQUISITE for the integral to show anything. Robin's direction (2026-08-02): *"we need to vastly expand our materials to include rainforest canopy leaves, rainforest woods, different sands ... seed all that from NASA data (use geological data for regions, biome data to extrapolate flora)"*, noting *"Work on Earth is not wasted; it can all be catalogued in a model that will be used again and again."* Verified open, no-auth sources: **ESA WorldCover** (10 m, 11 classes, public S3, CC-BY), **SoilGrids** (250 m sand/silt/clay fractions, ODbL), **GLiM** (global lithology). NOT HLS — it is Earthdata-Login-gated surface reflectance in MGRS COGs, i.e. imagery rather than materials. ★ A design distinction this raises: leaf TISSUE and tropical hardwood are substances and belong in `data/materials.json`; a rainforest CANOPY is not a substance but an ASSEMBLY (mostly air, leaves at some leaf-area-index), so giving it a bulk density would declare a number that traces to nothing (Law V). **MODIS MCD43A3 is a measured global albedo product**, which would upgrade docs/63's convergence invariant from internal consistency to agreement with reality | `terra::appearance::integrate_on_sphere`: `Terra::build_segment` only. The space band's segment does NOT run it (flagged in place) — two scenes drawing one Earth differently is what docs/63 exists to end, but the corridor's descent is not frame-reproducible, so it cannot be A/B'd until docs/63 item 1c's fixed-pose rig exists |
| 29 | **The appearance integral's cost was per-VERTEX, not per-probe — and the fix was to notice there was nothing to integrate.** ~~The structural fix is a mip pyramid of moments over the elevation~~ **REFUTED BY MEASUREMENT 2026-08-02, before it was built.** `web/rig/terra_appearance_price.mjs` prices the stage the gpu-perf way (move ONE thing, re-time — never delete the stage and re-time the whole build): pinning the sample grid from 1x1 to 8x8, a **20x increase in probes per vertex, moved the rebuild not at all — 173.3 ms to 176.1 ms, FLAT.** So the cost was never in the probes, and a pyramid aimed at probe count would have bought nothing. The cause: streamed tiles cover ~3.7 km of a ~71 km segment, so outside them `cell/step = 469/19550` floors to 0, every vertex was ALREADY clamped to a 1x1 grid, and the cost was paying per-vertex for an integral that could not have anything to say. **The fix is one early return: below the data's own resolution the mixture has one constituent and the surface is a plane, so the point sample IS the answer** (Law III applied to the integral itself). Measured after: rebuild **173-184 -> 60-69 ms**, and the cost now scales mildly with grid side (60 -> 69) because the only work left is where tiles actually cover. Ladder worst frame **158-186 -> 52-110 ms** against `main`'s 44-53; steady-state render p50 0.4 ms throughout | the pricing rig above, and the ladder rig run on `main` as the control rather than compared against a remembered number. ★ The rig itself was wrong TWICE first and both failures read as "the integral is free": it nudged the camera 0.004 deg to force a rebuild (the segment is over-built by `CAP_MARGIN` and spans ~71 km, so that is well inside its slack), and then it `await`ed a frame between invalidating the cache and timing — which handed control to the PAGE'S OWN render loop, which did the rebuild, leaving the timed call nothing to do. Both reported a flat 0.3 ms at every grid size | **docs/63** — what remains open is the ~15-25 ms the integral still adds over baseline, which is now proportional to the data that supports it. Robin's direction (2026-08-02) supersedes the pyramid idea and is better: **compile Earth once into a binary, use-case-optimised representation** carrying precomputed moments and material mixtures per tile, so the runtime reads rather than derives — *"one high cost compile and done/fast"*. That puts the pyramid in the ASSET instead of the runtime, which is where it belongs when the source data is terabytes | as row 28 |
| 30 | **Wood cannot splinter, because the anisotropy that makes it splinter is catalogued and unread.** `data/materials.json` gives `oak` and `pine` full orthotropic strength: `tensile_strength` 90 MPa ALONG the grain against `tensile_strength_perp` **5.5 MPa** across it, plus `youngs_modulus_perp`, `compressive_strength_perp`, `shear_strength` and `modulus_of_rupture`, under an explicit `assumed_condition` of *"White Oak (Quercus alba) at 12% moisture content; strongly anisotropic"*. **That 16x ratio IS splintering** — wood fails along the grain precisely because it is sixteen times weaker across it. Today every consumer reads the isotropic `fracture_strength`, so oak fails like a weak stone: it craters where it should split. And a PART has no grain DIRECTION at all, so even a reader of those fields would not know which way the plank runs | `grep -rn "youngs_modulus_perp\|tensile_strength_perp\|compressive_strength_perp\|modulus_of_rupture\|shear_strength" crates/engine/src/` returns **nothing**; only 2 of 29 materials carry `_perp` fields at all | **docs/64** — a part must carry its material's ORIENTATION, not just its identity, or anisotropic failure is unrepresentable however good the material data is. Rolled steel, composite layup and bedded sandstone are the same requirement (three materials already describe anisotropic `grain` in their optical notes). Robin's framing (2026-08-02) is what surfaced this: *"the collison engine may one day be used to simulate gunpowder in a cannon, the shot, and the cannonball splintering an enemy ship"* | **none** — the classic docs/48 shape: sourced, catalogued, and wired into nothing |
| 31 | **There is no chemical energy release anywhere in the engine — and the principle it is missing is RAPID OXIDATION, not "a propellant".** Robin (2026-08-02): *"Rapid oxidation will be an important principle in the engine (fires, etc) so this won't be wasted."* That is the right generalisation and it changes what gets built. A campfire, a burning ship, a gunpowder charge, a dust explosion and a rusting hull are **one reaction at different rates and different oxidiser availability** — which is the charter's own shape (one law, every scale), and means the engine needs an OXIDATION model of which a propellant is a special case, not a propellant model that fire would later have to be bolted onto. ★ The distinguishing quantity is **where the oxygen comes from**: a fire is air-limited and therefore geometry- and ventilation-limited; black powder is SELF-oxidising, which is precisely why it works in a sealed bore with no air; an explosion is the same reaction fast enough that the gas cannot escape before it does work. The limiting-reagent rule is the physics, not an approximation of it. Today: no combustion, no deflagration, no stored chemical energy anywhere; every energy source is gravitational, kinetic or thermal, and the only "detonate" in the tree is a comment about an unstable integrator | `grep -rn "combustion\|propellant\|chemical_energy\|deflagrat"` over `crates/engine/src/`: nothing but unrelated comments. The gas-side half IS present and verified — `eos.rs` (Tillotson) and `atmosphere.rs` (specific gas constant from molar mass) | **docs/64** — and the material data is now partly in place: `potassium_nitrate` (oxidiser), `charcoal` and `sulfur` (fuels) are catalogued with sourced properties, and they already split along exactly the fuel/oxidiser axis this needs. What each still lacks is its REACTION data: enthalpy of combustion per kg, stoichiometric oxygen demand, and available oxygen per kg for the oxidiser. Sourced for the mixture and recorded in docs/64: specific energy 2.86 MJ/kg, permanent gas yield ~0.265 m3/kg at STP, gamma ~1.2, Noble-Abel covolume ~1 cm3/g, flame temperature ~1950 K at 1000 psi. Law VII SOP first, always: source before code, and never an impulse dial | **none** |
| 32 | **The engine cannot burn wood — fire exists as thermochemistry and not as a capability.** `oxidation::burn` answers a SINGLE-SHOT question (this charge, this oxygen, this much energy and gas), which is all a cannon needs because a cannon burns once. A fire is a RATE, and four things are missing, **none of which is chemistry**: (a) **rate** — mass consumed per second, set by exposed surface area and oxygen supply, i.e. geometry not kinetics; (b) **ignition** — the threshold at which it becomes self-sustaining; (c) **feedback** — released heat raises temperature raises rate, which is what makes fire *fire* rather than a one-off release; (d) **oxygen coupling** — a closed room depletes and smothers. Then propagation falls out, as neighbours ignite by radiated and conducted heat. ★ Robin (2026-08-03), and this row exists because she asked for it recorded as a SHORTCOMING rather than written up as a rule: *"an absolutely vital capability for the engine"*, *"Rapid oxidation will be an important principle in the engine (fires, etc)"*, and plainly — ***"Eventually Integrity must be able to burn wood."*** That sentence is the acceptance test for this row: not a burn model in the abstract, a wooden thing that catches, spreads and is consumed | **3 of 32 materials carry `reaction` data and none of them is wood** — `potassium_nitrate`, `charcoal`, `sulfur`. Meanwhile `oak` (573 K) and `pine` (553 K) already carry `decomposes_k`, which IS the pyrolysis onset where wood begins giving off flammable volatiles: half the ignition data is catalogued with nothing to burn. No material anywhere carries an autoignition temperature | **docs/64** — and note this does NOT widen the chemistry scope Robin set (*"basic chemistry in scope, complex, maybe someday"*): the burn rate is a SUPPLY problem, and supply is what the limiting reagent already models, just resolved in time rather than once. Needs: `reaction` blocks for the combustible materials, autoignition temperatures sourced (Law VII SOP), and `atmosphere::AirField` finally wired — it is verified and instantiated in NO scene (row 12), so the oxygen a fire would deplete is not being tracked anywhere | `oxidation::burn`: its own tests only |
| 33 | **Interior ballistics is about to become a SECOND answer to "how does matter push on matter".** The gas in a gun's bore is MATTER: it is shaped by the barrel walls, it rebounds off them, it pushes the ball, and some of it escapes past the ball and leaves the muzzle. Robin (2026-08-03): *"the walls of the cannon will shape the trajectory of the gasses, but the gasses will impact them, rebound, impact the ball, etc. Once again one engine, one principle, near-infinite ramifications."* A 1-D Noble-Abel interior-ballistics model — which is where this work was heading, and which is why a covolume was sourced — would be a separate physics for a case the contact law already covers. ★ The honest resolution is docs/44's own duality rather than a choice between them: the 1-D closed form is the **DECLARED** (cheap) layer and the resolved gas particles are the **RESOLVED** layer, with the declared one required to be derived from and convergent to the resolved one. A gun firing off-camera should still cost almost nothing. ★★★ **BUT THE DECLARED LAYER MUST KNOW WHEN IT IS OUTSIDE ITS OWN VALIDITY, and that is what makes it admissible at all.** Robin (2026-08-03): *"a ball slightly too large, a charge too strong, should be able to destroy a cannon as history shows it did."* A 1-D model treats the barrel as a fixed boundary and the shot as free to move, so outside those assumptions it does not become INACCURATE — **it answers a different question**, confidently reporting a muzzle velocity for a gun that should have burst. That is worse than being wrong: it describes an event that did not happen, and nothing downstream can tell. ★★★ **AND THE VERDICT IS ARITHMETIC, NOT A SIMULATION** — Robin's refinement, which dissolves the apparent cost problem: *"if it all checks out we hand the easy 1d to the renderer, if physics predicts catastrophy, we share that with the renderer. No need to actually render the matter particles, so should be a fast calculation."* **Deciding WHETHER a gun bursts does not require simulating the burst.** Peak pressure from the burn, hoop stress `sigma = p*r/t` against the barrel's tensile strength, and driving pressure against the shot-start resistance of the ball's interference fit — all closed-form, on numbers already catalogued. Matter is resolved only for a burst that IS happening, to show HOW it comes apart, never speculatively to find out whether it will. That is Law III exactly: math sizes the interaction, and only the minimal necessary matter becomes particles. ★ **The honest bound on the predicate:** it answers *"does a SOUND gun of these dimensions hold?"* — real guns also burst from casting voids, fatigue and flaws, which arithmetic on nominal strength cannot predict. A flawed gun is a different assembly, and saying so is the difference between a bound and a blind spot | two concrete gaps found by looking rather than assuming: `grep -n "boundary\|wall" crates/engine/src/gpu_sph.rs` returns **nothing** — the SPH path has no wall handling at all, because its only consumer so far is a self-gravitating giant impact with no container; and its EOS table is **Tillotson only** (`SphEos::from_tillotson`), i.e. condensed matter, with no gas closure — `atmosphere.rs` has the ideal-gas one and the two have never met | **docs/64** — what the resolved path needs is a gas EOS (Noble-Abel is the ideal gas law plus a covolume, so it GENERALISES `atmosphere.rs` rather than competing with it: covolume 0 reduces exactly to today's behaviour) and a moving-boundary contact for the walls and the shot base. Both are additions to existing primitives, not new ones | `gpu_sph`: the space band only |
| 34 | **DEFECTS — a future capability, and the architecture already implies it rather than needing a new model for it.** Robin (2026-08-03): *"Over time we might find a way to model defects, and that could be very valuable... we *could* introduce a bubble in a cannon barrel in the model if we chose to and it should have catastrophic results."* ★★ **The insight is that a defect is not a physics, it is a GEOMETRY.** A casting void is absent matter. If a barrel is genuinely an assembly of material with shape — which is what docs/64 makes it — then a bubble is a hole in that shape, the stress concentration around it EMERGES, and the gun fails there. No `defect_model`, no flaw table, no probability of failure: the same contact and fracture law, applied to matter that has a gap in it. That is the engine's whole thesis stated on an object small enough to check. ★ Until then, the burst predicate answers *"does a SOUND gun of these dimensions hold?"* (row 33), which is a stated BOUND rather than a blind spot — real guns also burst from voids, fatigue and inclusions, and arithmetic on nominal strength cannot see any of them. ★★ **And a second failure mode belongs here, because the model should catch it too:** reloading a HOT gun could set the charge off in the loader's hands, which is why crews sponged between shots. That is the row 32 ignition threshold asked of a barrel instead of a fire — barrel temperature against the charge's ignition point — which makes a gun's RATE OF FIRE something that EMERGES from thermal mass and conductivity rather than being declared as rounds per minute | `gunmetal` and `cast_iron` are catalogued as of 2026-08-03 with the numbers that decide both: grey iron tensile 295 MPa against compressive ~930 (a barrel's hoop stress is TENSILE, the direction it is weakest in) and ductility 0.01 against bronze's 0.2 — which is why iron guns shatter where bronze ones bulge. ★ `a_barrels_thermal_mass_is_catalogued_well_enough_to_limit_a_rate_of_fire` pins that both metals carry specific heat AND conductivity, and that an iron barrel stores more heat per unit volume while bronze sheds it faster — two different guns with two different limits, from the data alone. ★ **`cast_iron_is_weak_exactly_where_a_barrel_is_loaded` could only be written HALF way**, and the missing half is row 30: `ductility` and `compressive_strength` are catalogued and `RawMechanical` does not deserialize either, so `Material` cannot carry them and no test can assert through the engine what the file plainly says | **docs/64** §7 — a defect makes the compile-time validation genuinely interesting rather than a formality: the same predicate run on an assembly with a void in it should FAIL, and that is how the feature proves itself | **none** — defects are not modelled and are not claimed to be |

| 35 | **A CANOPY IS DARKER THAN ITS OWN LEAVES, and the engine has no way to say so — because the darkening is an ARRANGEMENT, not a substance.** A single fresh leaf returns ~0.07-0.08 of the visible light that falls on it; a closed forest canopy returns roughly a third of that, and the difference is pure geometry — light that scatters into the canopy is caught by the next leaf down, and crowns shadow each other. `conifer_foliage` and `broadleaf_foliage` are catalogued honestly as LEAF TISSUE with measured spectra, so a forest footprint rendered from them comes out brighter than a real forest, and both entries say so in their own `notes`. **The tempting fix is to darken the leaf, and that would be a Law V fudge of the purest kind**: it would put a property of an assembly inside a substance, so the same leaf held in a hand would be too dark to make a canopy that was right. ★ This is the substance-versus-assembly distinction arriving a FOURTH time (rainforest canopy, black powder, Irish albedo, now this), which is strong evidence it wants to be a first-class idea rather than a lesson relearned. ★★ The same row carries the SEASON: a leaf's colour is its PIGMENT CONTENT, so autumn is a different spectrum (chlorophyll degrading, carotenoids unmasked), never a tint on the summer one — Robin (2026-08-03): *"Flora changes colors/albedo through the seasons... Grass starts green in spring on the prairie, transitions to golden-yellow, leaves burst into a riot of color in cold climates before falling"*, and decisively *"even more honest is having the engine signal TEMPERATURE changes based on the earth's tilt and position relative to the sun"* plus *"the model should be able to calculate amount of DAYLIGHT in each area of biomes based on the tilt/season"* — so the season must EMERGE from tilt and orbit, not be tabulated | the two materials' measured 400-700 nm means are 0.084 and 0.069, against published CANOPY visible albedos of ~0.03-0.05 for forest — a factor of ~2 that no term in the engine currently accounts for. `Material::spectrum` exists and is read by exactly one thing (the albedo derivation), so the representation the seasonal work needs is in place and unpopulated: no material carries a senescent state | **docs/63**'s appearance integral is where the canopy term belongs — a footprint's answer is a MIXTURE over an arrangement, which is exactly what a canopy is, so this is not a new mechanism but the existing one given something real to integrate. The pigment data is already in hand: the NEON dataset pulled for the albedo carries chlorophyll and carotenoid mass per sample, so the causal variable for phenology is measured and sitting in the source | `blackbody::reflectance_srgb`: `materials::albedo_derives_from_the_measured_spectrum` only |

| 36 | **CLOSED 2026-08-03 — the camera was a clamp on an inflated ground, in a scene that owned it; it is now MATTER in the engine.** Robin: *"Camera must exist in the engine, but can be directed by the scene"*, and decisively *"the engine does a lot of calculation based on what can be seen, so it must know everything about the camera all the time"* — a camera the scene owns is one the engine's own resolution decisions trail by a frame. What was there: `alt_m.clamp(min_alt, ..)` stacked on a ground height that was the **MAX over a ±0.2° (~22 km) neighbourhood**, whose own doc claimed the clamp meant *"the camera obeys real physics"* — precisely backwards, since a clamp can only push the eye straight UP the radial and a camera driven at a steep face pops through it. ★★ **And that same 22 km envelope placed the CANNON**, so the gun floated above its own ground whenever a hill stood anywhere within 22 km. That was a real bug and it shipped. ★ **Three answers to "how high is the ground here"** existed in Terra alone: a closure inside `build_segment` (raster blended with streamed tiles — what the mesh actually draws), the 22 km max (raster only, no tiles) for the camera, and that again for the gun. Collapsed to one `ground_elev_m`, which the mesh, the camera and the assembly placement all call | **Measured through the engine** (`web/rig/terra_camera_is_matter.mjs`): with the zoom floor set to **1 mm so the clamp could not be the cause**, an eye asked to sit 200 m BELOW ground comes to rest at **0.35 m — exactly the shell's half-extent — at the Rockies, the Himalaya and the prairie**, and at 0.39 m on the steep site because contact resolves along the NORMAL rather than the radial. That extra 4 cm is the slide a clamp cannot produce, and `the_shell_slides_along_a_slope_instead_of_popping_straight_up` pins it natively. ★ A second finding on the way: `sweep_shell_resolve` caps each sample's correction at 0.5 m — a relaxation RATE, right for per-frame motion and unable to recover from a teleport, and a scene teleports the camera every time it names a coordinate. Measured before the fix: the eye rested **895 m inside** a plateau. The sphere version now settles the constraint after sweeping | **docs/65** — the camera is the first thing paid off the scene/engine debt register, and `granular::sweep_shell_on_sphere` is where the law lives so it is natively testable; `Terra` sits behind `cfg(wasm32)` and cannot be unit-tested at all, which is docs/46 row 14 biting directly. ★★ **STILL OPEN, and it is NOT this row's doing: below about 2 m the surface is not drawn.** An altitude ladder (`web/rig/terra_low_altitude_ladder.mjs`) measures ground filling the frame at 2000/500/100/20/**5** m and gone at **2/1/0.5/0.2** m — and a CONTROL run on the pre-change build gives byte-identical numbers, so the cliff is pre-existing. What this row changed is that the inflated ground used to stop anyone ever getting low enough to see it. This is docs/63 item 4 measured precisely: the cliff is between 5 m and 2 m, not 1 m | `granular::sweep_shell_on_sphere`: `Terra::render` and (until it is deleted) the Ground scene |

| 37 | **CLOSED 2026-08-03 — the Ground scene is deleted, and with it the SECOND surface render path.** Robin, on finding it still shipping: *"'Ground' scene is back? It should be destroyed with fire. It is so wrong, so badly designed and totally out of step with the concept of the engine… a cube of terrain with no planet to support it, anathema to the engine."* She had killed its predecessor (`a9395bf`, docs/50); this was a REBUILD of it (`459404a`, docs/55) that never got killed. Its world declared `lat 45, lon -100` on Earth and then invented its relief from three fbm octaves in a 96³ voxel cube — docs/63 item 1's *"a real place with imaginary ground"*. Gone: `ground_scene.rs` (1,209 lines), `ground.html`, `src/ground.ts`, **`shaders/world.wgsl`** (its only consumer, so docs/63 item 3's two-render-paths becomes one), the nav entry, the vite input and four rigs. ★ The third `#[wasm_bindgen]` scene struct is gone with it, so `SCENE_MODULES` is now `["lib.rs"]` — row 14 is one struct closer to nothing | **The law was never in the scene.** The crater lifecycle — resolve-on-impact, real contact, `recohere_when_settled`, crater persists as geometry — lives in `simulation.rs`/`matter.rs` with **55 native tests**, including `a_settled_impact_aftermath_leaves_ground_not_bare_particles`. So the deletion cost a DEMO, not a verification, and that was checked before anything was removed rather than hoped afterwards. The world file survives as `assets/worlds/ground-patch.json`, the fixture three engine tests read to prove a ground patch still BUILDS from a definition. 523/523 native, `mod app` clean for wasm32, production build green, and the surviving scenes rig-photographed unchanged | ★★ **STILL OWED, and it is a FEATURE rather than a port: Terra cannot crater.** `Terra::render` steps `flight`, receives an arrival, logs its energy and **discards it** — no matter is resolved, no ground changes. Robin's shape for the replacement (2026-08-03): *"Cost should be resolved in a proper meteor shower follow that stops where we can observe terrain change with impact."* The camera half of that now exists (`camera_follow`, row 36). The missing half is a MUTABLE surface: `Simulation`'s lifecycle runs on a voxel `World`, while Terra's surface is a mesh derived from rasters, so a crater has nowhere to persist. `gpu_sph::promote_ground_cap` — surface → matter — is built, tested, and has **zero consumers**, which is the docs/48 pattern exactly | `simulation::Simulation`: native tests and `bin/run-definition` only — **no scene** |

| 38 | **The season now EMERGES from tilt and orbit — and what remains owed is the PIGMENT model and the thermal one.** Robin (2026-08-03): *"Even more honest is having the engine signal temperature changes based on the earth's tilt and position relative to the sun"*, and *"the model should be able to calculate amount of daylight in each area of biomes based on the tilt/season."* Built: `solar` — declination split out of the sun the terminator is already drawn with, day length `cos H0 = -tan(phi)tan(dec)`, the daily-insolation integral, and `Moment`/`RespondsToTime`, the general signal an actor answers (the test actor is NOCTURNAL, precisely so the mechanism cannot quietly become a leaf-colour hook). ★★ **THREE THINGS ARE STILL DECLARED.** (a) **The thermal term does not exist.** Insolation is TOP-OF-ATMOSPHERE — no cloud field — so a surface temperature from it alone runs warm; and temperature LAGS photoperiod because ground and ocean have heat capacity, which is why the warmest weeks trail the solstice and why leaves turn before the cold arrives. Without it there is no year-to-year variation and no frost event. (b) **Senescence interpolates between two measured leaves rather than deriving a spectrum from PIGMENTS.** The resolved version carries the pigment complement and sums what it absorbs — and Robin's note is why the complement must be a variable, not a constant: *"There is also red chlorophyll, which is useful for catching longer wavelengths."* Chlorophyll *a*/*b* give out around 660-680 nm while **chlorophyll d and f absorb well past it — f beyond ~720 nm** — which is how some cyanobacteria live on light a leaf cannot use. An engine that hard-codes "chlorophyll absorbs red" cannot represent them, or an ocean's photosynthesisers. (c) **Nothing consumes the senescence phase yet** — the materials carry no senescent spectrum in the shipped catalogue, so Ireland does not yet turn | **Measured from the data, not assumed.** The NEON fresh-leaf set carries chlorophyll AND carotenoid mass per sample, so broadleaf endmembers were binned by PIGMENT rather than by date: **senescent (chl 1.96 mg/g, n=12, 10 taxa) 450/550/660 = 0.044/0.141/0.048** against **mature (chl 11.10 mg/g, n=11, 9 taxa) = 0.034/0.089/0.031** — a 5.7x chlorophyll difference, and as chlorophyll falls the leaf brightens in BOTH green and red, which is senescence. Those n are thin and the low bin is low-chlorophyll rather than fully turned; both are stated. ★ The model reports its OWN validity (`senescence_phase` returns the phase and the annual day-length range), because at the equator the range is ~5 minutes and normalising by it turns noise into a full autumn — the docs/46 row 33 requirement applied again. ★★ And a TYPED FIXTURE caught itself here: the polar solstice insolation was asserted as "~52.6 MJ/m2" from recall and came back 46.8; the physics settles it with no reference at all, since at the pole the Sun circles at constant elevation so the day's total is exactly `S*sin(dec)*86400` = 46.7. The formula was right and the memory was wrong | **docs/65** — this is the first real instance of the assembly answering a time signal, and the cadence belongs to the ACTOR (a leaf answers in days, a bear in minutes, a rock never) so one clock serves many rates | `solar::senescence_fraction`: **no consumer yet** — the classic docs/48 shape, recorded on the day it was written |

| 39 | **EARTH HAS TWO AXIAL TILTS.** Robin, asking the right question while the seasons were being wired (2026-08-04): *"this is all to determine different seasons on earth, which invert between north/south hemisphere based on tilt of Earth's axis; I'm not sure we have a tilted axis yet?"* Half yes. **`orbit.rs` carries the real one** — `obliquity = 23.439 - 4.0e-7*n` degrees — and every seasonal quantity derives from it, so the subsolar latitude swings ±23.4°, the Arctic circle lands at 90-δ, and the hemispheres reverse. Terra is therefore CORRECT: it works in the Earth-fixed frame where the tilt appears AS the declination. **But `lib.rs` declares the space band's Earth with a spin axis PERPENDICULAR to its orbit** — the comment says so outright: *"Modern Earth: the measured sidereal day, spin axis ⊥ the orbital (x-y) plane"*, i.e. obliquity ZERO. ★ So the same body has an axial tilt of 23.439° for the purpose of lighting and 0° for the purpose of dynamics, which is Law II exactly: one question, two answers, and they cannot both be Earth | `grep -n 23.439` finds one site (`orbit.rs:776`); `lib.rs:1496-1499` builds `spin_l` from `DVec3::new(0.0, 0.0, 1.0)` with no obliquity applied. The seasonal tests in `solar` all pass BECAUSE they read the orbit-side value — they would pass identically with the body upright, which is what makes the divergence invisible from the test suite alone | **docs/25/27** — the consequences are real but confined to the space band: an Earth viewed from orbit shows no axial tilt, its terminator does not lean through the year, and any precession or tidal torque computed from `spin_l` is being computed for the wrong body. The fix is to build `spin_l` by rotating the orbital normal through the SAME obliquity `orbit` already owns, so there is one tilt and it is written once | `orbit::solar_declination_ra` (lighting, correct) and `OrbitDemo`'s body init (dynamics, upright) — the two answers |

| 40 | **REAL LAND COVER IS IN (2026-08-04), and the bake script had the layer name wrong.** Row 28 has blamed the invented latitude-band cover for the orange ground, the conifer Ireland and the nothing-to-integrate appearance integral. The cause turns out to be one string: `bake.py` asked GIBS for `MODIS_Terra_Land_Cover_Type_Yearly`, which does not exist. **GIBS answers a bad layer with HTTP 200 and an XML error body**, so the fetch looked successful, only the image decode failed, and the `except` swallowed it as *"GIBS land cover unavailable"* before falling back to six latitude bands. A silent fallback that logs a plausible reason is worse than a crash. Verified against GetCapabilities 2026-08-04: the real name is `MODIS_Combined_L3_IGBP_Land_Cover_Type_Annual`, and a request returns a genuine **18-colour IGBP raster (17 classes + water)** | Spot checks against the fetched raster, and they settle three open questions at once: **Galway returns the SAME class as the Serengeti** — grassland/savanna, i.e. Ireland is PASTURE, not the boreal conifer the invented bands claimed (which is why it could not turn, docs/46 row 38); Maine is MIXED forest; the Amazon evergreen broadleaf; the Sahara barren; Siberia woody savanna. Reference raster committed at `tools/bake-earth/reference/modis-igbp-landcover-2020-1024.png` so the next session starts from evidence rather than a download | **docs/63** — what remains is the IGBP colour → class → MATERIAL MIXTURE map, and it must be a mixture rather than one material per class, because IGBP classes are things like *woody savanna* and *cropland/natural mosaic* that are definitionally mixtures. That is exactly what `terra::appearance` was built to integrate and has never had. ★★ And Robin's next requirement lands here too (2026-08-04): *"low-cost models for grasses and trees (low poly for now but reasonably faithful)… stored as assemblies themselves and added as members of Earth so they can be rendered as called for"* — at altitude a plant is its albedo, at eye level it is geometry. That is the same object at two resolutions (docs/44), and it is what would finally make row 35's canopy darkening EMERGE from mutual shadowing instead of being a declared number | ★★ **DONE the same day**: `landcover.png` is now 18 MEASURED IGBP classes at 100% exact palette decode, and `earth.json` maps each to a MATERIAL MIXTURE whose fractions come from the IGBP class definitions (forests >60% tree, woody savanna 30-60%, savanna 10-30%, open shrubland 10-60%, barren <10%), taken at the midpoint of each stated range. Galway is grassland, Iowa cropland, the Amazon evergreen broadleaf, the Sahara barren. `Surface::biome_mixtures` replaced `biome_materials` and `aggregate_albedo_turned` gives a mixture its season, so a mixed forest turns partly and a savanna's grass and trees turn independently. ★★ **RIG-VERIFIED on the 5060 Ti, and the hemispheres run opposite in the PICTURE.** Measured R:G of the ground (hue, not luminance — at 45N the noon sun falls from ~68 to ~21 degrees between June and December, so brightness is dominated by the sun's own elevation and cannot isolate a season): **Maine 0.6886 -> 0.6908 -> 0.6939 -> 0.7005** and **Ireland 0.6855 -> 0.6879 -> 0.6904 -> 0.6984** across Jun/Sep/Oct/Dec, both reddening monotonically; **the Serengeti runs BACKWARDS — 0.6875 -> 0.6901 -> 0.6867 -> 0.6855** — because at 2.3S its December is its growing season. Nothing was told to do that; it falls out of the axial tilt through the declination. ★ Honest about the size: the effect is measurable and monotone but VISUALLY SUBTLE, because broadleaf is only 0.45 of that class and row 35's canopy-versus-leaf gap flattens it further. It turns; it is not yet dramatic. (The GPU needed an NVIDIA kernel-module reload first — 580.173.02 running against 580.178.04 userspace after an in-place upgrade.) |

**A distinct violation class (item 12).** Rows 1–11 are *one question, two answers*. Row 12 is different:
the optics are honest and the world beneath them is empty. It inverts *physics drives the render* — not
by faking the picture, but by leaving the picture's subject unbuilt. It is invisible to every existing
check, because the atmosphere tests pass, the sky tests pass, and nothing asks whether anything
**instantiates** what they describe.

### The wiring pattern — look for this before building anything

Three independent cases now share one shape: **the law is built and proven, then wired into one place or
none.**

| verified physics | wired into |
|---|---|
| docs/39 JIT particalization (conserving to <1e-12) | planetary scale only; terrain **never** |
| `granular::terrain_contact_resolve` (energy-monotone, hardware-verified) | GPU grains only; bodies **never**, until PR #15 |
| `atmosphere::AirField` (hydrostatic, drag, entry heating) | **nothing** |

The instinct on finding a gap has been "we need to build X". The evidence says the likelier truth is "X
exists, verified, and nothing calls it." **Grep for the primitive before writing one** — the
second-cheapest move after reading the docs.

Items 5, 6, 9 and 11 are the ones a "same in every scene" reading makes urgent: they are places where the
*same matter* behaves differently depending on which scene or which rung of the resolution ladder it is
on.

## 4. The scale test

A world is a world is a world. So for any primitive, ask: **does it run at both ends?**

The de-resolution cycle is the worked example. docs/39 proves
`field → particalize → simulate → quiesce → bake_back → field` at planetary scale, conserving mass,
momentum and COM to **<1e-12**. It is listed there as item #4 that terrain is "the separate low-energy
instance of the same primitive, after planetary scale." Measurement (§3 item 6) confirms terrain has only
the first half. The primitive is proven; it has simply never been instantiated at the second scale.

That is the shape of most remaining work: **not new physics — the same law, run at the other end.**

## 5. What this forbids

- Tuning one side to agree with a known-broken other side. docs/45 records the live case: terrain repose
  implemented correctly would disagree with grain repose, because the grain side under-predicts for its
  own flagged reason (spherical grains roll). Making them agree by adjusting the correct half would look
  consistent and be wrong.
- Constants that read as physics. `steep_drop = 3` (a 72° slope — RETIRED, docs/45), `MATERIALIZE_CAP = 14.0`
  (also since DELETED with the terrain scene, docs/50), `MAX_EJECT = 0.045`, a hardcoded damping ζ. Each was
  defensible when written and each silently became a
  lie when the world around it changed. A number is either derived from a material datum / conservation
  law, or explicitly flagged as a budget with an IOU (docs/24).
- A second implementation of a shared law "just for this scene". If the scene genuinely needs different
  physics, that is a docs/NN, not a copy.

## 6. How to use this doc

Before adding physics: check §2 for an existing primitive to extend. Before accepting a fork: apply §1.
After finding a new violation: **add a row to §3 with evidence**, so the next session inherits it instead
of rediscovering it.

## 7. The merge rule: wired, or IOU'd. Built and verified is not done

The ledger's consumers column exists because the repo's most corrosive pattern (rows 12 and 15, the
wiring table in §3) is invisible to every existing check: a law's tests pass whether or not anything
instantiates it. So the bar a physics change must clear to merge is stated here, once:

> **New physics lands either wired into at least one scene, or carrying a flagged IOU that names the
> milestone that wires it.** "Built and verified" does not count as done. It counts as inventory.

Corollaries:

- A ledger row whose consumers cell reads **none** stays OPEN no matter how green its tests are.
  Closing it means a production call site, not a passing suite.
- Consumer claims are grep results, never intentions. "Will be wired by X" without a flagged IOU
  naming X is the same claim as "none", written optimistically.
- Deleting a law's only consumer reopens its row (row 15 is the worked example: the wiring pattern at
  its sharpest, physics wired into one place and that place deleted).

---

**Related:** CLAUDE.md (the charter in brief) · docs/23, docs/24, docs/28 (the one-law charter docs) ·
docs/30 (temporal deltas; the physical-bound rule) · docs/32 §4 (the structural fork map) · docs/33
(realignment) · docs/38 (composite contact law; legitimate integrator specialization) · docs/39 (the JIT
particalization primitive, proven at planetary scale) · docs/44 (resolution by necessity) · docs/45
(terrain slope stability).
