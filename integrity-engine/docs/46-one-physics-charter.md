# docs/46 — One physics: the charter, and the conformance ledger

> **The promise.** REAL physics — one law, at every scale, in every scene. A world is a world is a world.
> This is the product, not an aesthetic preference about code structure. An engine that answers the same
> physical question two different ways in two different scenes has broken its promise, however good each
> answer looks on its own.
>
> **The span that promise commits to, stated as the acceptance test:** simulate a **star's photosphere
> and generate a solar flare**, or **a raindrop on a flower petal**, with the *same engine* — without
> reinventing or re-coding anything. It should fall out of the scale a scene is viewed at and the action
> occurring in it. **And the player must be able to SEE both.** That is ~15 orders of magnitude with no
> scene-specific branch on either the physics side or the render side.
>
> Judge every change against it: **if it would need re-coding to work at another scale, it is the wrong
> change** — however good the result looks in the scene it was written for. A new per-scene code path is
> a failure even when it ships something beautiful.

The mechanism for this is `docs/13-scale-relative-simulation.md` (*"both simulation and rendering cost
should scale with what is observable from the current viewpoint … detail emerges continuously: star
field → planet disk → landscape → terrain → the rock → its grains"*), with `docs/44` (resolution
EXTENT — how much to resolve) and `docs/47` §1 (resolution GRANULARITY — how fine). This charter states
the promise; those state how it is met, and the split is deliberate — restating their content here would
itself be two answers to one question. **Neither axis is built yet**, and that is the single largest gap
between this promise and the code: the resolution-EXTENT admission test is built and tested but unwired
(`resolution::admission_depth` — `ResolutionField::update` still hardcodes `necessity_depth = 1.0`; the old
`MATERIALIZE_CAP` clip was DELETED with the terrain scene, not wired), and granularity is design-only — the GPU `Particle` struct
has no size field and `part_half` is a per-dispatch uniform, so two grain scales cannot coexist in one
scene. A tyre contact patch needs centimetre grains in the same world a meteor uses metre grains; until
that lands, the span above is a promise the code cannot yet keep.

This doc exists so the ledger below is **read, not rediscovered**. Every session so far has re-derived
some part of it from scratch. `docs/32 §4` maps the forks structurally; this doc states the *rule* that
makes a fork acceptable or not, and keeps the running list of open violations with their evidence.

---

## 1. The rule: specialization vs violation

Not every difference is a breach. The criterion is sharp:

- **Legitimate specialization** — the *physics itself* differs, so the numerical treatment differs.
  Stiff granular contacts need an implicit/semi-implicit integrator to stay stable; self-gravitating SPH
  needs a symplectic leapfrog to conserve energy over orbits (docs/38 §"what unified GPU path does NOT
  mean"). Forcing one integrator on both is unstable or ruinously slow. Boundary conditions likewise:
  the terrain band models Earth as a rigid heightfield because you are standing on a 96 m patch, not
  simulating a planet's interior.
- **DECLARED (a resolution IOU)** — the resolved physics is real and correct but **unaffordable at the
  timescale humans and models need in order to play with it and learn from it**. So the outcome is
  computed from the material's own data and rendered, instead of being allowed to emerge. This is
  legitimate, and it is where most of the interesting engineering will live for years.
- **Violation (a fudge)** — the *same physical question* gets two different answers. Two implementations
  of one law that can drift. A law implemented completely in one place and partially in another. A
  quantity conserved in one scene and not in the other. Or a number chosen because the result looked right.

### Two things "no fudge" does NOT forbid (Robin, 2026-07-24)

The no-fudge Law is aimed at the ENGINE's physics, and even there it is not an absolute — it is bounded by
the compute we have. Two legitimate things are easy to mistake for fudge and must not be:

1. **A scene declaring initial conditions is not a fudge — it is what scenes are for.** Creating matter at a
   specified position and velocity (a meteor swarm arriving from outside Earth, a moon on a declared orbit)
   is honest ABRIDGEMENT: the scene skips the uninteresting prior history and starts the matter at the point
   that matters, in its true state. That is the ICs-not-consequences Law applied to scenes — a declared
   velocity vector is honest. (The 250 m meteor drop WAS a fudge, but not for being declared: it
   misrepresented the entry — wrong speed, wrong heating, wrong path. A real approach velocity from space is
   the honest version of the same declaration.)
2. **The declared-model layer is REQUIRED, not grudgingly tolerated.** To render math-driven results at the
   compute available, the engine stands in unaffordable resolution with models derived from the real physics
   and convergent to it. That is not a compromise of the Law — it is how the Law is met at all within a
   finite budget (Law VIII's honest bound). A declared model becomes a fudge only when it stops deriving
   from, or converging to, the real math — which is exactly the test below.

### Declared is not fudged — the distinction that keeps it honest

A drive-shaft *should* shear when torque exceeds what its material can carry, and in a perfect world it
would do so because the bonds actually failed. That is far too expensive today. So we compute whether it
shears — real torque against the material's real shear strength — and render that outcome.

**That is not a second answer to the same question.** It is the same physics evaluated analytically
because the resolved evaluation sits below the resolution we can afford. The test that separates it from
a fudge:

> **Can you name the resolved computation this replaces, and would the declared answer converge to it as
> resolution rises?**

If yes, it is a declared model carrying an IOU. If you cannot name it, it is a fudge wearing a physics
coat.

The codebase already does this well, and those are the pattern to copy: `plough_loft` declares an
excavation shock finer than a grain as a **conserved momentum transfer**, says so outright, and notes
that co-motion is the physical maximum so there is no coefficient to tune; `Furrow::ejection` marks its
velocity SCALE honest and its distribution SHAPE an explicit resolution IOU *"to be DELETED once particle
count is high enough for the flow to emerge on its own."*

Compare the fudges: `MAX_EJECT = 0.045`, `steep_drop = 3` (retired 2026-07-19, docs/45), a hardcoded
damping ζ. None names a resolved computation; none would converge to anything.

### The horizon — write IOUs so a descendant can delete them

This layer is a **compute-era limitation, not a design preference**. The purpose is to render real
physics on the timescale and compute humans and LLM models need to enjoy and learn from it. If compute
becomes cheap enough — quantum or otherwise — descendants of this engine should be able to delete the
calculate-and-render step and simply let the drive-shaft shear.

That only works if IOUs are written *for deletion*: each declared model must state the resolved
computation it stands in for, so retiring it is a substitution rather than an archaeology project.
**A declared model with no stated resolved counterpart is a fudge, because nobody can ever retire it.**

The test any change must pass: **does this reduce the number of places where one physical question has
two answers?** A declared model does not add an answer — it defers one, in writing. If a change adds a
genuine second answer, it needs a physics reason, written down, or it is debt.

## 2. What is already unified (do not fork these)

From docs/32 §4, and it is a real achievement worth protecting:

`granular::Contact` + `contact_accel` · the SPH kernel `sph_w`/`sph_dw` (one kernel for `AirField` AND
aggregate vapor) · `Furrow` + `ExcavSurface` + `ejection` + `ejecta_energy_scale` (ONE excavation
primitive — but see the caveat below: the LAW is one, yet neither production path CALLS it today) ·
`plough_loft` · `deposit_resting_grain` (one de-resolution primitive; CPU-only since the GPU debris
readback was deleted with the terrain scene) · `Body` as the universal particle · `Material`/`LayeredBody`
as the universal matter description · `zeta_for_restitution` (bond damping and contact damping from one
derivation).

> **Caveat verified 2026-07-23:** "unified" here means the LAW exists once and must not be re-derived — it
> does NOT mean every scene routes through it today. The `Furrow` excavation primitive is real and tested,
> but the terrain meteor still uses `matter::impact`'s inline voxel loop and the giant impact uses the SPH
> path; neither calls `Furrow`. So "do not fork" is guidance for when these are wired, not a claim that they
> already share one call. Routing `matter::impact` through `Furrow` is a tracked refactor, not done.

## 3. The conformance ledger — open violations

Each entry: what has two answers, the evidence, and where it is tracked. Structural forks 1–4 are
docs/32 §4's; the rest were found since and are recorded here so they are not rediscovered.

The **consumers** column (added 2026-07-22) lists the scenes/modules that actually INSTANTIATE the
physics in question, verified by grep on that date, never assumed. Where it reads **none**, that is
the point: a verified law with zero consumers stays OPEN no matter how green its tests are, because
its tests pass whether or not anything calls it (see §7).

| # | one question, two answers | evidence | tracked | consumers |
|---|---|---|---|---|
| 1 | ~~**Two container universes** — `Aggregate` (`Vec<Body>`, f64) vs voxel `World` + GPU f32~~ **CLOSED 2026-07-23 for the fork this row named**: the orbital scene's impact debris. The CPU `Aggregate` debris path is deleted: `OrbitDemo` no longer holds a `moon_debris` and never calls `impact::build_impact_debris*` (zero grep hits in `lib.rs`); a dropped moon resolves through the same GPU SPH machine as birth. Tests: `gpu_sph::a_dropped_moon_impact_leaves_most_matter_bound_on_the_sph_path` (the ported physics pin: bound fraction + emergent shock heating, at the assembly + CPU-KDK seam), `a_moon_drop_builds_and_strikes_through_the_same_assembly`, `a_live_drop_assembles_n_materials_on_its_own_geometry`. What `Aggregate` still hosts is NOT this row's fork: the ground scene's cohesive body (`simulation.rs:109`, `Aggregate::cohesive`; row 4 / docs/61 IOU 2 territory) and the atmosphere/gravity module tests | docs/32 §4.1 | docs/33 | closed (orbital debris); remaining `Aggregate` production consumer: `simulation::Simulation`'s cohesive body only |
| 2 | **Four integrators over one law** | docs/32 §4.2 | docs/38 (partly legitimate — see §1) | GPU KDK (`gpu_sph::encode_kdk`): `OrbitDemo`, birth AND the live moon-drop (`lib.rs:2246`). CPU KDK block (`Aggregate::step_block`): **none** since 2026-07-23: its consumer was the moon-drop debris, retired with row 1; `Aggregate::step` (per-substep) remains consumed by the ground scene's cohesive body (`simulation.rs:354`). CPU Euler settle (`MatterSim::step`): `Simulation` (Ground). GPU trapezoidal-implicit (`particle_step.wgsl`): `GpuProbe` only. `AirField` SPH relaxation: **none**. CPU KDK + adaptive dt (`hydrostatic::step`/`relax_step`): **none in a scene** (production builds `HydroBody` seeds and relaxes on the GPU via `cs_relax`); it is now the native suite's headless twin of the SPH machine (`a_dropped_moon_impact_leaves_most_matter_bound_on_the_sph_path`) |
| 3 | ~~**Rigid-boundary fork** — in an impact Earth is simultaneously materialized grains AND a rigid boundary~~ **CLOSED 2026-07-23 in the scene that had it**: `OrbitDemo`'s moon-drop arm (the one that set `boundary` + `boundary_hole` + `gravity_source` around a live debris cloud) is deleted with the CPU debris path; every resolved impact in the orbital scene now runs Earth as particles through the SPH machine, no rigid boundary. Tests: same set as row 1. The boundary machinery survives only inside `impact.rs`'s builders (`impact.rs:561-577`), whose remaining callers are that module's own physics tests: measurement fixtures, not a scene | docs/32 §4.3 | docs/33 | closed (no scene instantiates the fork; `impact::build_impact_debris_between`/`_scaled` are test-consumed only, verified by grep 2026-07-23) |
| 4 | **Two rigid-body reps** — `body::Sphere` vs the cohesive-`Aggregate` probe | docs/32 §4.4 | docs/38 4c′ | **none**: `body::Sphere` is built only in tests and `Simulation::step` passes an empty body slice (`simulation.rs:164`); the cohesive-`Aggregate` probe lost its scene with the terrain deletion (docs/50), leaving `PROBE_LATTICE`/`PROBE_STIFFNESS_CAP` (`lib.rs:291`) consumerless |
| 5 | ~~**Slope stability is half a law.**~~ **CLOSED 2026-07-19.** Terrain and grains now read the same `friction_coefficient` through one law, `granular::face_stable`; `steep_drop` is retired | was: `granular.rs:73` vs `matter.rs:538` (`h_crit = c/ρg` alone), non-convergent at 106→622 grains/pass. Now: fixpoint on the second pass, settled slope asserted against the DB μ, pristine terrain a no-op (470→0 grains) | **docs/45 §7** | closed |
| 6 | **The de-resolution ladder stops one rung short.** grain→voxel works; voxel→field is built but never triggered. **NARROWED 2026-07-19:** the mechanism is now SAFE — one authoritative `World::ground_top_voxel` answers "where is the ground" for voxels and field alike, and the GPU heightfield, the CPU bilinear surface and the rendered cap all read it. What remains is the TRIGGER, plus `patch_resolved` being one bool for the whole 96 m patch while demotion is per-column | was: three different answers to one question — GPU heightfield read raw voxels (demoted column ⇒ grains fall through), the cap read raw `terrain_height` (demoted crater renders as untouched ground), probe read `bulk_height`. Measured: 98% of grains return (3,605 → 78) | docs/47 §5, docs/39 item #4 | **none** for the open half: `World::demote_column_to_field`/`demote_patch_to_field` have zero production callers (the only call sites are `mesher.rs` tests, e.g. `mesher.rs:1003`); nothing in `Simulation` or the Ground scene ever triggers voxel→field. **NARROWED 2026-07-23 (docs/61):** the missing TRIGGER now exists for the rung above — demote-on-quiescence is `recohere::recohere_settled` (quiescent speed `sqrt(2gΔ)`, sustained one cell dynamical time `sqrt(2Δ/g)`), consumed in production by `Simulation::step` → `recohere_when_settled` (the Ground scene and `bin/run-definition`), folding a settled particle field back to voxels mass- and material-conserving. The voxel→field rung below it is still untriggered, and the SPH-remnant wiring is docs/61's flagged IOU |
| 7 | **Promotion is gated visually in one doc, physically in another.** docs/30: the trigger must be "a physical error bound … never a visual one". docs/39: gate on "camera-visible ∧ interacting" | the two docs, directly | **docs/44** | the gate that actually runs is docs/39's visual one: `Simulation::step` materialises on `(c - camera).length() < view_r` (`simulation.rs:156`) feeding `ResolutionField::update`, consumed by the Ground scene and `bin/run-definition`; docs/30's physical error bound has no consumer |
| 8 | **The honest footprint is computed, then discarded.** `crater_radius` from `V = E/σ` is derived, then clipped by `MATERIALIZE_CAP = 14.0`, and `resolve_patch` resolves the whole 96 m patch regardless | `lib.rs:858`, `lib.rs:992` (self-flagged) | **docs/44** | the evidence sites are gone: `MATERIALIZE_CAP` and `resolve_patch` were deleted with the terrain scene (docs/50), zero grep hits today. The question stands: `damage::crater_radius` is consumed by `interaction.rs:102` swept-collision effects (`OrbitDemo`), and the ground path clips its materialised crater at `MatterSim::impact`'s flagged `MAX_R = 24` LOD guard (`matter.rs:190`), consumed by `Simulation` (Ground) |
| 9 | **Matter leaks at the seam — and it is a DOMAIN property, now measurable per definition (docs/54).** Grains that leave the patch are culled by `matter::step`; the loss is invisible from a particle count, since de-resolution looks identical. `run-definition` reports created/returned/in-flight/lost: a 96 m patch with gentle events loses **0.0%** (260 created, 260 returned), while a 48 m patch with an energetic impact loses **28.8%** (6,328 created, 1,822 lost). The earlier ~2% figure was the big patch only. Sizing the domain to the event is the open question | measured 2026-07-21 from `definitions/ejecta-ground.json` and `definitions/small-island.json` | **docs/54** — was: ~2% of debris never returns to the field (deposition refused inside a dynamic body; the water branch is a self-flagged static-sea placeholder) | measured: 78 of 3,605 grains stranded per event, monotonic | this doc | `simulation::Simulation` (the Ground scene; `bin/run-definition` is the consumer that measures the loss) |
| 10 | **Vehicle/probe never contacts debris.** The probe is a CPU `Aggregate`; grains live in a GPU buffer. Coupling is a bounding-sphere exclusion, not contact | `lib.rs` settle path; `matter::couple_body` exists but is called only from tests | docs/38 4c′ | **none**: `matter::couple_body`'s only callers are its own tests (`matter.rs:2210`), and `Simulation::step` passes `&[]` for bodies (`simulation.rs:164`) |
| 11 | **An asteroid-era constant still runs in an Earth-g scene.** `MAX_EJECT = 0.045` m/s, capped "below the world's ~7 cm/s escape velocity" — a 0.1 mm ballistic hop at 9.81 m/s² | `matter.rs:41` | this doc | **none**: `MAX_EJECT` (now `matter.rs:63`) lives in `MatterSim::dig`, which no scene calls; its only callers are the `isotropy.rs` regression suite. The constant is unconsumed AND still wrong, in that order |
| 12 | **The render asserts a physical state the simulation does not have.** Every Earth scene draws an honest Rayleigh sky over a **vacuum**: `atmosphere::AirField` — pressure-layered, with verified hydrostatic balance, momentum-conserving drag and hypersonic entry heating — is instantiated in **no scene** | `grep AirField crates/engine/src/lib.rs` → nothing; 11 atmosphere tests pass regardless | **docs/48** | **none**: `AirField::new` appears only in `atmosphere.rs`'s own tests; every reference outside that file is a doc comment |

| 13 | **Incandescence has two curves.** "What colour does matter at temperature T glow?" is answered by `emission::incandescence` (docs/20, natively tested, returns premultiplied `[r,g,b]`) AND by a second copy inside `mod app` for the space band (returns `[r,g,b,intensity]`). They agree only on the 800 K glow threshold | both read directly: `emission.rs:13` ramps intensity `(T−800)/2200` capped at 4 with blue from 2600 K; the space-band copy ramps `x=(T−800)/2400` saturating at 3200 K with blue from `x>0.55`. At 2000 K one gives `[0.545, 0.297, 0]`, the other `[1.0, 0.5, 0.0]×0.6` | this doc — found 2026-07-20 during the docs/33 render-scaffolding lift; NOT unified in that PR because collapsing them changes what the space band looks like, which needs its own rig verification | `emission::incandescence`: the Ground scene only (`ground_scene.rs:764`, `:782`). The `mod app` copy (`lib.rs:3836`): `OrbitDemo` only (`lib.rs:3246`, `:3323`, `:3421`). `Terra` consumes neither |

| 14 | **Scene KINDS are code; scene INSTANCES are already data — except one.** Robin's requirement: a scene should be object/assembly definitions, coordinates and materials and "should NOT require special mods of the engine itself". **NARROWED 2026-07-21 by measurement, after the row was first written too broadly.** `orbit.html` and `twomoons.html` are the SAME script and the SAME `OrbitDemo` differing only by `data-world=…/world.json`; `terra.html` likewise. Those are data. What remains code: (a) the scene KIND — adding a genuinely new kind means a new `#[wasm_bindgen]` struct with its own pipelines and render loop, and deleting terrain cost **1,516 lines of `lib.rs`** plus a public-API symbol and a build entry; (b) **`birth.html` alone still has NO world file** — `data-scene="birth"` selects a hardcoded path whose initial conditions (body radii, materials, proto-Earth spin, impact geometry 1.15·v_esc / b≈R_e / d₀=1.6·contact) are Rust constants | pages measured directly; birth's ICs at `gpu_sph::build_impact_bodies` + `assemble_from_relaxed` | **docs/51** — (b) first, since it is the last scene on a code path | the scene kinds: `OrbitDemo` and `Terra` (`lib.rs`) plus `Ground` (`ground_scene.rs`), each a `#[wasm_bindgen]` struct. The birth ICs: `OrbitDemo` via `gpu_sph::build_impact_bodies_from`/`assemble_from_relaxed_with`; note `web/birth.html:77` now carries `data-world="/worlds/birth/world.json"` and the file exists, so (b) is narrower than first written |

| 15 | ~~**Deleting terrain orphaned three verified systems.**~~ **CLOSED 2026-07-21 (docs/53)** — `crate::simulation::Simulation` builds and steps them from a `"ground"` world DEFINITION (production code: 8 `MatterSim` refs, 4 `ResolutionField`, 1 `world::generate`), so the consumer is a file and no scene's deletion can orphan them again. Verified end to end from `definitions/ejecta-ground.json`: an effect propagates analytically off-camera, materialises 257 grains on entering view, and every one de-resolves back to the world (644,190 → 644,450 voxels, **+260** = 257 grains + 3 impact particles — matter conserved). Was: It was the ONLY production consumer of `matter::MatterSim` (the shared matter path), `resolution::ResolutionField` (docs/49 camera-driven resolution, wired the day before), and the voxel `world::World`+`mesher`; the granular GPU pipeline is now reachable only from `GpuProbe`, a compute-only diagnostic with no canvas | measured 2026-07-21 after the deletion: **0** references to `MatterSim`/`ResolutionField` anywhere in `lib.rs`; all 6 `world::generate` calls are inside `#[cfg(test)]`. Every test still passes, which is why it is easy to miss | **docs/51** — a requirement on the NEXT scene: re-consume them or delete them. This is docs/48's wiring pattern at its sharpest: physics wired into one place, and that place deleted | closed |

| 16 | **Scenes build their own worlds instead of using ONE Earth.** Robin: *"Terra should occur naturally from definitions of material, biomes, etc… a fully materialized object, reusable between scenes. Then this scene would simply be using that planet/solar system."* **NARROWED 2026-07-23 (docs/59 order-of-work 1): the IDENTITY and PARAMETERS are one value now.** All three shipped scenes read `assets/bodies/earth.json` and nothing else: the space band's `EARTH_*`/`MOON_*` constants are retired for cached reads of the definitions (`lib.rs` `EARTH_PARAMS`/`MOON_PARAMS`), Terra inherits its radius from the body the world names (`declared_planet_radius`; `worlds/earth/world.json` carries no `radius_m`/`mass_kg`), and the ground patch declares WHERE it sits (`lat`/`lon` in `worlds/ground/world.json`) and derives g, air pressure AND its material strata from the body at that site (`LayeredBody::surface_strata`; the world file's private strata list is gone). Enforced: `one_earth_tests::the_three_scenes_read_one_earth` asserts digit-identity across the three paths, and the `laws` scans (`DEFINITION_OWNED`, planet-block override check) keep copies from creeping back | measured 2026-07-21 (and still true): the ground patch is 96 m across = **0.00024% of Earth's circumference** - walk ~48 m and you reach the edge of the world. What remains of this row: the patch is still a PROCEDURAL cube that merely *names* its site - its relief does not sample the body's elevation rasters at the declared lat/lon (the `terrain_height` TODO(ETOPO)), no camera path connects Terra's globe to the patch (docs/59 items 2–5: descent camera, materialization, hand-down, re-coherence), and the strata band thicknesses are a declared log2 vertical LOD, flagged in `surface_strata` | **docs/23** (the north star: one Earth, the Moon hits it, zoom to the ball) + **docs/13** (scale-relative) + **docs/43** (worlds as data) + **docs/59** (the zoom - this row's remainder is its order of work) | one Earth's parameters: all three scenes (`lib.rs` shared params + `declared_planet_radius`, `simulation.rs::from_definition` via `GroundDef.planet`+`lat`/`lon`). Still scene-local: the patch's procedural relief (`world::terrain_height_with`) and its placement-blind coordinates |
| 17 | ~~**Four answers to "what is the disk?"**~~ **CLOSED 2026-07-23** — `gpu_sph::DiskView` is the single definition (COM, the 85%-mass remnant, the bound+perigee filter, the `find_clumps` call); `disk_stats_json`, `moonlet_bodies`, `largest_moonlet_orbit` and `disk_moonlets` are thin callers. Was: each carried its own copy of the preamble, and they had already DRIFTED — only `disk_stats_json` tested boundedness BEFORE geometry, so the render spheres, the escape check and the geologic hand-off all counted ESCAPING matter as disk | the fix existed in ONE copy with a comment explaining itself (*"Asking `perigee` for boundedness was safe only while it refused to describe hyperbolic orbits"*) and was never propagated to the other three. Net -34 lines. `escaping_matter_is_not_counted_as_disk` now pins it for every consumer at once; mutation-checked against the pre-fix behaviour | **docs/44** — this is the operator de-resolution consumes, so the drift would have been inherited by the clumping pass that collapses a united disk into a body | closed |
| 18 | ~~**The render overrides the physics for cap impacts — Earth cannot be seen to crater.**~~ **CLOSED 2026-07-23.** The globe now carries a measured crater: `accretion::crater_bowl` sizes the bowl's depth AND radius from the excavated volume (the target material lifted above its pristine radius), and `globe.wgsl::crater_sink` sinks the surface into a paraboloid `depth·(1−(θ/θr)²)`. So the depression shows ON the intact bulk sphere — the sphere is still drawn coherent, but it is no longer the whole story. Diagnosing it also found the real defect: the bowl WAS reaching the shader, but its radius came from a `0.72` dial while only depth was measured, giving d/r≈0.06 (a flat saucer); both now come from the one volume plus the sourced simple-crater shape d≈0.4r (Melosh). Rig-measured after the fix: d/r=0.40. Was: `lib.rs` computed `accretion::representation(...)` then discarded it whenever `sph_cap` was set, drawing a flawless sphere with coherence pinned to 1.0 | the numbers disagreed with the picture: birth (no cap) sheds real Earth material (rig 2026-07-23, disk 0.28 M☾ / 56% Earth) while the cap path rendered the target untouched. Now `a_crater_bowl_keeps_its_shape_at_every_size` pins the geometry (it caught a factor-of-2 volume error a screenshot could not) | **Law VI** — the render now REPORTS the excavation instead of overriding it. NOTE (still open, smaller): a fully unambiguous daylit face-on crater shot needs a camera-aim hook the scene does not expose; and the bulk sphere's own coherence is still 1.0, correct only because the cap depression sits on top | closed |

| 19 | ~~**Universal constants retyped across the engine.**~~ **CLOSED 2026-07-24.** `orbit::G` and `blackbody::SIGMA` are the only definitions; every module delegates, and `laws::SINGLE_SOURCE` now counts both so a sixth copy fails the build. Was: **G written out in five modules** (`gravity`, `planet`, `damage`, `accretion`, `orbit`) plus **two shaders**, and Stefan–Boltzmann in three — one of them the truncated `5.670e-8`, so `aggregate`'s cooling moonlet and `atmosphere`'s ablating meteor radiated by *literally different constants* | found by counting, not by reading: nothing had drifted far enough to fail a test. The WGSL copies were invisible to a Rust-only search and only surfaced once σ was registered in the existing guard | this doc — the shader copies are legitimate (WGSL cannot read a Rust constant) and are PINNED by `pinned_constant_tests::the_shaders_gravitational_constant_matches_the_engines`, the `EARTH_RADIUS_M` treatment: a second copy is honest only when something fails if the two disagree | closed |
| 20 | **Entry heating uses a continuum law across a path that is not all continuum.** `atmosphere::stagnation_heat_flux` is Sutton–Graves, correct in dense air and correct for the ground scene it was built for. The orbit-to-ground swarm (docs/59) flies the *whole* path, and above ~100 km a metre-class body is in transitional and then free-molecular flow, where the classical meteor law `Λ·½ρv³` is the right one. The engine applies one of the two everywhere instead of choosing by regime | the regime is COMPUTABLE, not a judgement call: the mean free path follows from catalogued data — `λ = (μ/ρ)·√(πM/2R_uT)` over air's `dynamic_viscosity` and `molar_mass`, which reproduces the measured 68 nm at sea level — so Knudsen `λ/2r` could select or blend the laws. Magnitudes at 100 km, 1 m body, 20 km/s: Sutton–Graves 1.0e6 W/m² vs free-molecular ~2e5 (Λ=0.1), so the error is bounded and not catastrophic at metre scale — but it grows for small fragments, which is exactly the population that decides what burns up | **docs/59** — the flagship's visible trail forms in this regime. Note `dynamic_viscosity` is in `materials.json` but not yet parsed by `materials.rs` | not surveyed — row added after the 2026-07-22 consumers survey |
| 21 | ~~**A body ablating in air is treated as isothermal.**~~ **CLOSED 2026-07-24.** The heat front now advances at the material's own thermal diffusivity `α = k/(ρc)` (`atmosphere::soak_depth`, `heated_mass`), so only the mass the heat has REACHED takes part in the temperature change — and bulk heating survives as the limit the model reduces to once the skin reaches the radius. `thermal_conductivity` is now sourced in `data/materials.json` for 28 of 29 materials (`hh_plasma` left unknown); `docs/04` had reserved the field and nothing had ever used it. MEASURED before → after, 8 iron fragments at 15 km/s from 200 km: a 30 cm parent went 0% ablated → **4.3%**, 1 m 0% → 0.75%, 3 m 0% → 0.03%, and every size up to 3 m now reaches iron's 3134 K boiling point instead of a few hundred K. The live swarm's ablated mass went 397 kg → **15,871 kg**. Two things fell out that nothing declares: the heated layer SETTLES at ~1.4 cm for iron (ablation strips it as fast as conduction deepens it — the classical `δ = α/2v` boundary layer of an ablating surface), and basalt ablates MORE than iron at every size because its 21×-lower diffusivity keeps the heat at the surface, which is why stony meteorites carry thin fusion crusts. A 10 m body still does not reach boiling, and that is Sutton–Graves being right rather than a limit: flux goes as `√(ρ/R_n)`, so a blunter body is heated less — the reason re-entry capsules are blunt. Was: heating the whole mass at bulk heat capacity made thermal response scale with VOLUME, so a 0.5 m iron body flew a perfectly correct 20 km/s entry and barely warmed | the giveaway was that the model got the OBSERVABLE right (small meteoroids burn up, iron meteorites land) for the wrong mechanism — it was only valid below the thermal skin depth, √(αt) ≈ 1.5 cm for iron over ten seconds | **STILL OPEN, smaller:** only the skin's temperature is tracked, so the interior is unmodelled and the heat that conducts past the front is not accounted anywhere; the resolved computation is a real temperature PROFILE through the body, and the moving-boundary treatment of a receding surface is its refinement | closed for the defect named; the remaining interior-profile half not surveyed |
| 22 | **Ablated mass reaches the atmosphere and stops.** `atmosphere::Trail` closed the conservation hole (shed vapour used to cease to exist) but its end state is `merged_kg`: mass booked into the air, permanently | in reality that vapour condenses to micrometeoritic dust and settles out — ~40,000 t/yr on Earth — so the atmosphere is a staging post, not a sink. Named rather than lost, which is the difference between an IOU and a leak | **docs/59** — the resolved counterpart is `AirField` parcels that condense and fall under the same gravity as everything else. Robin (2026-07-24): booking it is the right first cut, *"understand it will eventually fall to the earth"* | not surveyed — row added after the 2026-07-22 consumers survey |

| 23 | ~~**A second answer to "how steep can this material stand".**~~ **CLOSED 2026-07-24, same day it was opened — by Robin, mid-build.** `surface_detail` needed a roughness ceiling and I wrote a slope rule for it, while `granular::repose_allowance`/`face_stable` (docs/45) had carried Mohr–Coulomb all along *specifically* so "ground and grain answer the slope question with ONE law". Robin: *"shouldn't this material work already be hard baked into the engine? How/why were you able to change it and get it wrong with impugnity?"* — and it was wrong in both ways `granular`'s own doc warns about: friction alone (the table refutes it: dry sand grips HARDER than granite, μ 0.67 vs 0.60, yet only granite stands vertical — sand's cohesion is 0, granite's 28 MPa), then friction+cohesion added into ONE slope, when they are an OR over two different measurements (friction on the slope, cohesion on the bank). FIX: `relief_amplitude_m` holds no slope physics — it asks `granular` and only converts a permitted drop into an amplitude; `repose_allowance_on(mu, r, quantum)` takes the field's quantum as a parameter (`SLOPE_QUANTUM_M` belongs to the voxel field, not to the physics) so delegation is possible at all; `critical_bank_height` gives `h_crit` a home (it was inline in `matter.rs` while its partner term lived in `granular` — half a law in each place) | **the reason it was possible is the ledger entry that matters**: `laws.rs` hardens CONSTANTS (`SINGLE_SOURCE`) and keeps scenes out of collision (`COLLISION_PRIMITIVES`), but a DERIVED LAW had no guard, so a reasonable author who did not find the primitive could contradict it with every test still green | **now guarded by a CONSISTENCY test, not a grep**: `generated_relief_is_stable_by_the_engines_own_slope_law` hands every octave of generated relief back to `granular::face_stable` as the face it implies — 7 materials, 6 orders of wavelength — and requires it to be held up. A name check would not have caught this. **The pattern generalises: any module that derives a quantity another module owns should be pinned by agreement, not by convention.** | closed |
| 24 | ~~**Impact energy had two formulas.**~~ **CLOSED 2026-07-25** during the integration of Sean Reid's `upstream-5-ground-ball`, and neither side went looking for it. `interaction::detect_swept` priced every body-body collision at **½·μ·|Δv|²** with the reduced mass — its own comment: *"the energy actually available at the contact frame, not either body's kinetic energy in an arbitrary frame"* — while the terrain arrival path computed **½·m·v²** inline. They were never rivals: as the struck mass grows without bound, μ → m and Δv → v, so **bare terrain is the IMMOVABLE LIMIT of the general collision, not a case beside it**. One `flight::delivered(body, target_vel, target_mass_kg)` now answers, `None` meaning matter too massive to move. Found only because his ball is a target that CAN recoil, which forced the question "what is the energy when the thing struck is free to move" to be asked of the ground path too | the two expressions read directly: `interaction.rs:196` vs the old `0.5 * b.mass_kg * speed * speed` in `flight::Flight::step_bodies` | this doc — pinned by `flight::an_immovable_target_is_the_reduced_mass_limit`, which asserts the finite-mass case CONVERGES to the immovable one over 10³…10¹⁵ × the striker's mass rather than merely agreeing at one point | `flight::Flight` (Ground + Terra entry) and `interaction::detect_swept` (`OrbitDemo`) now share the one formula |


| 25 | **The re-coherence crossing has no energy sink, and re-cohered rubble is instantly bedrock-competent (docs/61) - three debts.** (a) *Binned heat dropped:* a grain's carried temperature has nowhere to go when it becomes a voxel, because the voxel store holds no thermal state at all. (b) *Settle kinetic energy zeroed:* sub-threshold motion (bounded per grain by `m g Δ`, the quiescence criterion's own quantum) is dissipation, and dissipation becomes heat, not nothing. Both are now MEASURED at the crossing instead of silently zeroed: `Recohered::binned_kinetic_j` / `binned_heat_j` book energy-in minus remainder-out per column (heat only where `Material::specific_heat` is sourced; an unknown c stays unknown), `Simulation` accumulates them (`recohered_kinetic_j()` / `recohered_heat_j()`), and `bin/run-definition` prints them whenever the rung ran. The deferred computation both name: a voxel-side thermal field (temperature or heat density per voxel or per column) that `matter::deposit_grain` deposits into; the closing test is today's conservation test flipped from measuring the loss to asserting the deposit lands in the field, e.g. `the_crossing_deposits_its_energy_into_the_voxel_thermal_field` (today's measuring form: `the_crossing_measures_the_binned_kinetic_energy_and_carried_heat`). `Aggregate::drain_settled` and the per-grain settle path (`deposit_resting_grain` from `matter.rs:1155`) share (a) and (b) and are not yet measured. (c) *Instant consolidation, design only:* ejecta re-coheres at reference density and full material strength - `deposit_grain` writes ONE u16 material index (`matter.rs:137`) and a voxel is nothing but that index (`world.rs:60`), so fresh rubble reads back the intact material's `density` and `fracture_strength`, when real fresh ejecta is porous (bulking 1.2 to 1.5x its source volume) and unconsolidated. Deferred: a consolidation state (porosity and strength fraction) on re-cohered matter, relaxing toward intact over a physical timescale (overburden pressure and time, from the material's own data); the test that would close it: `fresh_recohered_ground_is_porous_and_weak_and_consolidates_toward_intact` (fresh re-cohered gravel occupies more columns per kg and detaches under a tool an intact voxel shrugs off, then converges to intact as consolidation runs) | the voxel store's whole per-cell state is `voxels: Vec<u16>` (`world.rs:60`); `grep temp_k crates/engine/src/world.rs` returns nothing; the measurement is `binned_kinetic_j` in `recohere.rs`, asserted conserving by `the_crossing_measures_the_binned_kinetic_energy_and_carried_heat` | **docs/61** | the measured half: `Simulation::recohere_when_settled` accumulates, `bin/run-definition` reports the `recohered` line. The thermal sink and the consolidation state: **none** exist to consume |

| 26 | **The zoom's upward rung is built, verified, and ~~deliberately unwired~~ WIRED (narrowed 2026-07-23).** The camera-driven trigger landed: `crate::site` derives the view-necessity threshold (one coarse particle's matter share against the docs/49 angular budget, `d* = s/theta`, measured from the live field when one exists), and the space band materializes the Ground Zero world's declared ball and strata patch through this rung on the descending crossing, folds through the docs/61 gauge on the ascending one, with the ledger and every refusal on the HUD. (a) RESOLVED 2026-07-23: a relief stall is a true fixed point of the shifting (measured: the site's relax oscillates around 4.9e-2 to the 5000-iteration cap), caused by the release criterion's own two smoothing scales; the rung now judges a PROVEN stall against the coarse field's measured disagreement across exactly those scales (refine::scale_mismatch) and releases with the residual and the derived bound both stated (site: 4.8e-2 under 9.1e-2; relief lattice: 5.1e-2 under 1.2e-1; the strict 5e-3 remains the criterion for every converging run, and an unguarded truncation still refuses); (b) an ISOLATED sub-resolution body (the ball) has no coarse environment to relax against (measured divergent), so it splits exactly, unrelaxed, stated; (c) NARROWED 2026-07-23: the mid-event hand-down landed as BOUNDARY STATE - the guard band re-samples the coarse field once per coarse step (`site::resample_guards`; the guards ARE the coarse field at the boundary, so the event's energy arrives through state, not a formula), `site::EventWindow` books the window within its stated 1e-6 bound, and the pi-scaling gate has its end-to-end consumer (`refine::measure_crater_rim` at the field's own quantum, prediction frozen from the measured contact state, verdict or stated sub-quantum refusal on the HUD). NARROWED FURTHER 2026-07-23: the released site ENTERS DYNAMICS (site::SiteDynamics) - the fine parcels are cohesive matter through the ground scene's own builder (simulation::CohesiveBody::from_parcels, one law for the site's ball and the ground scene's), stepped under bonds, gravity and the one terrain-contact law, driven during an event by the guard band's booked step-to-step delta through the one door (Aggregate::deposit_impact), fate by damage::classify per parcel, verdict word shared, fold via the docs/61 gauge; the release gate still keeps an UNRELEASED patch out of dynamics. Still open in (c): the N-material upload reaching the guards (the sampled state inherits the coarse field's EOS collapse, the named IOU); site-local excavation of the floor law (the parcels carry the crater's truth, the declared surface stays the floor); and the site clock's real-second rate against the scaled celestial clock (a stated compute bound; verdicts are deposit-driven and land at the coarse step). `crate::refine` is docs/59 item 3, the conserved hand-down: one-shot icosahedral splitting with a mandatory retained center child (stencil constants re-derived by least squares for the engine's OWN kernel, separation 0.3051 h and child smoothing 0.7915 h in the support-radius convention; the literature pair 0.2 h / 0.9 h measures 4.9% L2 density error on this kernel vs 0.7% for the derived pair), relax-then-release against the coarse field's interpolated density with a frozen clock (release is the stated 5e-3 relative density-error bound, never an iteration count), one-rung-per-interface discipline and contamination as refusal, the five-quantity conservation ledger, and the Holsapple-Housen pi-scaling gate (v2.2.1 rows) with its live crater consumer | native tests: exact conservation of mass, momentum, angular momentum, kinetic and internal energy on the split; density blip measured 7.5e-2 released to 5.0e-3 on uniform basalt and 9.5e-2 to 5.0e-3 across a basalt/iron interface; contamination and interface-ratio refusals with stated reasons; the gate reproduces a hand-computed Meteor Crater example; the window book is pinned by a quiet-hot-quiet pulse across the shipped site's boundary (guards carry exactly the field's state, the un-stepped fine children bitwise untouched, mass single-owner) and the rim measurement by synthetic bowls (one-quantum dimple refuses, intact shell refuses); the relief stall's derived-bound release is pinned with the bound recomputed independently in the test; the DYNAMICS are pinned by the quiescent fall-rest-settle-fold arc and by the demo beat at the seam (a moon-magnitude boundary pulse delivered through the door SHATTERS the site's ball by classify alone - measured fate 0 solid / 42 fractured / 171 molten / 60 vapor, mass conserved, mid-event fold refused). Green whether or not anything calls it, which is exactly why this row exists (§7) | **docs/59** (order-of-work items 2 and 3) | `crate::site` (the trigger, the spec, the hand-down, the fold) and `OrbitDemo::update_site` (the per-frame crossing check, the HUD line, the billboard draw); armed by any world carrying a `ground` block (`worlds/ground-zero/world.json`). (a) is closed; the row stays open for the (c) remainder |
| 27 | **Sub-raster relief is scaled by a number measured four orders of magnitude away from what it scales, so the ground is generated nearly flat — and this is why a descent reveals no detail.** `Terra::build_segment` forms `slope_fraction = tier_slope / mu` (it was `build_cap` until the surface collapsed into one segment, docs/63 — the formula moved unchanged), where `tier_slope` is the elevation gradient over a baseline of TWO RASTER TEXELS (39 km on the shipped 2048x1024 Earth) and `mu` is a grain-scale friction coefficient. A 39 km baseline is a REGIONAL TILT; it cannot be steep. Measured IN-SUITE on the shipped raster through the engine's own sampler, 16,384 land points: median 0.00202, p90 0.01149, largest anywhere 0.0667 — and **Everest itself reads 0.0328**, because averaging over 39 km flattens it. So `slope_fraction` is ~0.003 typically and ~0.10 at its most extreme, never the 1.0 the law is written in terms of. Compounding it, `relief_amplitude_m` is `min(drop/2, lambda/4)` and for cohesive rock the `lambda/4` NO-OVERHANG cap binds at every wavelength below ~2 km — a HEIGHTFIELD property, not the rock's — which makes amplitude proportional to wavelength, i.e. Hurst exponent H = 1, the smoothest self-affine surface and the one whose slope is IDENTICAL at every scale. Real topography has H ~ 0.5-0.7 and gets relatively rougher as you close in. The two together are the answer to *"why does the ground flatten on descent"*: below ~20 km altitude the frame fits inside one raster texel, so measured elevation contributes only a smooth ramp and every visible bump has to come from a generator that is both scaled to ~0.003 and structurally incapable of getting rougher as you approach | measured: 1-vs-4 ground tiers is max 4 pixel values different at 100 m altitude (so it is NOT the tier ladder, which was the standing hypothesis; ★ the ladder itself was DELETED 2026-08-02 with the globe/cap collapse, docs/63 — geometry was never the right carrier for sub-cell detail, which is the explanation that measurement lacked at the time); at `slope_fraction = 1.0` the same generator gives RMS slope ~1.0 and 106 m of relief in a 109 m frame, so the amplitude law is not the limiter either; at the measured worst (fraction 0.111) it gives 11.8 m, at median land 0.36 m. ★ The first version of this measurement was taken by a SEPARATE SCRIPT that reimplemented `Raster::coords` with a half-texel offset the engine does not have: invisible across 16k points (median and p90 agreed to three digits) but it read Everest as 0.008 against the engine's 0.033. A reimplementation of the thing under test is a second answer to the same question, and moving the measurement in-suite (a `png` DEV-dependency; the browser decodes rasters at runtime, so nothing reaches the wasm bundle) is what caught it | `surface_detail::generated_relief_has_the_same_slope_at_every_scale_below_two_km` and `surface_detail::a_regional_gradient_cannot_reach_a_material_scale_slope` pin both halves; docs/59 Stage B | **OPEN — diagnosed and now PARAMETERISED, no fix applied.** The fix is NOT a multiplier (that is a dial, Law V): a wrong EXPONENT cannot be corrected by a coefficient. **MEASURED 2026-08-01, `earths_topography_is_self_affine_and_its_exponent_is_not_one`:** the structure function of the shipped raster over land (golden-angle points, great-circle pairs, both ends land, 5,846 land points and ~40k pairs per lag) gives RMS dz of 137.9 m at 19.5 km rising to 905.2 m at 625 km, a log-log slope of **H = 0.483** over 39-625 km. That is the accepted value: Turcotte's spherical-harmonic spectrum gives S(k) ~ k^-2 below 10,000 km and beta = 2H+1, so beta ~ 2 implies H ~ 0.5, and a recent Earth/Venus roughness study reports H ~ 0.55 at beta = -2.1. So the generator's implied H = 1 is off by a factor of **(19550/lambda)^(1-H)** -- **50x too smooth at 10 m wavelength**. The measurement also supplies the ANCHOR a fix needs: RMS dz at one texel = 137.9 m, a real roughness rather than a regional tilt. **Honest caveat:** the local slope drifts from ~0.77 at the shortest pair to ~0.41 at the longest, so a single H over 39-625 km is a fit, not a constant, and extrapolating it four more decades to metre scale is a DECLARED model whose resolved counterpart is finer data. FINER DATA EXISTS and is free -- Copernicus GLO-30 (30 m, TanDEM-X, COG on AWS Open Data), ETOPO 2022 (15 arc-sec ~450 m topo+bathy, NOAA public domain, the drop-in upgrade for this combined raster), AWS Terrain Tiles (global z/x/y **terrarium PNG**, no auth, RGB-packed metres -- the same encoding this engine already reads). But 30 m global is ~1.4e12 samples: unshippable whole, so it is a TILED/streamed source, and even at 30 m the generator is still what covers 30 m down to cm. **CORRECTED 2026-08-01, Robin caught it:** this row first said data was NOT an alternative, which had the emphasis backwards. Finer data is the PRIMARY fix — it shortens the generator's span from 5.29 decades to 2.48 (GLO-30) or 1.68 (z=15 tiles), cutting the H=1 error at 0.1 m from 544x to 19x or 7.4x, AND it repairs the slope_fraction category error BY CONSTRUCTION, because that bug is really that two texels is 39 km; at 30 m texels the baseline is 60 m, where real ground does approach what the material can hold. What data cannot do is reach 10 cm (2.5 decades short at GLO-30) or ship as a file (1.8 TB; a web-budget single file buys only ~5-10 km/texel). So: stream tiles, and keep the exponent for the residual. Robin's call |
| 28 | **The appearance integral is built, correct and has NOTHING TO INTEGRATE — the surface's own description is 19.5 km per texel and INVENTED.** `terra::appearance` integrates a footprint's material mixture and slope variance (docs/63), and at Terra's 469 m mesh cell over the Colorado Rockies it changes the picture by almost nothing. Not because the model is wrong — because the data cannot answer it. The land-cover raster is 2048x1024 (**19.5 km/texel**), so a mesh cell sits INSIDE one texel and the mixture has exactly one constituent; the elevation raster at the same resolution is a bilinear ramp across a cell, i.e. a PLANE, which by construction has no slope variance. Worse, `web/public/bodies/earth/SOURCES.txt` says the land cover is *"DERIVED climate approximation from lat+elevation+coast, NOT a measured land-cover dataset"* — six latitude bands in `tools/bake-earth/bake.py::bake_landcover`. **And the biome map commits a category error: land-cover class 3 ("forest") maps to material `pine`, which is pine TIMBER (albedo [0.68, 0.48, 0.21], a brown), so the Amazon and the Congo are drawn the colour of cut lumber.** ★★ **And the fix is not a better wood — it is the right PART of the plant.** Robin (2026-08-03): *"Pine Timber is always the wrong choice for flora though, we should look for 'pine needles' or 'pine leaves', same with other biomes."* What a surface shows from above is the CANOPY — needles, leaves, litter — and timber is the trunk, which is barely visible from any distance and is a different material with a different albedo, roughness and thermal behaviour. So every vegetated biome wants a FOLIAGE material rather than a structural one: pine needles, broadleaf, grass blade, scrub, litter. **This is the substance-versus-assembly distinction arriving a third time** — leaf tissue and hardwood are both substances, a canopy is an assembly of them, and the map was reaching for the assembly's least visible constituent. MEASURED at Galway 2026-08-03: frame RGB 134/119/64 IS pine timber's [0.68,0.48,0.21], and `gunmetal` is [0.55,0.42,0.24], so a bronze gun standing on "forest" is nearly the same colour as the ground and disappears into it. Robin, on seeing it: *"Currently our information is rather sparse and appears to be extrapolated based on latitude, so the Sahara is green"* and *"the Amazon Rainforest in Brazil/etc looks pretty sparse"* ★★ **HALF CLOSED 2026-08-03, and the half that closed is the MATERIALS.** `conifer_foliage` and `broadleaf_foliage` are catalogued as substances, and **their albedo is not chosen** — it is a mean of 449 measured fresh-leaf spectra (NEON domains + the UCSB HyspIRI California campaign, 98 taxa) convolved against the engine's own CIE 1931 observer under a 5772 K sun, via `blackbody::reflectance_srgb`, with `albedo_derives_from_the_measured_spectrum` re-deriving it every run so the stored triple can only ever be a CACHE. The catalogue's 400-700 nm means land at 0.084 (conifer) and 0.069 (broadleaf) against published measured values of 0.08-0.09 — an independent confirmation nobody tuned toward. The land-cover class that conflated the Amazon with Siberia was also SPLIT (3 broadleaf / 6 needleleaf), because one forest class pointing at one material would have shipped the second foliage substance wired to nothing. **Measured A/B through the engine, same rig, same sun, same camera: Galway 148.3/130.9/68.1 red-brown -> 82.1/111.6/64.4 GREEN; Amazon 148.3/132.3/67.9 -> 78.2/108.9/55.8; Siberia 146.9/124.7/66.9 -> 74.7/101.8/62.3; and the Sahara BIT-IDENTICAL either side**, which is the negative control saying only the forest classes moved. Before the split all three forests were literally the same colour. What is still open is the DATA: the cover is still latitude bands, so Ireland is still called forest when it is pasture, and ESA WorldCover is still the fix. ★ Also found and fixed on the way: `tools/bake-earth/bake.py` wrote to `web/public/worlds/earth/`, which stopped holding Earth's rasters when Earth became a shared body — a rebake would have written a full set of rasters into a directory nothing reads and reported success. | `the_shipped_rasters_cannot_express_a_mixture_at_mesh_cell_scale` measures it THROUGH the engine at the scale ladder's own site (39N, 106W, 469 m cell): mixture length **1**, sigma **< 1e-3 rad**, while the mean slope is real (>1e-4) and the mesh already carries it. The test FAILS when better land cover lands, which is its purpose. Rig A/B at 100 m altitude, main vs branch: both frames a flat green fill | **docs/63** — and it re-orders that doc's plan: the data expansion is not "later", it is PREREQUISITE for the integral to show anything. Robin's direction (2026-08-02): *"we need to vastly expand our materials to include rainforest canopy leaves, rainforest woods, different sands ... seed all that from NASA data (use geological data for regions, biome data to extrapolate flora)"*, noting *"Work on Earth is not wasted; it can all be catalogued in a model that will be used again and again."* Verified open, no-auth sources: **ESA WorldCover** (10 m, 11 classes, public S3, CC-BY), **SoilGrids** (250 m sand/silt/clay fractions, ODbL), **GLiM** (global lithology). NOT HLS — it is Earthdata-Login-gated surface reflectance in MGRS COGs, i.e. imagery rather than materials. ★ A design distinction this raises: leaf TISSUE and tropical hardwood are substances and belong in `data/materials.json`; a rainforest CANOPY is not a substance but an ASSEMBLY (mostly air, leaves at some leaf-area-index), so giving it a bulk density would declare a number that traces to nothing (Law V). **MODIS MCD43A3 is a measured global albedo product**, which would upgrade docs/63's convergence invariant from internal consistency to agreement with reality | `terra::appearance::integrate_on_sphere`: `Terra::build_segment` only. The space band's segment does NOT run it (flagged in place) — two scenes drawing one Earth differently is what docs/63 exists to end, but the corridor's descent is not frame-reproducible, so it cannot be A/B'd until docs/63 item 1c's fixed-pose rig exists |
| 29 | **The appearance integral's cost was per-VERTEX, not per-probe — and the fix was to notice there was nothing to integrate.** ~~The structural fix is a mip pyramid of moments over the elevation~~ **REFUTED BY MEASUREMENT 2026-08-02, before it was built.** `web/rig/terra_appearance_price.mjs` prices the stage the gpu-perf way (move ONE thing, re-time — never delete the stage and re-time the whole build): pinning the sample grid from 1x1 to 8x8, a **20x increase in probes per vertex, moved the rebuild not at all — 173.3 ms to 176.1 ms, FLAT.** So the cost was never in the probes, and a pyramid aimed at probe count would have bought nothing. The cause: streamed tiles cover ~3.7 km of a ~71 km segment, so outside them `cell/step = 469/19550` floors to 0, every vertex was ALREADY clamped to a 1x1 grid, and the cost was paying per-vertex for an integral that could not have anything to say. **The fix is one early return: below the data's own resolution the mixture has one constituent and the surface is a plane, so the point sample IS the answer** (Law III applied to the integral itself). Measured after: rebuild **173-184 -> 60-69 ms**, and the cost now scales mildly with grid side (60 -> 69) because the only work left is where tiles actually cover. Ladder worst frame **158-186 -> 52-110 ms** against `main`'s 44-53; steady-state render p50 0.4 ms throughout | the pricing rig above, and the ladder rig run on `main` as the control rather than compared against a remembered number. ★ The rig itself was wrong TWICE first and both failures read as "the integral is free": it nudged the camera 0.004 deg to force a rebuild (the segment is over-built by `CAP_MARGIN` and spans ~71 km, so that is well inside its slack), and then it `await`ed a frame between invalidating the cache and timing — which handed control to the PAGE'S OWN render loop, which did the rebuild, leaving the timed call nothing to do. Both reported a flat 0.3 ms at every grid size | **docs/63** — what remains open is the ~15-25 ms the integral still adds over baseline, which is now proportional to the data that supports it. Robin's direction (2026-08-02) supersedes the pyramid idea and is better: **compile Earth once into a binary, use-case-optimised representation** carrying precomputed moments and material mixtures per tile, so the runtime reads rather than derives — *"one high cost compile and done/fast"*. That puts the pyramid in the ASSET instead of the runtime, which is where it belongs when the source data is terabytes | as row 28 |
| 30 | **Wood cannot splinter, because the anisotropy that makes it splinter is catalogued and unread.** `data/materials.json` gives `oak` and `pine` full orthotropic strength: `tensile_strength` 90 MPa ALONG the grain against `tensile_strength_perp` **5.5 MPa** across it, plus `youngs_modulus_perp`, `compressive_strength_perp`, `shear_strength` and `modulus_of_rupture`, under an explicit `assumed_condition` of *"White Oak (Quercus alba) at 12% moisture content; strongly anisotropic"*. **That 16x ratio IS splintering** — wood fails along the grain precisely because it is sixteen times weaker across it. Today every consumer reads the isotropic `fracture_strength`, so oak fails like a weak stone: it craters where it should split. And a PART has no grain DIRECTION at all, so even a reader of those fields would not know which way the plank runs | `grep -rn "youngs_modulus_perp\|tensile_strength_perp\|compressive_strength_perp\|modulus_of_rupture\|shear_strength" crates/engine/src/` returns **nothing**; only 2 of 29 materials carry `_perp` fields at all | **docs/64** — a part must carry its material's ORIENTATION, not just its identity, or anisotropic failure is unrepresentable however good the material data is. Rolled steel, composite layup and bedded sandstone are the same requirement (three materials already describe anisotropic `grain` in their optical notes). Robin's framing (2026-08-02) is what surfaced this: *"the collison engine may one day be used to simulate gunpowder in a cannon, the shot, and the cannonball splintering an enemy ship"* | **none** — the classic docs/48 shape: sourced, catalogued, and wired into nothing |
| 31 | **There is no chemical energy release anywhere in the engine — and the principle it is missing is RAPID OXIDATION, not "a propellant".** Robin (2026-08-02): *"Rapid oxidation will be an important principle in the engine (fires, etc) so this won't be wasted."* That is the right generalisation and it changes what gets built. A campfire, a burning ship, a gunpowder charge, a dust explosion and a rusting hull are **one reaction at different rates and different oxidiser availability** — which is the charter's own shape (one law, every scale), and means the engine needs an OXIDATION model of which a propellant is a special case, not a propellant model that fire would later have to be bolted onto. ★ The distinguishing quantity is **where the oxygen comes from**: a fire is air-limited and therefore geometry- and ventilation-limited; black powder is SELF-oxidising, which is precisely why it works in a sealed bore with no air; an explosion is the same reaction fast enough that the gas cannot escape before it does work. The limiting-reagent rule is the physics, not an approximation of it. Today: no combustion, no deflagration, no stored chemical energy anywhere; every energy source is gravitational, kinetic or thermal, and the only "detonate" in the tree is a comment about an unstable integrator | `grep -rn "combustion\|propellant\|chemical_energy\|deflagrat"` over `crates/engine/src/`: nothing but unrelated comments. The gas-side half IS present and verified — `eos.rs` (Tillotson) and `atmosphere.rs` (specific gas constant from molar mass) | **docs/64** — and the material data is now partly in place: `potassium_nitrate` (oxidiser), `charcoal` and `sulfur` (fuels) are catalogued with sourced properties, and they already split along exactly the fuel/oxidiser axis this needs. What each still lacks is its REACTION data: enthalpy of combustion per kg, stoichiometric oxygen demand, and available oxygen per kg for the oxidiser. Sourced for the mixture and recorded in docs/64: specific energy 2.86 MJ/kg, permanent gas yield ~0.265 m3/kg at STP, gamma ~1.2, Noble-Abel covolume ~1 cm3/g, flame temperature ~1950 K at 1000 psi. Law VII SOP first, always: source before code, and never an impulse dial | **none** |
| 32 | **The engine cannot burn wood — fire exists as thermochemistry and not as a capability.** `oxidation::burn` answers a SINGLE-SHOT question (this charge, this oxygen, this much energy and gas), which is all a cannon needs because a cannon burns once. A fire is a RATE, and four things are missing, **none of which is chemistry**: (a) **rate** — mass consumed per second, set by exposed surface area and oxygen supply, i.e. geometry not kinetics; (b) **ignition** — the threshold at which it becomes self-sustaining; (c) **feedback** — released heat raises temperature raises rate, which is what makes fire *fire* rather than a one-off release; (d) **oxygen coupling** — a closed room depletes and smothers. Then propagation falls out, as neighbours ignite by radiated and conducted heat. ★ Robin (2026-08-03), and this row exists because she asked for it recorded as a SHORTCOMING rather than written up as a rule: *"an absolutely vital capability for the engine"*, *"Rapid oxidation will be an important principle in the engine (fires, etc)"*, and plainly — ***"Eventually Integrity must be able to burn wood."*** That sentence is the acceptance test for this row: not a burn model in the abstract, a wooden thing that catches, spreads and is consumed | **3 of 32 materials carry `reaction` data and none of them is wood** — `potassium_nitrate`, `charcoal`, `sulfur`. Meanwhile `oak` (573 K) and `pine` (553 K) already carry `decomposes_k`, which IS the pyrolysis onset where wood begins giving off flammable volatiles: half the ignition data is catalogued with nothing to burn. No material anywhere carries an autoignition temperature | **docs/64** — and note this does NOT widen the chemistry scope Robin set (*"basic chemistry in scope, complex, maybe someday"*): the burn rate is a SUPPLY problem, and supply is what the limiting reagent already models, just resolved in time rather than once. Needs: `reaction` blocks for the combustible materials, autoignition temperatures sourced (Law VII SOP), and `atmosphere::AirField` finally wired — it is verified and instantiated in NO scene (row 12), so the oxygen a fire would deplete is not being tracked anywhere | `oxidation::burn`: its own tests only |
| 33 | **Interior ballistics is about to become a SECOND answer to "how does matter push on matter".** The gas in a gun's bore is MATTER: it is shaped by the barrel walls, it rebounds off them, it pushes the ball, and some of it escapes past the ball and leaves the muzzle. Robin (2026-08-03): *"the walls of the cannon will shape the trajectory of the gasses, but the gasses will impact them, rebound, impact the ball, etc. Once again one engine, one principle, near-infinite ramifications."* A 1-D Noble-Abel interior-ballistics model — which is where this work was heading, and which is why a covolume was sourced — would be a separate physics for a case the contact law already covers. ★ The honest resolution is docs/44's own duality rather than a choice between them: the 1-D closed form is the **DECLARED** (cheap) layer and the resolved gas particles are the **RESOLVED** layer, with the declared one required to be derived from and convergent to the resolved one. A gun firing off-camera should still cost almost nothing. ★★★ **BUT THE DECLARED LAYER MUST KNOW WHEN IT IS OUTSIDE ITS OWN VALIDITY, and that is what makes it admissible at all.** Robin (2026-08-03): *"a ball slightly too large, a charge too strong, should be able to destroy a cannon as history shows it did."* A 1-D model treats the barrel as a fixed boundary and the shot as free to move, so outside those assumptions it does not become INACCURATE — **it answers a different question**, confidently reporting a muzzle velocity for a gun that should have burst. That is worse than being wrong: it describes an event that did not happen, and nothing downstream can tell. ★★★ **AND THE VERDICT IS ARITHMETIC, NOT A SIMULATION** — Robin's refinement, which dissolves the apparent cost problem: *"if it all checks out we hand the easy 1d to the renderer, if physics predicts catastrophy, we share that with the renderer. No need to actually render the matter particles, so should be a fast calculation."* **Deciding WHETHER a gun bursts does not require simulating the burst.** Peak pressure from the burn, hoop stress `sigma = p*r/t` against the barrel's tensile strength, and driving pressure against the shot-start resistance of the ball's interference fit — all closed-form, on numbers already catalogued. Matter is resolved only for a burst that IS happening, to show HOW it comes apart, never speculatively to find out whether it will. That is Law III exactly: math sizes the interaction, and only the minimal necessary matter becomes particles. ★ **The honest bound on the predicate:** it answers *"does a SOUND gun of these dimensions hold?"* — real guns also burst from casting voids, fatigue and flaws, which arithmetic on nominal strength cannot predict. A flawed gun is a different assembly, and saying so is the difference between a bound and a blind spot | two concrete gaps found by looking rather than assuming: `grep -n "boundary\|wall" crates/engine/src/gpu_sph.rs` returns **nothing** — the SPH path has no wall handling at all, because its only consumer so far is a self-gravitating giant impact with no container; and its EOS table is **Tillotson only** (`SphEos::from_tillotson`), i.e. condensed matter, with no gas closure — `atmosphere.rs` has the ideal-gas one and the two have never met | **docs/64** — what the resolved path needs is a gas EOS (Noble-Abel is the ideal gas law plus a covolume, so it GENERALISES `atmosphere.rs` rather than competing with it: covolume 0 reduces exactly to today's behaviour) and a moving-boundary contact for the walls and the shot base. Both are additions to existing primitives, not new ones | `gpu_sph`: the space band only |
| 34 | **DEFECTS — a future capability, and the architecture already implies it rather than needing a new model for it.** Robin (2026-08-03): *"Over time we might find a way to model defects, and that could be very valuable... we *could* introduce a bubble in a cannon barrel in the model if we chose to and it should have catastrophic results."* ★★ **The insight is that a defect is not a physics, it is a GEOMETRY.** A casting void is absent matter. If a barrel is genuinely an assembly of material with shape — which is what docs/64 makes it — then a bubble is a hole in that shape, the stress concentration around it EMERGES, and the gun fails there. No `defect_model`, no flaw table, no probability of failure: the same contact and fracture law, applied to matter that has a gap in it. That is the engine's whole thesis stated on an object small enough to check. ★ Until then, the burst predicate answers *"does a SOUND gun of these dimensions hold?"* (row 33), which is a stated BOUND rather than a blind spot — real guns also burst from voids, fatigue and inclusions, and arithmetic on nominal strength cannot see any of them. ★★ **And a second failure mode belongs here, because the model should catch it too:** reloading a HOT gun could set the charge off in the loader's hands, which is why crews sponged between shots. That is the row 32 ignition threshold asked of a barrel instead of a fire — barrel temperature against the charge's ignition point — which makes a gun's RATE OF FIRE something that EMERGES from thermal mass and conductivity rather than being declared as rounds per minute | `gunmetal` and `cast_iron` are catalogued as of 2026-08-03 with the numbers that decide both: grey iron tensile 295 MPa against compressive ~930 (a barrel's hoop stress is TENSILE, the direction it is weakest in) and ductility 0.01 against bronze's 0.2 — which is why iron guns shatter where bronze ones bulge. ★ `a_barrels_thermal_mass_is_catalogued_well_enough_to_limit_a_rate_of_fire` pins that both metals carry specific heat AND conductivity, and that an iron barrel stores more heat per unit volume while bronze sheds it faster — two different guns with two different limits, from the data alone. ★ **`cast_iron_is_weak_exactly_where_a_barrel_is_loaded` could only be written HALF way**, and the missing half is row 30: `ductility` and `compressive_strength` are catalogued and `RawMechanical` does not deserialize either, so `Material` cannot carry them and no test can assert through the engine what the file plainly says | **docs/64** §7 — a defect makes the compile-time validation genuinely interesting rather than a formality: the same predicate run on an assembly with a void in it should FAIL, and that is how the feature proves itself | **none** — defects are not modelled and are not claimed to be |

| 35 | **A CANOPY IS DARKER THAN ITS OWN LEAVES, and the engine has no way to say so — because the darkening is an ARRANGEMENT, not a substance.** A single fresh leaf returns ~0.07-0.08 of the visible light that falls on it; a closed forest canopy returns roughly a third of that, and the difference is pure geometry — light that scatters into the canopy is caught by the next leaf down, and crowns shadow each other. `conifer_foliage` and `broadleaf_foliage` are catalogued honestly as LEAF TISSUE with measured spectra, so a forest footprint rendered from them comes out brighter than a real forest, and both entries say so in their own `notes`. **The tempting fix is to darken the leaf, and that would be a Law V fudge of the purest kind**: it would put a property of an assembly inside a substance, so the same leaf held in a hand would be too dark to make a canopy that was right. ★ This is the substance-versus-assembly distinction arriving a FOURTH time (rainforest canopy, black powder, Irish albedo, now this), which is strong evidence it wants to be a first-class idea rather than a lesson relearned. ★★ The same row carries the SEASON: a leaf's colour is its PIGMENT CONTENT, so autumn is a different spectrum (chlorophyll degrading, carotenoids unmasked), never a tint on the summer one — Robin (2026-08-03): *"Flora changes colors/albedo through the seasons... Grass starts green in spring on the prairie, transitions to golden-yellow, leaves burst into a riot of color in cold climates before falling"*, and decisively *"even more honest is having the engine signal TEMPERATURE changes based on the earth's tilt and position relative to the sun"* plus *"the model should be able to calculate amount of DAYLIGHT in each area of biomes based on the tilt/season"* — so the season must EMERGE from tilt and orbit, not be tabulated | the two materials' measured 400-700 nm means are 0.084 and 0.069, against published CANOPY visible albedos of ~0.03-0.05 for forest — a factor of ~2 that no term in the engine currently accounts for. `Material::spectrum` exists and is read by exactly one thing (the albedo derivation), so the representation the seasonal work needs is in place and unpopulated: no material carries a senescent state | **docs/63**'s appearance integral is where the canopy term belongs — a footprint's answer is a MIXTURE over an arrangement, which is exactly what a canopy is, so this is not a new mechanism but the existing one given something real to integrate. The pigment data is already in hand: the NEON dataset pulled for the albedo carries chlorophyll and carotenoid mass per sample, so the causal variable for phenology is measured and sitting in the source | `blackbody::reflectance_srgb`: `materials::albedo_derives_from_the_measured_spectrum` only |

| 36 | **CLOSED 2026-08-03 — the camera was a clamp on an inflated ground, in a scene that owned it; it is now MATTER in the engine.** Robin: *"Camera must exist in the engine, but can be directed by the scene"*, and decisively *"the engine does a lot of calculation based on what can be seen, so it must know everything about the camera all the time"* — a camera the scene owns is one the engine's own resolution decisions trail by a frame. What was there: `alt_m.clamp(min_alt, ..)` stacked on a ground height that was the **MAX over a ±0.2° (~22 km) neighbourhood**, whose own doc claimed the clamp meant *"the camera obeys real physics"* — precisely backwards, since a clamp can only push the eye straight UP the radial and a camera driven at a steep face pops through it. ★★ **And that same 22 km envelope placed the CANNON**, so the gun floated above its own ground whenever a hill stood anywhere within 22 km. That was a real bug and it shipped. ★ **Three answers to "how high is the ground here"** existed in Terra alone: a closure inside `build_segment` (raster blended with streamed tiles — what the mesh actually draws), the 22 km max (raster only, no tiles) for the camera, and that again for the gun. Collapsed to one `ground_elev_m`, which the mesh, the camera and the assembly placement all call | **Measured through the engine** (`web/rig/terra_camera_is_matter.mjs`): with the zoom floor set to **1 mm so the clamp could not be the cause**, an eye asked to sit 200 m BELOW ground comes to rest at **0.35 m — exactly the shell's half-extent — at the Rockies, the Himalaya and the prairie**, and at 0.39 m on the steep site because contact resolves along the NORMAL rather than the radial. That extra 4 cm is the slide a clamp cannot produce, and `the_shell_slides_along_a_slope_instead_of_popping_straight_up` pins it natively. ★ A second finding on the way: `sweep_shell_resolve` caps each sample's correction at 0.5 m — a relaxation RATE, right for per-frame motion and unable to recover from a teleport, and a scene teleports the camera every time it names a coordinate. Measured before the fix: the eye rested **895 m inside** a plateau. The sphere version now settles the constraint after sweeping | **docs/65** — the camera is the first thing paid off the scene/engine debt register, and `granular::sweep_shell_on_sphere` is where the law lives so it is natively testable; `Terra` sits behind `cfg(wasm32)` and cannot be unit-tested at all, which is docs/46 row 14 biting directly. ★★ **STILL OPEN, and it is NOT this row's doing: below about 2 m the surface is not drawn.** An altitude ladder (`web/rig/terra_low_altitude_ladder.mjs`) measures ground filling the frame at 2000/500/100/20/**5** m and gone at **2/1/0.5/0.2** m — and a CONTROL run on the pre-change build gives byte-identical numbers, so the cliff is pre-existing. What this row changed is that the inflated ground used to stop anyone ever getting low enough to see it. This is docs/63 item 4 measured precisely: the cliff is between 5 m and 2 m, not 1 m | `granular::sweep_shell_on_sphere`: `Terra::render` and (until it is deleted) the Ground scene |

| 37 | **CLOSED 2026-08-03 — the Ground scene is deleted, and with it the SECOND surface render path.** Robin, on finding it still shipping: *"'Ground' scene is back? It should be destroyed with fire. It is so wrong, so badly designed and totally out of step with the concept of the engine… a cube of terrain with no planet to support it, anathema to the engine."* She had killed its predecessor (`a9395bf`, docs/50); this was a REBUILD of it (`459404a`, docs/55) that never got killed. Its world declared `lat 45, lon -100` on Earth and then invented its relief from three fbm octaves in a 96³ voxel cube — docs/63 item 1's *"a real place with imaginary ground"*. Gone: `ground_scene.rs` (1,209 lines), `ground.html`, `src/ground.ts`, **`shaders/world.wgsl`** (its only consumer, so docs/63 item 3's two-render-paths becomes one), the nav entry, the vite input and four rigs. ★ The third `#[wasm_bindgen]` scene struct is gone with it, so `SCENE_MODULES` is now `["lib.rs"]` — row 14 is one struct closer to nothing | **The law was never in the scene.** The crater lifecycle — resolve-on-impact, real contact, `recohere_when_settled`, crater persists as geometry — lives in `simulation.rs`/`matter.rs` with **55 native tests**, including `a_settled_impact_aftermath_leaves_ground_not_bare_particles`. So the deletion cost a DEMO, not a verification, and that was checked before anything was removed rather than hoped afterwards. The world file survives as `assets/worlds/ground-patch.json`, the fixture three engine tests read to prove a ground patch still BUILDS from a definition. 523/523 native, `mod app` clean for wasm32, production build green, and the surviving scenes rig-photographed unchanged | ★★ **STILL OWED, and it is a FEATURE rather than a port: Terra cannot crater.** `Terra::render` steps `flight`, receives an arrival, logs its energy and **discards it** — no matter is resolved, no ground changes. Robin's shape for the replacement (2026-08-03): *"Cost should be resolved in a proper meteor shower follow that stops where we can observe terrain change with impact."* The camera half of that now exists (`camera_follow`, row 36). The missing half is a MUTABLE surface: `Simulation`'s lifecycle runs on a voxel `World`, while Terra's surface is a mesh derived from rasters, so a crater has nowhere to persist. `gpu_sph::promote_ground_cap` — surface → matter — is built, tested, and has **zero consumers**, which is the docs/48 pattern exactly | `simulation::Simulation`: native tests and `bin/run-definition` only — **no scene** |

| 38 | **The season now EMERGES from tilt and orbit — and what remains owed is the PIGMENT model and the thermal one.** Robin (2026-08-03): *"Even more honest is having the engine signal temperature changes based on the earth's tilt and position relative to the sun"*, and *"the model should be able to calculate amount of daylight in each area of biomes based on the tilt/season."* Built: `solar` — declination split out of the sun the terminator is already drawn with, day length `cos H0 = -tan(phi)tan(dec)`, the daily-insolation integral, and `Moment`/`RespondsToTime`, the general signal an actor answers (the test actor is NOCTURNAL, precisely so the mechanism cannot quietly become a leaf-colour hook). ★★ **THREE THINGS ARE STILL DECLARED.** (a) **The thermal term does not exist.** Insolation is TOP-OF-ATMOSPHERE — no cloud field — so a surface temperature from it alone runs warm; and temperature LAGS photoperiod because ground and ocean have heat capacity, which is why the warmest weeks trail the solstice and why leaves turn before the cold arrives. Without it there is no year-to-year variation and no frost event. (b) **Senescence interpolates between two measured leaves rather than deriving a spectrum from PIGMENTS.** The resolved version carries the pigment complement and sums what it absorbs — and Robin's note is why the complement must be a variable, not a constant: *"There is also red chlorophyll, which is useful for catching longer wavelengths."* Chlorophyll *a*/*b* give out around 660-680 nm while **chlorophyll d and f absorb well past it — f beyond ~720 nm** — which is how some cyanobacteria live on light a leaf cannot use. An engine that hard-codes "chlorophyll absorbs red" cannot represent them, or an ocean's photosynthesisers. (c) **Nothing consumes the senescence phase yet** — the materials carry no senescent spectrum in the shipped catalogue, so Ireland does not yet turn | **Measured from the data, not assumed.** The NEON fresh-leaf set carries chlorophyll AND carotenoid mass per sample, so broadleaf endmembers were binned by PIGMENT rather than by date: **senescent (chl 1.96 mg/g, n=12, 10 taxa) 450/550/660 = 0.044/0.141/0.048** against **mature (chl 11.10 mg/g, n=11, 9 taxa) = 0.034/0.089/0.031** — a 5.7x chlorophyll difference, and as chlorophyll falls the leaf brightens in BOTH green and red, which is senescence. Those n are thin and the low bin is low-chlorophyll rather than fully turned; both are stated. ★ The model reports its OWN validity (`senescence_phase` returns the phase and the annual day-length range), because at the equator the range is ~5 minutes and normalising by it turns noise into a full autumn — the docs/46 row 33 requirement applied again. ★★ And a TYPED FIXTURE caught itself here: the polar solstice insolation was asserted as "~52.6 MJ/m2" from recall and came back 46.8; the physics settles it with no reference at all, since at the pole the Sun circles at constant elevation so the day's total is exactly `S*sin(dec)*86400` = 46.7. The formula was right and the memory was wrong | **docs/65** — this is the first real instance of the assembly answering a time signal, and the cadence belongs to the ACTOR (a leaf answers in days, a bear in minutes, a rock never) so one clock serves many rates | `solar::senescence_fraction`: **no consumer yet** — the classic docs/48 shape, recorded on the day it was written |

| 39 | **EARTH HAS TWO AXIAL TILTS.** Robin, asking the right question while the seasons were being wired (2026-08-04): *"this is all to determine different seasons on earth, which invert between north/south hemisphere based on tilt of Earth's axis; I'm not sure we have a tilted axis yet?"* Half yes. **`orbit.rs` carries the real one** — `obliquity = 23.439 - 4.0e-7*n` degrees — and every seasonal quantity derives from it, so the subsolar latitude swings ±23.4°, the Arctic circle lands at 90-δ, and the hemispheres reverse. Terra is therefore CORRECT: it works in the Earth-fixed frame where the tilt appears AS the declination. **But `lib.rs` declares the space band's Earth with a spin axis PERPENDICULAR to its orbit** — the comment says so outright: *"Modern Earth: the measured sidereal day, spin axis ⊥ the orbital (x-y) plane"*, i.e. obliquity ZERO. ★ So the same body has an axial tilt of 23.439° for the purpose of lighting and 0° for the purpose of dynamics, which is Law II exactly: one question, two answers, and they cannot both be Earth | `grep -n 23.439` finds one site (`orbit.rs:776`); `lib.rs:1496-1499` builds `spin_l` from `DVec3::new(0.0, 0.0, 1.0)` with no obliquity applied. The seasonal tests in `solar` all pass BECAUSE they read the orbit-side value — they would pass identically with the body upright, which is what makes the divergence invisible from the test suite alone | **docs/25/27** — the consequences are real but confined to the space band: an Earth viewed from orbit shows no axial tilt, its terminator does not lean through the year, and any precession or tidal torque computed from `spin_l` is being computed for the wrong body. The fix is to build `spin_l` by rotating the orbital normal through the SAME obliquity `orbit` already owns, so there is one tilt and it is written once | `orbit::solar_declination_ra` (lighting, correct) and `OrbitDemo`'s body init (dynamics, upright) — the two answers |

| 40 | **REAL LAND COVER IS IN (2026-08-04), and the bake script had the layer name wrong.** Row 28 has blamed the invented latitude-band cover for the orange ground, the conifer Ireland and the nothing-to-integrate appearance integral. The cause turns out to be one string: `bake.py` asked GIBS for `MODIS_Terra_Land_Cover_Type_Yearly`, which does not exist. **GIBS answers a bad layer with HTTP 200 and an XML error body**, so the fetch looked successful, only the image decode failed, and the `except` swallowed it as *"GIBS land cover unavailable"* before falling back to six latitude bands. A silent fallback that logs a plausible reason is worse than a crash. Verified against GetCapabilities 2026-08-04: the real name is `MODIS_Combined_L3_IGBP_Land_Cover_Type_Annual`, and a request returns a genuine **18-colour IGBP raster (17 classes + water)** | Spot checks against the fetched raster, and they settle three open questions at once: **Galway returns the SAME class as the Serengeti** — grassland/savanna, i.e. Ireland is PASTURE, not the boreal conifer the invented bands claimed (which is why it could not turn, docs/46 row 38); Maine is MIXED forest; the Amazon evergreen broadleaf; the Sahara barren; Siberia woody savanna. Reference raster committed at `tools/bake-earth/reference/modis-igbp-landcover-2020-1024.png` so the next session starts from evidence rather than a download | **docs/63** — what remains is the IGBP colour → class → MATERIAL MIXTURE map, and it must be a mixture rather than one material per class, because IGBP classes are things like *woody savanna* and *cropland/natural mosaic* that are definitionally mixtures. That is exactly what `terra::appearance` was built to integrate and has never had. ★★ And Robin's next requirement lands here too (2026-08-04): *"low-cost models for grasses and trees (low poly for now but reasonably faithful)… stored as assemblies themselves and added as members of Earth so they can be rendered as called for"* — at altitude a plant is its albedo, at eye level it is geometry. That is the same object at two resolutions (docs/44), and it is what would finally make row 35's canopy darkening EMERGE from mutual shadowing instead of being a declared number | ★★ **DONE the same day**: `landcover.png` is now 18 MEASURED IGBP classes at 100% exact palette decode, and `earth.json` maps each to a MATERIAL MIXTURE whose fractions come from the IGBP class definitions (forests >60% tree, woody savanna 30-60%, savanna 10-30%, open shrubland 10-60%, barren <10%), taken at the midpoint of each stated range. Galway is grassland, Iowa cropland, the Amazon evergreen broadleaf, the Sahara barren. `Surface::biome_mixtures` replaced `biome_materials` and `aggregate_albedo_turned` gives a mixture its season, so a mixed forest turns partly and a savanna's grass and trees turn independently. ~~★★ RIG-VERIFIED on the 5060 Ti, and the hemispheres run opposite in the PICTURE.~~ **RETRACTED 2026-08-04 — see row 41.** The numbers below are real but they are not the leaves: the vertex colour never reaches the shader, so what was measured is the sun's own elevation changing through the year. The phenology is correct and does not arrive. Original text kept: Measured R:G of the ground (hue, not luminance — at 45N the noon sun falls from ~68 to ~21 degrees between June and December, so brightness is dominated by the sun's own elevation and cannot isolate a season): **Maine 0.6886 -> 0.6908 -> 0.6939 -> 0.7005** and **Ireland 0.6855 -> 0.6879 -> 0.6904 -> 0.6984** across Jun/Sep/Oct/Dec, both reddening monotonically; **the Serengeti runs BACKWARDS — 0.6875 -> 0.6901 -> 0.6867 -> 0.6855** — because at 2.3S its December is its growing season. Nothing was told to do that; it falls out of the axial tilt through the declination. ★ Honest about the size: the effect is measurable and monotone but VISUALLY SUBTLE, because broadleaf is only 0.45 of that class and row 35's canopy-versus-leaf gap flattens it further. It turns; it is not yet dramatic. (The GPU needed an NVIDIA kernel-module reload first — 580.173.02 running against 580.178.04 userspace after an in-place upgrade.) |

| 41 | **CLOSED 2026-08-05 — TERRA HAS A SKY, AND IT IS THE AIR (docs/66).** The finding, kept as written: **TERRA HAS NO SKY, AND THE VERTEX COLOUR IS THROWN AWAY — two findings, one wrong claim of mine.** Robin (2026-08-04): *"the engine seems to be rendering the color without taking available light into account… Grass shouldn't be apparently brighter than everything else at night, right?"* ★★ **(a) `shaders/sky.wgsl` is compiled by NOTHING.** Its header says *"for the terrain scene"*; that scene was deleted in July, its successor the Ground scene was deleted 2026-08-03 (row 37), and **Terra never had a sky of its own**. Terra lights the GROUND correctly and adds a Rayleigh veil to it, but the sky region falls through to the star field — so a daylight frame near the ground is vivid lit grass under a BLACK STARFIELD, which reads exactly as Robin describes: the ground looks luminous because the sky is missing, not because the ground is wrong. Measured: the night side IS correctly near-black (Galway at -19.9 deg sun elevation, ground 14/17/23) and identical over sand, forest and open ocean, so albedo-without-light is NOT the defect. ★★★ **(b) THE VERTEX COLOUR NEVER REACHES THE PICTURE.** `globe.wgsl` computes `albedo = grain * u.tint`, where `grain` is the triplanar texture of the class's DOMINANT material; `i.col` appears in that shader only inside comments. So the land-cover MIXTURE and the SEASON are both computed per-vertex and discarded | **Proved by mutation, not read**: every land vertex's colour was set to magenta and the frame came back unchanged GREEN (56.9/81.4/55.1). ★★ **This retracts row 40's headline.** I reported *"Ireland turns"* from an R:G shift of 0.6886 -> 0.7005 across Jun-Dec, and that measurement cannot have been the leaves: with the vertex colour discarded, what changed was the SUN'S OWN ELEVATION at 45N (about 68 deg in June to 21 in December) and therefore the atmospheric reddening. The Serengeti 'running backwards' fits the same explanation — its sun is highest in December. The phenology is computed, tested and correct; it simply does not arrive | **docs/12** settles the texture design (procedural, from cited optical properties, no bundled images) and it is working: ground high-frequency detail measured 0.134 at 4 km against 0.47-0.70 below 600 m, at 8 m per tile. What is missing is that the texture is ONE material's grain where the ground is a mixture, and plants have no texture at all. The fix for (b) is to spend the mixture and the season in the shader rather than replacing them; the fix for (a) is a sky Terra owns | ★★ **BOTH HALVES CLOSED.** (b) closed 2026-08-04 — the vertex colour now arrives as a mixture-over-dominant-albedo ratio. (a) closed 2026-08-05: `sky.wgsl` is compiled, and by a MARCH rather than the flat-slab closed form it used to call. `atmosphere::air_inscatter` + its WGSL mirror `atmos.wgsl` is ONE integral that the sky, the ground's aerial perspective and the star field all spend, pinned to `rayleigh_veil` (under 1%) in the geometry that closed form actually solves. Rig-measured at 2560x1600 with an AIRLESS-EARTH control in the same run: zenith **52/95/185 with air against 2.7/3.0/3.7 with none**; sunset B/R **3.12 -> 0.85** with nothing changed but the clock; twilight now EMERGES from the planet's own shadow instead of a declared sqrt(2H/R) ramp. Retired with it: `veil_column_fraction`, `rayleigh.wgsl`, and the painted 0.01/0.01/0.03 background. ★ The gate that would have caught this exists now — `laws::compiled_shader_tests::every_shader_is_compiled_by_something` — and caught a SECOND orphan on its first run: `particles.wgsl`, superseded in **July** |
| 42 | **THE SPACE BAND'S SHELL BODIES STILL SPEND THE CLOSED FORM — now a MEASURED approximation rather than an unexamined one.** With the ground and the sky both marching `air_inscatter`, the one place `rayleigh_veil` still reaches a picture is the per-grain shell tint (`OrbitDemo`'s 4,096 grain shells, and Terra's own shell fallback): flat-lit spheres tinted on the CPU with the plane-parallel formula plus `rayleigh_transmit`. Those are drawn from OUTSIDE looking down, which is exactly the geometry that formula solves, so this is legitimate specialization and not two answers — but its ASSUMPTIONS are the approximation, and they are now quantified instead of assumed | `the_march_reproduces_the_closed_form_from_above` pins the two to **under 1%** in the plane-parallel limit, which is what makes them one law. `sphericity_is_small_overhead_and_decisive_at_the_limb` prices what the flat-slab assumption costs on the real Earth: **under 5%** looking straight down, and **the whole limb** — the closed form has no ray that misses the ground, because a slab has none | **docs/66 §2** — the fix is to march the shells too. It is not in the same change because those bodies are tinted per-grain on the CPU, which is a different cost question, not a different physics one | `atmosphere::rayleigh_veil`: the shell tint in both scenes, plus the test that pins the march to it |
| 43 | **ONE FRAME, TWO EXPOSURES — and giving Earth a sky is what made it visible.** The star pass carries `exposure = 80`; every other view of this world carries `atmosphere::SUN_GAIN = 22`. Both are openly declared display constants, but the RATIO between them is a physical claim and it is wrong: a magnitude-0 star delivers ~2.5e-8 W/m2 against the Sun's 1361 W/m2, so the honest ratio is ~5e-11, modified by how much of a pixel's solid angle the PSF concentrates that flux into. Nothing could see this before 2026-08-05, because each pass tone-mapped its own output and the two never met in one number | **Measured in the first daylight sky frame**: at the zenith the brightest pixel is **+128% above the sky mean**, where a real daytime star is ~0.01% of it. Stars were plainly visible through a blue sky; they now SUM with it (`tonemap(L_sky + L_star*T)`, docs/66 §5), which is why they are faint rather than absent — and why what is left is a clean statement about the exposure ratio rather than a compositing bug | **docs/66 §5** — the derived form needs the pixel's SOLID ANGLE, which is real physics (a star genuinely is brighter through a longer lens) and which neither constant currently knows. The deeper fix is to tone-map ONCE into an HDR target: every pass compressing its own radiance is the reason sums that should be physical are not | `render::StarField` (exposure 80, both scenes) against `atmosphere::SUN_GAIN` (everything else) |
| 44 | **SPACE IS A BACKGROUND PASS, NOT A COLLECTION OF ASSEMBLIES — and a body's boundary is inferred rather than stated.** Robin, correcting a rig that was locating the planet's edge by scanning pixel columns (2026-08-05): *"This should be done in the engine as a boundary between the assembly (containing the atmosphere as a component of Earth) and space, which should be a collection of assemblies of type 'star' at coordinates."* Two gaps. (a) A star is not an assembly: `sky::Star` carries a real position in parsecs, a real luminosity and a colour derived from a real temperature — everything an assembly needs — but it is consumed by `render::StarField`, a background pass, so the engine cannot ASK a star anything and "the sky" stays a render concept rather than the assemblies that happen to be far away. (b) Nothing downstream should infer where a body ends; the assembly knows | ★ (b) is PARTLY closed the same day, and the wording was corrected by Robin in the doing: I wrote *"a body ends where its air ends"* and she replied *"not accurate, as bodies are assemblies. An assembly ends at the outermost boundary of the assembly"* — so the rule is general and air is merely Earth's outermost component. `AirColumn::outer_reach()` states that component's contribution (~97 km for Earth, exactly the surface for a body declaring none, no branch) and `angular_reach_from` gives it from an altitude — `an_assembly_reaches_past_its_core_by_its_outermost_component`. **The general `Assembly::outer_reach` — a max over components — does not exist**; `assembly.rs` has no extent method at all. The rig that prompted this was finding the limb where the ROCK was, which is 97 km inside where the assembly actually is. (a) is open and unmeasured | **docs/65** — the scene names the actors, the assemblies know themselves, the engine holds the universe. A boundary between two assemblies is the engine's fact to state; every time something else derives it — a rig from pixels, a renderer from a hardcoded radius — it is one error class in different clothes. ★ Robin also: *"And of course Luna should be its own assembly"* — today the Moon is a `LayeredBody` the space band places, correct in the respect this change touches (it declares no air, so it gets a knife-edge terminator from the same code path Earth's sky comes from) but not yet an assembly with components in the docs/64 sense | `render::StarField`: both scenes, as a background pass; `sky::Star`: parsed, never asked |
| 45 | ★★★ **TWO FORMATS FOR "A THING THAT EXISTS", AND NOTHING RECORDED IT.** `assembly::Assembly` (parts of material + shape + orientation + packing, connections, nesting) describes SIX objects — cannon, charge, shot, oak, spruce, grass tuft. `planet::LayeredBody` (concentric layers, an atmosphere mass, surface rasters) describes every planet. Robin, on being told Earth is the second (2026-08-05): *"WHAT? Everything we've done is supposedly building an assembly of earth, and you blithely inform me it is a LayeredBody? … A layered body is either a bad assessment or the last couple of days work with Claude are a lie."* The assessment was right and **the DOCUMENTATION was the lie**: `docs/65` §4 is titled *"Where it stands, honestly"*, counts 79 scene-called methods and three scene structs, and never says that no planet is an assembly; the architecture page shipped to integrity.bothead.net said *"adding a species, a vehicle or a planet is adding an assembly"* in the present tense. Two days of work were described in assembly language on top of that silence | ★★ **The split is lineage, not principle, and the engine already proves it in BOTH directions.** Robin's argument: *"A planet is an accretion of debris bound by its own gravitational effects which we've worked hard to model."* The round trip is built and tested — `hydrostatic::HydroBody::particalize(&LayeredBody, n)` turns layers into particles and `accretion::sample_layers(...)` reads layers back out of particles. So `LayeredBody` is not a different KIND of object: it is a **de-resolved summary of an assembly of matter**, exactly the relationship `Derived` already has to `Assembly::parts`. Two names for one idea, sharing no type. What actually resists is three things assemblies need anyway: `Shape` has no shell variant (though `Tube` IS the cylindrical one), a planet's surface is a raster (which docs/63 already argues is a RESOLUTION of matter, not a different kind), and containment at 10^12 cannot be a `Vec` | **docs/67** states the unified model, its migration order and the smallest falsifiable first step. ★ The guard rail that makes it safe already exists: `one_earth_tests::the_three_scenes_read_one_earth` asserts DIGIT-IDENTITY across the three scenes reading Earth, so any migration that moves a number fails. ★★ This is the FIFTH arrival of the substance-versus-assembly distinction — row 35 counted four (rainforest canopy, black powder, Irish albedo, canopy darkening) and said it *"wants to be a first-class idea rather than a lesson relearned"* | `Assembly`: six objects. `LayeredBody`: every planet. Neither knows the other exists |
| 46 | **`instance::Instance` IS BUILT AND HAS NO CONSUMER** — filed on the day it was written, by its author, because that is the only defence against the pattern. docs/67 step 2 separates the TYPE (`broadleaf-tree-oak.json`, one species) from the INDIVIDUAL (the tree at 53.1N, with its own lean, damage, heat and container), which is the prerequisite for everything on the assembly roadmap: damage, attitude, momentum and containment have nowhere to live without it. Five tests pass. Nothing holds one | ★ **The reason is a real dependency, not neglect, and the migration order predicted it.** The obvious first consumer is Terra's cannon, whose placement is `cannon_at: (lat, lon, bearing)` — a GEOGRAPHIC placement. Turning that into a `Placement { at_m, attitude, within }` means the gun is *within Earth*, which requires Earth to be an assembly with an id: docs/67 **step 5**, three steps later. The second candidate, `terra::flora::Sited`, is regenerated from a position hash every rebuild, so an instance placed there would not survive the next frame — that is the derivable-rule-plus-exceptions problem, docs/67 **step 4** | **docs/67** §6 — the order is 2 (this) → 3 (extent/attitude/momentum on the instance) → 4 (containment as rule + exceptions) → 5 (`Shape::Shell`, Earth as parts) → 6 (the signal) → 7 (the per-assembly solver). This row closes when something holds an `Instance`; if it is still open when step 4 lands, the order was wrong and that is worth knowing | nothing. `instance.rs` is reachable from `lib.rs` and called by no scene, no solver and no renderer |
| 47 | **CLOSED 2026-08-05 — the trees stay put.**  — `flora::scatter`'s lattice is anchored to the CAMERA, not to the ground.** Robin asked for the opposite explicitly (2026-08-04): *"so trees don't move to different positions when one turns one's head away and turns back"*, and the module's own doc claims it: *"no seed, no state, no order dependence, so the answer cannot depend on when or whether anything looked"*. It is half true. The hash is stateless, but its inputs are not absolute: cells run `-n..=n` about `centre_lat`/`centre_lon`, and the cell SIZE comes from the cover fraction sampled at that centre. Move the camera and both the lattice origin and possibly the spacing change, so every plant in the stand is regenerated somewhere else | Found by attempting to wire `containment::Contents` to it (docs/67 step 4), which requires a rule to be a pure function of POSITION so an exception can name what it damaged. `containment::tests::the_same_region_answers_the_same_way_however_often_it_is_asked` is the assertion flora would fail: it checks that a tree keeps its identity when the same ground is queried from a different centre. Terra only rebuilds after 2 m of camera movement, which is why this has not been obvious — the stand is stable until it jumps | **docs/67 step 4** — and the fix is the one containment needs anyway: index cells in ABSOLUTE coordinates. That means the lattice spacing must come from the KIND's own crown rather than from the local cover fraction, with cover modulating density by SKIPPING cells instead of by resizing the lattice. Then the grid is global, a tree's cell is a fact about the ground, and its derived id is stable — which is the precondition for a bus being able to crush one ★ **FIXED the same day.** The lattice is now ABSOLUTE: spacing comes from the KIND's own crown (`sqrt(crown_m2)`) and cells are indexed from a global origin, with each ROW's east-west spacing taken from the row index rather than from the camera. Cover no longer resizes the lattice — it decides OCCUPANCY, cell by cell, which is what a cover fraction means and which also makes density follow the cover at each plant's own position instead of the one sampled at the query centre. `Sited` now carries the derived `InstanceId`, so a container can hold an exception for one plant. Two tests: the same ground asked from three centres 4-9 m apart returns the same plants with the same ids, positions, yaw and scale; and two species sharing the ground never share an identity — Robin: *"different flora types have different spacing, so lattices will overlap"*, and they must, since grass grows under an oak. ★ **The cost is measured, not waved at**: spacing no longer widens where cover is thin, so a sparse meadow walks as many cells as a dense one — ~220,000 for grass over a 25 m disc, about 1.7x the old lattice, and the flora test module went 0.05 s to 3.5 s once the tests asked about realistic radii. Two honest ways down are named in place; neither is worth doing before a rig measures a rebuild in a real scene | `terra::flora::scatter`: `Terra::build_flora` only |
| 48 | **CLOSED 2026-08-05 — THE PLANT BUDGET WAS A CRATER.** Robin, on an earlier render: *"when I scrolled in far enough trees should be visible, the columns were clumped in one tiny area in center of viewport, suspiciously crater like."* `flora::scatter` ended `out.sort_by(distance); out.truncate(budget)`, which is not a design — it is a way to bound a count. The arithmetic is exact: grass at a 0.35 cover fraction is ~31 plants/m², so 1,200 of them are exhausted inside `sqrt(1200/(31π))` = **3.5 m**. A disc of columns centred under the camera, every oak starved out | ★ **It predated the lattice change, and I first said it did not.** Cover used to set SPACING (one plant per `crown/frac` of ground) and now sets OCCUPANCY (one per `crown`, with probability `frac`); both are a density of `frac/crown`, identically, so the lattice fix was density-neutral and merely removed the accident that had been spreading the budget across kinds. Robin's earlier render is the evidence | The budget is now spent on the ANGLE a plant subtends — its own extent over the slant distance to the eye — which is the criterion docs/44 already uses to decide whether to resolve anything, and the same one that turns a plant back into its albedo above 300 m. No per-kind quota is written. `Kind::height_m` comes from `Assembly::reach_m`. Measured on Maine mixed forest: oaks present, stand reaches past 25 m, grass underfoot still resolved | `terra::flora::scatter` |
| 49 | **CLOSED 2026-08-05 — NO OAK COULD EVER APPEAR FROM STANDING HEIGHT, AT ANY BUDGET.** `build_flora` scanned ONE disc for every kind, sized `(alt * 8).clamp(6, 120)`. At 1.7 m eye height that is **13.6 m**, while oaks sit `sqrt(crown)` = **16 m** apart — a query disc smaller than a lattice's spacing cannot contain a single one of its cells. The number was derived from nothing; it is a renderer's fidelity-versus-cost decision (docs/68) wearing the model's clothes | Each kind now derives its own reach from two things already in hand, its density (`frac/crown`) and the budget: beyond `sqrt(budget/(π·density))` a kind cannot win a slot however big it looks, so scanning further is provably wasted. **Correct AND cheaper**: grass reaches ~3.5 m (3.4k cells) and oaks ~460 m (2.6k cells), against 285k cells for a single 32 m disc that still would not have held the oaks. Confirmed live — the mesh went from 43,200 tris (36 each, i.e. all grass) to 157,920 (131 each, a real mixture) | **docs/68** — the LOD constants belong on the renderer's side of the protocol. This one is fixed in place; `FLORA_ALT_M` and the segment's tessellation are the same species of number and are still model-side | `terra::flora::scatter`, `Terra::build_flora` |
| 50 | ★★★ **CLOSED 2026-08-09 — THE FLORA IS DRAWN AND CANNOT BE SEEN — 473,760 indices every frame, nothing on screen.** Robin asked to see the trees; there are none, at any altitude, at any site. The hunt is recorded rather than a conclusion, because what it ELIMINATED is the useful part. **Confirmed working**: `scatter` returns 1,200 plants including oaks; the placement transform reproduced NATIVELY (`assembly::placement_tests`) puts a tuft 3 m out at **3.077 m** from the anchor, standing **0.350 m** tall; the uploaded buffer probed in the live scene holds 151,647 verts and 473,760 indices at **0.02–251 m** from the anchor; the draw call itself logs `flora DRAWN` every frame; `Part::along` is `[0,1,0]` in source and compiled alike; and an UNFILTERED console capture shows no wgpu validation error of any kind | **Eliminated by probe, each deployed and photographed**: lifting every plant **3 m** changed nothing, so they are not buried by the ground mesh; lifting **200 m** changed nothing either, so it is not a ground-height disagreement at any plausible scale; drawing them with the SEGMENT's own uniform — the one the visible ground uses — also showed nothing, so it is not the flora uniform's model matrix. Culling is off, tint alpha is 1, and the same `globe_pipeline` draws the ground correctly in the same pass ★★ **SECOND ROUND, 2026-08-09, after row 51 fixed the camera.** Every earlier probe had been confounded by the 60 m error — plants lifted 200 m were ABOVE a frame whose eye was 60 m up, not hidden by anything. Re-run with the eye genuinely at 2 m: **plants lifted 5 m, self-lit, tinted magenta at 30x — still ZERO fragments**, so it is not depth and not occlusion. And a blind spot in my own instrumentation closed: the first bounds probe used `f32::min`/`max`, which IGNORE NaN, so a buffer of NaN would have reported clean bounds. Re-probed explicitly: **151,647 verts, 473,760 indices, max index 151,646 (exactly verts−1), NON-FINITE 0**. The data is perfect and nothing rasterises. | **docs/68 — this is the evidence for the seam.** Every model-side cause is eliminated and the picture never moved, which is as clean a bisection as this codebase offers: the fault is on the RENDERER's side of the line. Remaining suspects, narrow: the vertex ATTRIBUTE layout for assembly-built meshes specifically (the segment's vertices come from `terra::segment`, the flora's from `Assembly::mesh` — two producers, one declared layout), the material index reaching `texture_2d_array`, and depth behaviour at 1e-7 display units. ★★★ **BISECTED, THEN THE BISECTION DISPROVED — by the instrument built to test it.** The half-swaps showed the flora's uniform, pipeline and draw are all functional (segment mesh + flora uniform CHANGES the frame, mean 121.86 -> 99.36), so the fault was placed on the vertex buffer's contents, the one link verified on the CPU and never on the device. `renderer::Readback` was built to check it and **disproved it on its first use**: `terra-flora vertices: the device holds what was sent (6,672,468 bytes, 5,601,074 non-zero)`, a position-sensitive match. The INDEX buffer, which takes a different upload path (`mapped_at_creation` + copy rather than `write_buffer`), was checked too: 1,895,040 bytes, exactly 473,760 x 4, also a match. ★★ **So every link is verified — geometry, indices, uniform, pipeline, draw, layout, depth, camera — and the picture shows nothing.** That is a CONTRADICTION rather than a mystery, which is a better place to be stuck: one of those checks is verified in a way that does not mean what it appears to. ★ The instrument earned itself immediately — **one deploy cycle to overturn a wrong conclusion, against the nine it took to reach it**. Robin, whose hint it was: ★★★ **AND THEN IT GAVE THE COORDINATES.** Robin: *"at least now we should be able to see the coordinates of the graphical elements and figure out what happened."* Transforming known flora vertices with the EXACT matrices the shader receives: `clip[0]: ndc (0.409, -0.258, 0.997) IN FRAME`. **The plants are in frame**, and sitting at **z = 0.997** — jammed against the far plane. (The other two samples have negative `w`: plants behind the eye, which half of them are.) Confirmed against `fly_camera::view`: at 2 m altitude `near = (alt_disp*0.03).clamp(5e-9, 0.5)` = **0.06 m** and `far = horizon*1.4` = **7 km**, a ratio of **1:117,000**, so `1 - near/z` puts everything past ~20 m at z ≈ 0.997 and the whole visible world collapses into the last thousandth of the depth range. That is where the remaining contradiction lives: the ground is drawn first and `depth_compare: Less` rejects anything not strictly nearer. ★ The named fix is **reversed-Z** — near at 1.0, far at 0.0, `Greater` compare — which is the standard answer to exactly this near/far ratio and which the existing `Depth32Float` attachment already suits. It changes every depth comparison in both scenes, so it is its own change with its own rig evidence. ★ Not yet closed: a 5 m lift should have put a plant against the sky, where the clear depth is 1.0 and z = 0.994 passes, and it did not — so either the lift probe predates the camera fix (it does) or one more thing is in the way. *"I've got decades of experience behind me informing my hints"* | ★★★ **MEASURED 2026-08-06 — THE EYE AND THE PLANTS DISAGREE ABOUT GROUND LEVEL BY 60 METRES.** Probing the model translation actually used: `|anchor|=1.000017 |eye|=1.000026 |delta|=9.411e-6` = **59.96 m**, while `fly.alt_m` says the camera is **1.7 m** above the ground. The transform itself is right (the first frame after a rebuild reads exactly 1.70 m); what is wrong is that `view.eye` is built from one ground height and `build_flora`'s anchor from another. `TERRA_RELIEF_EXAG` is 1.0, so it is not exaggeration. The plants are placed ~58 m below the surface the camera stands on, i.e. buried. **Law II at the place it hurts most: two answers to "where is the ground here".** Robin predicted it while the hunt ran: *"the model needs to own ground level at all coordinates and be able to place assemblies there."* ★ NOT a complete explanation — a 200 m lift should then have floated them into view and did not, so either that probe ran against a different camera state or a second cause sits behind this one. The 60 m is measured; closure is not claimed | **docs/67** — ONE owner for ground level: the Earth assembly answering "what is the surface at this coordinate" once, for the camera, the mesh, the flora and anything else placed on it. Today `view_basis`, `ground_disp_at` and the segment mesh each arrive at it their own way ★★★ **CLOSED 2026-08-09. The plants were BEHIND THE CAMERA, and the named fix was wrong.** The suspect this row ended on was reversed-Z, from a probe reading `ndc z = 0.997` against a 1:117,000 near/far ratio. A NEGATIVE CONTROL settled it in one run — draw everything EXCEPT the ground and the plants were still absent (black sky, stars, an intact horizon), so depth and occlusion were innocent and a change touching every depth comparison in both scenes was called off. Projecting the mesh's own bounds with the frame's own matrices instead: `ndc y [-110.6 -9.2]`, `w [-1.215e-6 -1.022e-7]` — **all eight corners with NEGATIVE w**. `|anchor − eye|` came back as `|36.1 m − alt|` at 4000, 600, 300, 120, 40, 12, 3 and 1.5 m: a CONSTANT 36.1 m error. The elevation tiles stream in seconds after load and REVISE the ground (Galway: 426 m → 24 m in the log), `build_flora` had baked the pre-tile number into its anchor, and its rebuild test asked only *has the camera moved 2 m horizontally* — so neither the revision nor an entire descent from 4 km to standing height could invalidate it. Fixed by keying the cache on its INPUTS (`flora::Built`: position, the ground under it, and the eye height the angular budget was spent at), which is natively testable because it lives in the model and not in the wasm-only scene. **Rig-photographed at Galway: plants visible from standing height for the first time.** ★ The 58 m burial recorded above was REAL and is row 51's camera bug, already closed; it was not this. ★★ Why it took so long, and it is a docs/68 lesson in Robin's words — *"too much pollution between duties"*: the renderer had BAKED a model answer, so it owned a copy the model could not correct. Nothing was ever wrong on the GPU, which is why nine deploy-and-photograph cycles bisecting toward the device found nothing and `renderer::Readback` kept truthfully reporting that the device held what was sent |
| 51 | **CLOSED 2026-08-09 — A PLACEMENT IS NOT A MOVEMENT, and the camera was resting on mountains it never visited.** The camera is matter (row 36), so its contact sweep runs from where it last was to where it is going — right for a camera that WALKS, wrong for one that is PLACED. `place_camera` set the fly camera and left `last_eye_m` holding the previous eye, so `sweep_shell_on_sphere` swept a straight line from the old position to the new continent, resolved against the highest ground it crossed, and left the eye resting on a ridge nowhere near where the scene asked for | **Measured**: `place_camera(45.3, -69.0, 1.7, ...)` produced an eye **60 m** above the ground (delta 9.411e-6 display against an anchor of 1.000017), and the shell's own fix-up then recomputed `alt_m` to MATCH — so the HUD read a plausible **11 m** while the request had been 1.7, and every plant, placed on the real surface, sat ~58 m under the drawn one. With the swept path cleared on placement the same call reads **alt 2 m**. ★ The same trap sat in `clear_camera_pose` (the eye jumps from a pose back to the fly camera) and `camera_follow` (it jumps to a seat on a moving actor); all three now clear it, because the rule is general — **any verb that places the eye rather than moving it has no swept path** | **docs/65** — a camera verb behaving as though it were a motion verb, and the fix is to say which it is. The camera stays matter; it simply did not sweep | ★ **NOT the whole of row 50**: with `alt_m` correct the flora still does not draw, so a second cause sits behind this one |
| 52 | **A RENDERER CACHE THAT NO MODEL CHANGE CAN INVALIDATE** — the general form of row 50, and it is not fixed everywhere. The flora mesh was one; the rule it was keyed on (*has the camera moved 2 m*) named ONE of its three inputs. The pattern is a renderer holding a baked copy of a model answer whose only invalidation test is a camera predicate — so anything that changes the ANSWER without moving the CAMERA is invisible to it. Streamed elevation tiles do exactly that, seconds after load, on every scene | **Measured** at Galway: a 36.1 m error that survived a 4 km descent and every frame of it, while `flora_count` reported 1,200 and the draw reported 43,200 triangles. Closed for flora by `flora::Built` (`flora::cache_tests`, three tests, each failing against the rule it replaces). **Still open elsewhere**: `segment_built` (`ground_cap::tier_is_current`) keys the ground mesh on centre, cap angle and cell size — none of which changes when a tile arrives and revises the elevation UNDER an unmoved camera; the cannon's placement is recomputed per frame and is therefore safe by accident rather than by rule | **docs/68** — the honest fix is the protocol: the model tells the renderer its answer changed, instead of the renderer guessing from the camera when to ask again. Until that exists, every cache must name its own inputs | `terra::flora::Built`, `terra::ground_cap::tier_is_current`, `Terra::build_segment` |
| 53 | **CLOSED 2026-08-12 — TWO ANSWERS TO "HOW HIGH IS THE GROUND HERE", STILL** — the mesh displaces its vertices by measured elevation PLUS generated sub-raster relief; `ground_disp_at`, which places the flora, stands the cannon and resolves the camera shell, returns the measured term ALONE. So the surface you can SEE and the surface you can STAND ON are different surfaces. Three answers were collapsed into one on 2026-08-03 (`ground_elev_m`) and this is the fourth, which that collapse did not reach | **Measured** 2026-08-09 by a probe now logged on every segment rebuild: at Galway the gap is **+1.19 m** at the centre and **12.3 m worst over the patch** while the coarse raster is the only datum; once tiles cover (2.87 m/px) it falls to **0.000 m**, because the octave count is bounded by `log2(base/cell)` and the mesh cell is already finer than the data. So it is small exactly where the camera stands and large exactly where a descent has not yet streamed — the worst possible place for a discrepancy nobody is measuring | **docs/67** — ONE owner for ground level. Note the deeper problem the numbers expose: the generated relief is band-limited to the MESH's local cell, so the ground's height currently DEPENDS ON HOW FINELY IT IS DRAWN. That is Law IV inverted — the honest form is a ground height the model owns at full detail, with the mesh carrying a band-limited version that converges to it | `Terra::build_segment`, `Terra::ground_disp_at`, `surface_detail::micro_relief_m` ★★★ **CLOSED 2026-08-12.** `Terra::ground_relief_m` is the one owner, and the mesh and the model call it with different band limits rather than computing it twice. **The MODEL asks at `GROUND_DETAIL_FLOOR_M` = 1 cm** — a physical length, so the ground's height no longer depends on how finely it happens to be drawn (which was Law IV inverted); the truncation error is bounded at ~2.5 mm because amplitude goes as wavelength. **The RENDERER asks at its own cell**, which is legitimate under docs/68 §1b only because it CONVERGES. Measured live at Galway: the disagreement went from **12.3 m** to **0.31 m at a 999 m cell, 2.5 mm at 32 m, and exactly 0.000 once the tiles cover** — and `surface_detail::a_coarser_band_limit_approaches_the_full_answer_as_it_refines` pins the monotone convergence (23.1 → 3.26 → 0.085 → 0.046 → 0.0007 m). ★ The slope the relief scales by is now sampled PER POINT rather than once per segment at the camera, because the roughness of ground at a coordinate cannot depend on where anyone is standing |
| 54 | **CLOSED 2026-08-12 — THE RENDERER COMPUTED THE CRATER.** `globe.wgsl`'s VERTEX shader carries `crater_sink()` — a paraboloid excavation profile, `depth · (1 − (θ/θr)²)` inside an opening angle — and sinks the surface with it. Robin, 2026-08-09: *"The engine understands craters — radius, energy, impact, velocity. The renderer should be blissfully free of these calculations and just render the world as best it can, represent what the assemblies/models are telling it."* The engine's own ground answer (`ground_elev_m`) has no crater term at all | **Not yet measured**; the failure is structural and predicted: the ground you can SEE inside a bowl and the ground the model reports differ by the whole crater depth, so anything placed on the ground inside one — a plant, the cannon, the camera shell — stands on the un-excavated surface and floats. It is row 53's disease with a much larger amplitude, and it is the same shape as row 50's cause: a renderer deriving a fact instead of representing one | **docs/68 §1b** — the renderer may approximate HOW it shows what the engine says; a crater is WHAT the engine says. The depth already comes from excavated mass (Law VI), so the quantity is honest — it is living on the wrong side of the seam | `shaders/globe.wgsl` `crater_sink`, `Terra::ground_elev_m` ★★★ **CLOSED 2026-08-12.** `damage::Excavation` states the bowl in the ENGINE — a paraboloid, natively tested (deepest on axis, exactly 3/4 depth at half the opening angle, zero at the rim and beyond), with `from_energy` built on the same `crater_volume` that decides a meteor's crater, so a caller still cannot ask for a bigger hole. `globe_mesh::SurfaceSampler::excavated_by` subtracts it, which puts the crater in **every** surface built from that sampler — the globe, the corridor cap and Terra's segment — and, crucially, in the MODEL's own answer, so something standing in a fresh crater no longer floats at the pristine radius. `crater_sink` is deleted from `globe.wgsl`; the uniform patch is gone and `CRATER_UNIFORM_OFFSET` is retired with its two fields kept as documented PADDING, because `SKYLIGHT_UNIFORM_OFFSET` names a byte offset past them. The mesh rebuilds when the bowl changes materially, keyed on the excavation it was built for (row 52's lesson). ★★ **AND THE GATE THAT GUARDED THIS WAS BROKEN BY CLOSING IT.** `a_render_shader_does_not_move_matter` compared a DEBT list against the tree; with the list empty, empty equalled empty and re-adding `crater_sink` PASSED. Caught only by re-running the mutation after closing the debt. It now scans for the CONCEPT — any function in a render shader whose name says it moves, deforms or excavates matter — and was verified against both the old offender and a NEW verb in a different shader |
| 55 | **A CONTAINER DOES NOT PLACE ITS CONTENTS — A SCENE WALKS A LIST.** Robin decreed the model (docs/67: an assembly holds other assemblies and positions them) and then, on being told the code now matched it, checked rather than accepted: *"that 'its container places a plant' may not be accurate yet. That is the model I decreed, but I'm not certain the source matches that model."* It does not. `Terra::build_flora` iterates `scatter`'s output and places each plant itself; Earth is a `LayeredBody` with no contents to ask | **Partly closed 2026-08-09**: the placement geometry is now a model question with no viewer in it — `assembly::stand_on_body` answers in metres in the body's frame, `assembly::model_of` is the separate renderer step, and `instance::Placement` finally has a consumer (row 46). What remains is WHO ASKS. Before this the answer was a display-scaled eye-relative matrix, so "where is this plant" could not be asked without a camera in the room | **docs/67 §5** — the blocker is Earth-as-assembly, the same one rows 46 and 52 wait on. ★ The doc comment says so in as many words, because the failure mode here is documented: on 2026-08-05 an architecture page described assemblies in the present tense that did not exist | `Terra::build_flora`, `assembly::stand_on_body` |
| 56 | **NOTHING IS LIT BY THE SKY.** Every surface in the engine receives DIRECT SUNLIGHT and nothing else. The atmosphere's own radiance is computed — `air_inscatter` is spent by the sky pass, by the ground's aerial perspective and by the star field — but it never returns as IRRADIANCE on anything, so a surface facing away from the sun is lit by nothing at all | **Measured by picture** 2026-08-09, and it is what made the new grass clumps legible as a bug: blades of the SAME `grass` material as the ground beside them render nearly BLACK, because a near-vertical 3 mm blade presents almost no area to the sun. A horizontal ground hid it; the first vertical surface at eye level exposed it. Real grass is lit predominantly by skylight — a hemisphere of blue — which is why a lawn is not black between the sunlit tips | **docs/66** — the integral that would answer this is already built and already spent three times; what is missing is the fourth consumer, the hemispherical integral of sky radiance over a surface's normal. That is a real computation with a real answer, not a shading knob, so it is an IOU with its own name rather than an ambient constant. **Do not add an ambient term** | `shaders/globe.wgsl` `fs_main`, `atmosphere::air_inscatter` |
| 57 | ★★★ **CLOSED 2026-08-10 — THE PLANTS WERE DECORATION — NOTHING CAN INTERACT WITH THEM.** Robin asked directly (2026-08-09): *"is the grass assembly part of Earth now, does it obey physics (is it in the engine's impact purview at least)?"* It is not and it does not. `terra::flora::scatter` has exactly ONE caller — `Terra::build_flora`, a SCENE — and every other mention of flora in the crate is a GPU buffer label or a log line. No collision path, no damage path, no impact reaches a plant | **Grepped, not assumed.** `containment::Contents` and `Populates` have ZERO production consumers; `instance.rs` has none either (row 46). Earth is a `LayeredBody` holding nothing (row 55). So a meteor through this meadow meets bare ground and 1,200 clumps of grass are unmoved — including the ones it passed through | ★★ **This fails Robin's own illusion rule VACUOUSLY, and that is the sharp form of it** (docs/68): *"it's OK if what the eye can see is an illusion, as long as when it's interacted with the simulation/physics govern it."* A canopy TEXTURE is honest because the meteor meets real matter behind it and the hole is real. Here the meteor meets NOTHING — the rule is satisfied only because no interaction exists to test it. That is the difference between a resolved representation and decoration, and it is the line this engine exists to hold | `terra::flora::scatter`, `Terra::build_flora`, `containment`, `instance` ★★ **PARTLY CLOSED 2026-08-09 (docs/70).** `Assembly::meet` is the verb an assembly was missing: energy arrives, every part on its path takes what it costs to break, and the remainder leaves — Robin's own mechanism, *"debited from the total energy of a collision for zero loss"*. It calls `damage::crater_volume` rather than restating `E/σ`, so a grass blade and a continent are the same arithmetic (Law II). MEASURED: a 250 J rock loses **0.061%** to a grass clump and **100%** to an oak, the two answers differing only by the matter; `spent + remaining = arriving` to 1e-12 for every material and energy tested; a 1 mJ event cannot break even one 4.8 mJ blade and nothing continues. `instance::Damage::part_integrity` finally has something to hold (row 46). ★ **STILL OPEN, and it is the larger half**: nothing CALLS it. No impact, meteor or cannon shot routes through an assembly yet, so a meteor still meets bare ground. And only the FRACTURE channel is built — displacement, compaction and heat are named IOUs in docs/70 §4, which is why the haystack Robin asked for is still under-resisted: straw absorbs by COMPACTING, not by snapping ★★★ **CLOSED 2026-08-10 — the impact meets them.** `Terra::impact_meets_the_plants` joins links that all existed and none of which were connected: `flight::Arrival` says where and how much, `damage::crater_radius(crater_volume(E, σ))` — the SAME pair that sizes a meteor's crater — says how far it reaches, `flora::scatter` says what grows there, `Assembly::meet` says what the energy does to each, and `containment::Contents` remembers only the divergences. The renderer then asks the model what it remembers and does not draw what is gone (`Assembly::mesh_damaged`), which is Law VI running the right way. **MEASURED end to end natively**: a 24-pounder fired across the meadow lands and **2,510 clumps are remembered as changed**, from zero before the gun fired. Verified by cutting the link — with the arrival no longer reaching the plants the test goes red. ★ Two bugs found doing it, both mine: the event entered from directly OVERHEAD while travelling the impactor's direction, so a nearly-flat shot passed over every clump and broke nothing; and the first assertion compared the impact's population against `flora_count`, which is what the RENDERER resolved near the camera — measuring the camera instead of the impact |
| 58 | **THE GROUND KNOWS WHAT MONTH IT IS AND THE PLANTS DO NOT.** `terra::globe_mesh::SurfaceSampler` carries `at_epoch`, so the ground's albedo answers for the season — that is what the phenology work built. `assembly::Assembly::mesh` takes `mats[mi].albedo` flat, with no clock anywhere in its signature, so every plant is drawn in its summer colours on every date. Two answers to *what colour is grass on this date*, and the two are drawn side by side in the same frame | **Found by Robin looking at the picture** (2026-08-09): *"one would assume the light scatter from the atmosphere would make the grass green… or yellow, whatever colour it should be in that season."* Both halves of that sentence are defects and they are INDEPENDENT: row 56 is why the blades are black at all, and this is why they would still be the wrong colour once lit | **docs/65** — an assembly should answer the time signal it is already sent (`Assembly` has the mechanism; the MESH is what has no clock). The fix is to hand `Assembly::mesh` the epoch the surface beside it already has, so a leaf and the ground it stands on cannot disagree about the date | `assembly::Assembly::mesh`, `terra::globe_mesh::SurfaceSampler::at_epoch`, `assembly::Assembly::mesh_in_season`, `solar::senescence_fraction`. ★ **CLOSED 2026-08-24: the plants now read the same clock the ground does.** Both halves already existed with no consumer between them — `solar::senescence_fraction(lat, t)` (chlorophyll spent, derived from local day length against its own annual extremes) and `Material::albedo_when_turned(turned)` (the two measured spectra mixed through the CIE observer). `Assembly::mesh_in_season`/`mesh_seasoned` are the wire; `build_flora` asks the ONE senescence model what date it is. `mesh`/`mesh_damaged` delegate with `turned = 0.0`, asserted bit-identical, so the cannon and the fixtures are untouched. Gated with a negative control: a grass tuft's drawn colour moves with the season (summer `[0.095,0.140,0.063]` -> autumn `[0.123,0.168,0.066]`) and a bronze cannon does NOT, which is what stops the test passing for a `mesh_in_season` that tints everything. ★★ **AND THE PICTURE DOES NOT CONFIRM IT.** Row 57's pinned-epoch worlds made this checkable, so it was checked: Galway 53.27N, eye 1.6 m, June solstice vs December solstice, identical but the date. Ground band, 14,250 samples — June `R32.5 G45.4 B16.1`, December `R51.5 G73.8 B25.9`: **+61.0% brighter and 2.6% GREENER**, the WRONG DIRECTION for autumn. By eye December read as "paler, therefore turned"; the measurement says the frame is dominated by the very different sun elevation at that latitude and the hue moved against senescence. So the wire is verified in code and NOT in the picture — either that band is terrain albedo rather than flora, or the two seasonal paths disagree. ★ Compounding it, already catalogued: the measured senescent endmember is not fully cured grass (NEON sampled the growing season), so the turn is mild by construction. **OPEN as a picture claim; the ledger keeps it until a shot separates senescence from illumination.** |
| 59 | ★★ **PARTLY CLOSED 2026-08-10 — AN ASSEMBLY CANNOT CONTAIN AN ASSEMBLY.** `Assembly` is `{ id, name, parts, connections, notes, derived }` and a `Part` is a SHAPE PLUS A MATERIAL — there is no sub-assembly anywhere in the type. docs/67 says an assembly *"can contain other assemblies"*; the source does not, so the model is a decree the code has never implemented | **Found by Robin asking the obvious question** of a haystack built as one cylinder of straw at packing 0.071 (2026-08-10): *"a clump of grass should be an assembly of multiple 'grass blade' assemblies. A haystack is then a pile of 'grass blades' with the 'dry' properties, no?"* Yes — and it is not representable. The grass clump is 32 blade PARTS, not 32 blade ASSEMBLIES, and the bale is one part with a packing fraction standing in for **95,239 straws** (1.14 M triangles if resolved). ★ Her model also RETIRES work done around it: if the blades are real, a pile's void is GEOMETRY and `packing`, the bale-scale `compressive_strength` and the Gibson-Ashby scaling in `damage::crush_stress_pa` all become unnecessary — they exist only because the blades are summarised | **docs/67 §5, docs/70.** The resolution question is real and already has its answer's shape: `Assembly::derived` is to `parts` what a de-resolved summary is to resolved matter, exactly as `LayeredBody` is to an assembly of particles (row 55's blocker). So composition and DE-RESOLUTION are one feature, not two: a haystack is a pile of blades that summarises itself when nobody is close enough to count them | `assembly::Assembly`, `assets/assemblies/grass-tuft.json`, `haystack-bale.json` ★★ **PARTLY CLOSED 2026-08-10 (docs/71).** `Assembly::contains: Vec<Contained>` — a type, a COUNT and an arrangement, so a pile stores its rule and never its members: 304,781 blades cost one integer. Mass and volume flow through it EXACTLY (`count` × the member's own), so de-resolution is not an approximation of weight. The bale is now a pile of `grass-blade-dry` and still weighs **135.7 kg**; its packing is no longer authored anywhere but is what that many blades in that cylinder come to (0.07143 against the measured bale's 0.0714). `meet` sees contained groups through their summary, and `a_pile_answers_the_same_summarised_or_resolved` pins the invariant the feature lives by. ★ **STILL OPEN**: (a) the grass CLUMP is still 32 blade PARTS rather than 32 blade ASSEMBLIES — the data change ripples into `crown_m2` and the flora budget and is its own step; (b) a pile is never actually RESOLVED into members — `Arrangement::Pile` declares where they would go and nothing places them; (c) docs/71 §3b, Robin's better idea, is unbuilt: *"as you pile them a stack should naturally form… simulate it and then map the pile as a derived assembly"* — the packing is COMPUTED from a cylinder somebody chose rather than MEASURED from a heap that settled |
| 60 | **A SETTLED HEAP OF STRAW COMES OUT TEN TIMES TOO LOOSE.** `pile::settle` drops members and measures what forms — Robin's own idea (docs/71 §3b): *"as you pile them a stack should naturally form if you drop them all in the same location… simulate it and then map the pile as a derived assembly."* It works, and it disagrees with reality | **Measured 2026-08-10**: 400 dry blades dropped in one place settle into a 0.41 m heap at **packing 0.0024 (3 kg/m³)**, against real LOOSE HAY at **0.029 (40 kg/m³)** and a field bale at 0.071. An order of magnitude. Three named causes, in the order worth attacking: (1) **a capsule cannot BEND**, and a bent stem NESTS along its neighbours instead of propping against them — for fibres this is the dominant term; (2) they cannot TANGLE, since capsules slide where straw hooks; (3) 400 members is a small heap, mostly free surface, while bulk density is a bulk property. ★ Two setup errors were found and fixed on the way, both mine and both instructive: a release column seven metres tall with only 0.94 s of simulated fall (the 'heap' was still a cloud at 0.0001), and `vel *= 0.98` PER STEP, which at a 0.235 ms timestep is a damping rate of 85/s and held the blades in the air | **docs/71 §3b.** The mechanism is right and the number is not, so `haystack-bale.json` keeps its COMPUTED packing and says so. What this buys already: the simulation is falsifiable against a real bulk density, which a typed number never was. **Do not adopt the simulated packing until it reproduces loose hay** | `pile::settle`, `assets/assemblies/haystack-bale.json` ★★ **AND THE TARGET WAS THE WRONG OBJECT** (Robin, 2026-08-11): *"a hay bale is tighter packed than a loose pile of straw (a haystack) in real life… in a hay bale, compressing bands are employed… in a hay stack it's all gravity"*, then *"which is why I suggested modelling the haystack."* One object named `haystack-bale` carried a BALE's density with no bands on it — a conflation of two different things. Now split: `haystack` (1.9 M blades, 40 kg/m³, gravity only — **this** is what `settle` must reproduce) and `hay-bale` (304,781 blades, 100 kg/m³, compressed and bound). ★ The bale's packing is now an explicitly DECLARED stand-in with its computation named: the baler does work against the straw's own crush resistance (`damage::crush_stress_pa`, already built for the other direction) until twine tension and compressed straw balance, and that equilibrium is the packing. No twine part is declared either, because polypropylene and sisal are not catalogued and the SOP is that a substance is sourced before it is used. ★★★ **2026-08-15 — THE HEAP DOES NOT SETTLE, AND THE THREE NAMED CAUSES WERE THE WRONG THREE.** The row above asserted that a capsule's inability to BEND was the dominant term. That assertion was never measured, and measuring it retired the headline instead of confirming it. (1) **The old 0.0024 was not a settled packing.** It was a snapshot at a flat four seconds of a heap still in motion; `pile::settle` had no rest criterion at all, while the module doc claimed *"if a heap is still moving at the end its packing is not a settled packing"* and `Settled` carried no field that could show it. It now runs against `recohere::SettleGauge` — **the ONE settling gauge**, docs/61 — and reports `quiet`, `elapsed_s` and `peak_speed_ms`. 400 blades now run the full 20 s cap and never accumulate one `t_q` of quiet; packing reads 0.0019. (2) ★★ **THE SOLVER MANUFACTURES ENERGY.** Traced (`pile::settle_traced`, `pile::Sample::energy_j`): total mechanical energy falls 3.12e-1 → 2.60e-2 J and then **RISES 9.1%** to 2.83e-2 J, while the heap's height bottoms out at 0.270 m near t = 12 s and climbs back to 0.306 m. Gravity is the only thing doing work and every contact dissipates, so a dissipative pile re-expanding is proof of injection. **This is the same defect already diagnosed and fixed once in the terrain path** (the settling-storm bug; penalty spring releasing ½k·pen² as launch KE, fixed at 726dddb by a non-injecting velocity/position constraint) and left un-unified — `granular::terrain_contact_resolve` is the non-injecting law, but it is particle-vs-heightfield and the rod-rod path still goes through the `granular::contact_accel` penalty spring. (3) ★★ **RODS CANNOT ROTATE.** `pile::Rod::axis` is written once at construction and never again: no angular velocity, no torque, no moment of inertia, and contact forces found at off-centre closest-approach points are applied as pure translation to the centre. A dropped straw ROTATES TO LIE FLAT, which is the principal way a rod heap densifies; here the initial uniform-on-the-sphere orientation is permanent, so the near-vertical third of the population props the heap open indefinitely. **Neither (2) nor (3) was on the cause list.** Bending stays a real term and is now unblocked on data — `straw.youngs_modulus_stem` = 5.67 GPa and `rigidity_modulus_stem` = 407 MPa are catalogued from O'Dogherty et al. 1995 (four-point bending transverse to the stem, wheat cv. Mercia, 8-22% moisture w.b.), against the 150 kPa BALE figure that was previously the only stiffness in the entry, a ~38,000x category error waiting to be made; `grass.youngs_modulus_culm` = 5.55 GPa likewise, with `youngs_modulus_blade` deliberately NULL because a leaf blade is not a culm and none was found — but bending cannot be MEASURED through a heap that is being inflated by its own integrator and cannot re-orient. **Order now: injection, then rotation, then bending.** ★ Two traps paid on the way, both mine: an un-armed gauge fed a population released AT REST fires at exactly `t_q`, because free fall from rest reaches `v_q = √(2gΔ)` in exactly `√(2Δ/g)` — the two halves of the criterion are the same two numbers, so it is a dead heat decided by rounding, and three of four ladder rungs reported "settled" at 0.09 s against a required 0.0947 s. Arming on first motion fixes that and does NOT fix the second: asked at the 0.044 m ENVELOPE cell, `v_q` = 0.93 m/s sits inside the range of speeds a heap has while falling, so a 200-blade heap was declared settled at 0.34 s with its top still descending and its mean speed still rising. The gauge must be asked at the scale the simulation RESOLVES — the contact radius, 9x stricter — not the coarser cell the envelope is reported on. `SettleGauge` now carries its cell (`for_cell`) instead of hardcoding the voxel world's 1 m, at which a 0.35 m blade counts as quiet below 4.4 m/s. ★★★ **2026-08-16 — THE FLOOR IS FIXED, AND MY EXPLANATION OF IT WAS REFUTED.** Three independent adversarial audits confirmed the DEFECT and killed the CAUSAL STORY, which is recorded here because a refuted claim left standing is how it gets re-derived. **What survived**: the ghost is a fixed offset and not a reflection, `overlap` is zero to within rounding, `f_coh` runs at full strength, and the net is **12.245 m/s² downward** — 1.248x gravity, and it collapses exactly to `sigma/(rho*L)` = 6000/(1400*0.35), independent of cross-section (my 12.26 used a rounded radius). Worse than first stated: the same call also applies `mu*cohesion` = 9.80 m/s² of horizontal Coulomb brake, and because the force triggers on the rod's lowest END while the clamp acted on its CENTRE, an unsupported near-vertical rod sank its full 0.175 m half-length — **325 contact radii** — through the floor in 0.126 s with nothing under it. **What was refuted**: (a) *"the position clamp does positive work every step; that is the pump"* — NO. Its `+mgA*dt²` of position work is exactly the PE gravity removed in the same step and its velocity zeroing removes exactly that step's added KE; net identically zero for any A (checked at A = 5, 9.81, 22.05, 50), and a net SINK at the arrival speeds in play (break-even 4.61 mm/s, actual 5.18 mm/s). (b) *"the floor can never push up"* — NO, at the bit level: `f_rep` is gated on `overlap > 0.0` but dominated by its DAMPING term, rounding puts overlap positive on 5-8% of in-zone steps, and the damper then fires at up to **+1320 m/s²**, ~100x the cohesion. (c) ★★ **THE 9.1% RISE WAS SUBSTANTIALLY A METER ARTIFACT.** `Sample::energy_j` is KE + mgy only, so the image cohesion was an unaccounted potential well up to 9.54e-4 J deep per rod against a mean per-rod traced energy of 1.3e-4 J — a rod merely SINKING raised the trace with nothing created, and a single ISOLATED rod showed a 10.2% rise from that alone. **Read a rise in that trace as evidence, never as proof.** ★ THE FIX IS STILL RIGHT, for the simpler reason that a floor which cannot push up is not a floor: `pile::floor_contact` now uses `granular::terrain_contact_resolve` (h=0, flat), the engine's existing non-injecting constraint, and the clamp is deleted. MEASURED after: energy monotone (rise from minimum **0.0%**), and 200 blades come to rest in **0.80 s** where they previously never did. ★★★ **BUT IT WAS NECESSARY AND NOT SUFFICIENT, AND THE RESIDUAL SCALES WITH MEMBER COUNT**: post-fix, 100 rods settle in 1.44 s, 200 in 0.80 s, and **400 still runs the full 20 s cap** at peak 0.384 m/s. Count-dependence is the signature of a rod-ROD problem, which is what the refuters predicted from the fact that the late rise happened while HEIGHT ROSE (sinking into the cohesion well raises the trace, climbing out lowers it, so the floor bug has the wrong sign for that window). ★★★ **AND THE ROD-ROD CONTACT IS RUNNING AT BALE SOFTNESS.** `granular::contact_from_material` derives stiffness as `E*r/m` from `mat.youngs_modulus`, which for straw is the **150 kPa BALE** figure — the very category error row 60 catalogued `youngs_modulus_stem` = 5.67 GPa to fix, now shown to have a LIVE CONSUMER rather than only a future one. A hunt lens measured the heap running at 99% of the speed at which a pair passes THROUGH itself: the contact is ~650x too compliant for the mass it must stop, so blades tunnel instead of stopping each other. Using the fibre modulus makes the contact **37,800x stiffer** and needs a **194x smaller timestep** (16.5 M steps per 20 s against 85 k), which `granular.rs`'s own note anticipates — *"a stiff contact is kept stable by implicit integration, not by clamping"* — so this is blocked on an implicit contact solve, not on a constant. Also flagged and open: `vel *= 1 - 2*dt` is a body drag applied to rods in FREE FLIGHT, a dial tuned by outcome (Law V), and the native path lacks the `|v_t|/dt` friction clamp the GPU path has |
| 61 | **ONE GRASS, ONE BLADE — NATURE IS DIVERSE AND THE ENGINE IS NOT.** There is a single `grass-blade` and a single `grass` material, used for every grassland on Earth. Robin, 2026-08-12: *"we will need to backfill other types of grass (prairie grass, Kentucky bluegrass, etc) for different biomes; nature is diverse"*, and *"also new classes of plants like Clover."* A meadow in Galway and a prairie in Nebraska currently grow the identical plant | **Structural, and newly CHEAP** — composition (docs/71) is what makes this a data problem rather than a code one: a blade is now a TYPE, so a species is a blade with its own dimensions and material, a clump is a pile of that species, and the land-cover class picks which. Nothing in `flora::scatter`, `Assembly::meet` or the renderer would change. The work is sourcing each species' real leaf dimensions and LAI, per the material SOP, and clover is not a blade at all — it is a trifoliate leaf on a petiole, which is a different assembly shape | **docs/71.** ★ And the tolerance that goes with it, in Robin's words when a 0.07 mm change in a clump's crown cost two plants out of 1,200: *"there is variance in nature… no two are really alike, so I think we can be tolerant of minor (sub-mm) variations."* Two tests asserting EXACT plant counts were loosened to ranges — they were asserting a precision grass does not have, and chasing that number was the wrong instinct | `assets/assemblies/grass-blade.json`, `data/materials.json`, `assets/bodies/earth.json` |
| 62 | ★★★ **NO GRAIN-GRAIN CONTACT IN THIS ENGINE REALISES ITS MATERIAL'S RESTITUTION.** `granular::damping_for_restitution` inverts the textbook `e = exp(-zeta*pi/sqrt(1-zeta^2))` and returns `c = 2*zeta*sqrt(k)`, and its doc states the intent plainly: *"This makes how bouncy a contact is a **material property**, not a dial — the source of truth is `Material::restitution`."* It is not. That calibration is for a grain against a FIXED WALL. `contact_accel` applies `+a` to i and `-a` to j, so for an equal-mass PAIR the relative coordinate obeys `x'' + 2c*x' + 2k*x = 0`: natural frequency `sqrt(2k)`, critical damping `2*sqrt(2k)`, and the REALISED damping ratio is `4*zeta*sqrt(k) / (2*sqrt(2k))` = **sqrt(2)*zeta**. Every pair contact is mis-damped by the same factor, in every scene | **MEASURED 2026-08-16, twice independently** (a workflow lens, then reproduced from scratch against the shipped constants). Straw asks for `e = 0.05`; `zeta_intended` = 0.69011, realised 0.97596. The shipped contact returns **e = 0.1399 dt-converged (0.1476 / 0.1415 / 0.1402 / 0.1399 as dt refines by 4x) — 2.8x too bouncy.** ★★ **AND THE OBVIOUS FIX MAKES IT WORSE**: correcting the sqrt(2) ALONE takes it to **0.2138**, because the DOMINANT error is not the calibration but the `.max(0.0)` no-tension cutoff at `granular.rs:203`, which truncates the dashpot's tensile phase and detaches the contact early. Both corrections together — reduced-mass damping AND an allowed tensile phase — return **0.0498 against the catalogued 0.0500**. ★ Every number here is **dt-INDEPENDENT**, so the miscalibration is not the timestep's fault. ★★★ **BUT THE FIX IS NOT FREE, AND THE FIRST WRITE-UP OF THIS ROW SAID IT WAS.** Correcting the calibration roughly DOUBLES `c`, and `granular.rs`'s own module doc warns that *"explicit damping overshoots and pumps energy once `Z*c*dt` nears 2"* — so doubling `c` halves the coordination at which that bites, from Z ~ 14.5 to Z ~ 7.3, which a heap reaches easily. MEASURED: the 400-rod pile at its old timestep went from a 0.46 m heap to a **14.94 m** one, peak 4.88 m/s, worst energy drawup **+2075%**. `pile::settle`'s timestep rule saw only the SPRING (`0.1/sqrt(k)`) and was silent about the DAMPER, which is a separate explicit term with its own stability limit; it now takes `min(0.1/sqrt(k), 0.1/c)` — ~2.75x more steps for straw — and the drawup returns to **+0.000%**. The GPU path pays this with an implicit directional solve; the native path pays it in dt. ★ Also corrected: the calibration is verified on the NATIVE explicit path only. `particle_step.wgsl` consumes the same coefficient through a different integrator (its `f_rep` carries no damping term at all), so the GPU's realised restitution has shifted too and has NOT been re-measured — re-run the `tools/gpu-verify` bounce test before trusting it; refining dt makes the contact BOUNCIER, not less, which also exonerates the integrator as an energy source (the production dt is a net damper worth ~9% of outgoing speed) | The module doc at `granular.rs:24-26` claims *"grain-grain contact **conserves energy**… verified on real hardware (`tools/gpu-verify`)"* — but that is the GPU path, whose implicit directional solve and `|v_t|/dt` friction clamp the NATIVE path does not have. The native force law is the *physics of record* and was never held to the same measurement. **A declared material property that no contact reproduces is a Law VII violation with a live consumer in every granular scene**, not a pile-specific defect | `granular::damping_for_restitution`, `granular::contact_accel` (the `.max(0.0)` at :203), `granular::zeta_for_restitution`. ★ TRAP FOR WHOEVER FIXES IT: verify against a dt-refinement ladder, not a single run — at the production dt the shipped value reads 0.1476 and the converged one is 0.1399, so a single-dt check can be off by 5% and hide which way a change moved things |
| 63 | ★★★ **THE CATALOGUE'S RESTITUTIONS WERE NEVER SOURCED — 27 OF 32 WERE TYPED.** Row 62 fixed how the engine DELIVERS `Material::restitution`; this row is about where those numbers came from, and the answer is nowhere. Only **5 of 32** entries mentioned restitution anywhere in their sources or notes, and one of those (`grass`) is the prose *"Restitution near zero"*, which is an assertion. Exactly ONE material (`limestone`) cited anything restitution-specific. Every other entry's citations are about density, friction, thermal conductivity or Young's modulus — the restitution was typed alongside them and inherited their credibility | **SOURCED 2026-08-17**, 6 parallel literature agents each followed by an adversarial verifier that re-read the primary documents (figures read as IMAGES, not from prose) rather than trusting the quotes. **14 of 23 solids/granulars now carry a MEASURED restitution with the impact velocity it was measured at**; 9 are flagged as genuine gaps and keep no invented value. ★★ The uncited numbers were **systematically too low, the granular ones by 3-5x**: straw 0.05 -> **0.238 @ 1.17 m/s**, grass 0.10 -> **0.468 @ 2.62 m/s**, sand 0.20 -> **0.75 @ 1.74 m/s**, gravel 0.20 -> **0.83 @ 1.0 m/s**, ice 0.40 -> **0.88 @ 0.25 m/s**, iron 0.60 -> **0.95 @ 0.2 m/s**, cast_iron 0.30 -> **0.69 @ 3.05 m/s**, snow 0.10 -> **0.30 @ 4.12 m/s**, copper 0.45 -> 0.66. Aluminium, granite, limestone, pine and rubber were within ~10% and are now cited rather than merely lucky | ★★★ **THE VELOCITY IS PART OF THE NUMBER.** Goldsmith 1960, on the cast-iron sphere pair that is the engine's exact case, states it outright: *"data indicate that a definite value for the coefficient of restitution cannot be assigned to the impact of spheres unless their size, material, and impact velocity are specified initially."* So `restitution_at_ms` is catalogued beside every sourced value and DECLARED UNWIRED in `laws`, because a LINEAR contact has no velocity dependence for it to anchor — it is the input the nonlinear contact of row 62 needs, recorded before that contact exists so the sourcing is not done twice. ★ Each entry also records its CONFIGURATION, because `e` belongs to a PAIR: same-material sphere pairs are ideal (cast_iron), sphere-on-thick-slab of the same material is an accepted proxy (copper, ice), and a harder impactor onto a softer target is an UPPER BOUND and is labelled as one (aluminium: a 440c steel sphere ~6x harder than the 6061-T6 puck, so the plastic work goes into the aluminium) | 9 honest gaps, each with what was searched: `gunmetal`, `nickel`, `basalt`, `sandstone`, `peridotite`, `oak`, `clay`, `dirt`, `concrete`. Two are worth knowing: for `sandstone` the ideal source EXISTS but was not obtainable, and `nickel` was DOWNGRADED from a found value because the paper, though real and correctly quoted, did not answer the question asked. ★ 9 further entries (7 gases + `water`, `crude_oil`) carry `restitution: 0.0` as a PLACEHOLDER for "a parcel of this does not bounce" — not a measurement, and after row 62 an expensive one: a linear no-tension contact reaches e = 0 only as zeta goes to infinity, so it clamps at zeta ~ 50 (c ~ 70.7*sqrt(k), **38.8x** the old value) and drives a 25x smaller stable timestep. The gas path already sidesteps this by setting `normal_damp: 0.0` and dissipating VISCOUSLY; the liquids do not, and are flagged rather than changed |
| 64 | ★★★ **THE ENGINE HAS THE FAILURE HALF OF `docs/18` AND NOT THE ELASTIC HALF — AND THE GRASS BLADE PROVED IT.** Robin, 2026-08-17: *"we must look for the general applicable case if we're going to have a hope of modeling everything"*, on trees, poles and bent nails as well as grass. `docs/18` already states the one process — stress compared to the material's yield/fracture strength, matter fails, momentum conserved, energy accounted — and targets MPM: *"one solver, per-material constitutive models, unifying elastic, plastic, granular and fluid response."* Cratering, crushing, fragmenting and granular flow are BUILT. **Nothing lets a body store elastic strain and give it back.** Its own table calls a solid *"elastic below strength (bounce/embed)"*, and that bounce is contact restitution — a property of a COLLISION, not of a body deforming. So bending is not a new law; it is the branch of an existing one that was never built | **SOURCED 2026-08-17** (4 lenses + adversarial verification, primary documents re-read). Three of my assumptions were wrong. (1) `youngs_modulus_blade` was NULL and the CULM's 5.55 GPa stood in; the blade's is **1.06 GPa**, so the engine was **5.23x TOO STIFF** — the honest number makes the droop WORSE, not better. (2) **Turgor contributes NOTHING and the sign is backwards**: it stiffens a closed thin-walled cylinder via axial tension and a lamina has no organ-scale lumen, while every hydration dataset says DRYING STIFFENS grass (turgid 1051 -> wilted 1787 MPa). (3) **"Real grass stands up" is FALSE for a 26 cm blade.** Greenhill's self-weighted-column criterion (constant 7.8373, recomputed by shooting) gives `L_crit = (7.837*EI/q)^(1/3)` = **208 mm**, and measured Lolium blades are 258 and 316 mm — **1.24x and 1.52x post-critical**. A real ryegrass blade CANNOT stand as a straight column; it arches, which is what a sward looks like, predicted with no tuning | ★★★ **A DIAL-FREE FALSIFICATION OF THE FLAT SLAB.** Solving the actual elastica for the measured 258 mm blade at an 80 deg ligule angle, the bare flat-slab section puts the tip **32 mm BELOW the soil it grows from** — geometrically impossible, and it needs no threshold to say so. Recovering a tip at ground level needs I_real/I_flat >= 1.15; measured *Imperata cylindrica* midrib thickness (429.7 um against 196.5 um lamina, so 10.5x local stiffness) independently gives 1.5-2.4. ★★ **STORE THE PARTS, NOT THE PRODUCT**: there is deliberately no catalogued `flexural_rigidity`, because EI depends on width, on thickness CUBED and on shape, so a stored EI is wrong for every blade but the 11 mm wheat leaf it was measured on. The one directly measured Poaceae blade EI (Wu 2024, 0.9 +/- 0.3e-5 N.m^2) becomes an ACCEPTANCE TEST on the composition instead. ★ Robin's ruling on the two remaining discrepancies: blade LENGTH (0.35 m vs the measured 258 mm) is **render prerogative** — a choice of which plant we depict, with the physics then obliged to say honestly that a 350 mm blade is 1.68x post-critical and arches more. Density is NOT: `density` 1400 is dry CELL WALL and the modelled blade was **1.97x too heavy**, so `density_fresh_blade` = 710 kg/m3 is now recorded | **PARTLY CLOSED 2026-08-17 — THE SHAPE FACTOR IS RETIRED, SAME DAY IT WAS PROPOSED.** Robin: *"Render can fudge, physics shouldn't."* A stopgap `I_real/I_flat ~ 2.0` is bounded 1.5-2.4 by two independent routes and is a declared IOU rather than a fudge — but it is a fudge-SHAPED patch over a geometry the assembly could simply state. A blade is not a slab times a correction: it is a **LAMINA PLUS A MIDRIB**, and declaring those as two parts makes the second moment of area fall out with no factor anywhere, **DONE**: `grass-blade.json` declares two lamina wings either side of a thicker median band and `Assembly::section` computes I from that geometry by the parallel-axis theorem — recruitment **1.946x**, a RESULT rather than a chosen number, cross-checked against the closed form `(1-f) + f*r^3`. ★ TRAP PAID: the first declaration glued a rib ONTO a full-width lamina (double-counting the lamina beneath it, 1.167x), and the cross-section frame was built from an arbitrary perpendicular so the width axis was indexed as the thickness one — reporting **85.2x**. Both were caught only by that closed-form cross-check; a shape FACTOR could never have surfaced either. The one unmeasured input is now the MIDRIB WIDTH FRACTION (0.10), which is a measurable dimension rather than a knob. ★★★ CLOSED 2026-08-17: **`Part` now carries `roll_rad`**, so a V-FOLD IS expressible. `along` is a DIRECTION — two degrees of freedom — and an orientation is three; the missing one was roll, and its absence made Ellis's entire V-shaped class of Poaceae blades undeclarable, because two lamina arms at ±φ both point the same way. `Part::orientation()` is now the ONE place a part's orientation is built (there were already two copies of the arc rotation, the mesher's and the cross-section's, and a third would have been where they began to disagree), and `section()` rotates each part's own moments into the beam frame by `I' = I_v·cos²φ + I_w·sin²φ`. VERIFIED against the standard folded-section result `I_V = 2[(t·b³/12)sin²φ + (b·t³/12)cos²φ]` across Ellis's classes — I/I_flat = 1.000 / 2.640 / 7.122 / 13.245 / 19.367 / 21.903 at θ = 180/150/120/90/60/45°, matching the independently sourced 1.0 / 2.6 / 7.1 / 13.3 / 19.4 / 21.9, and reducing EXACTLY to the flat slab at 180°. Defaults to 0.0, asserted bit-identical to the previous rotation, so every shipped assembly is unmoved. This also unblocks what row 30 needs: a plank has a grain direction AND a face, and so do rolled steel and composite layup. ★★★ **THE ELASTIC BRANCH IS BUILT (2026-08-19), `crates/engine/src/flexure.rs`** — `docs/18`'s missing half. `solve()` is a large-deflection ELASTICA (`dθ/ds = M/EI`, fixed-point relaxation), not small-deflection beam theory, because a grass blade is post-critical and linearising the slope would be wrong by more than the answer. It is a **DECLARED SPECIALISATION** of docs/18's continuum target, gated on TWO independent dial-free analytic references: (1) it converges to the linear cantilever `δ = qL⁴/(8EI)` as the load vanishes — measured ratio 1.00033, 1.00033, 1.00008, 0.97632 across four decades of load, sitting AT the linear answer when small and departing DOWNWARD as the beam curls; (2) Greenhill's self-weighted column, constant 7.8373 — an upright beam at 0.5 L_crit stands dead straight and at 1.6 L_crit falls 1.25 lengths short of vertical. When MPM lands the convergence owed is against IT, and the module doc is the IOU. ★★ `bending_stress_pa` (`σ = M·c/I`) is the BRIDGE to the ladder, and **`modulus_of_rupture` now has its first reader** — it moved out of `laws::UNWIRED_MATERIAL_PROPERTIES`, where it had sat described as literally *"bending failure of a beam or plank"*, and is parsed into `Material` as an `Option` because it is catalogued on only 2 of 37 materials; a material with no limit returns `None` rather than acquiring one. ★ TRAP PAID: the moment accumulator paired each coordinate with its OWN force component (`y·f_y`) instead of the other's — not a cross product at all. It cost exactly a factor of **2/3**, the solver converged happily to it (residual 2e-16), and nothing but a closed-form reference could have seen it. Also open: the PLASTIC rung (`ductility` 30/37 — the bent nail), RUPTURE for the other 35 materials, the PLASTIC branch (`ductility` 30/37 — the bent nail), and RUPTURE (`modulus_of_rupture` on only **2/37**, already declared unwired as literally *"bending failure of a beam or plank"*, and the first reader of oak's orthotropic set, which memory records as catalogued with ZERO readers). ★ THE LAW II TRAP: `docs/18` targets a continuum solver, so a beam chain answers the same question by another route — it must be a DECLARED SPECIALISATION with a convergence test, and since MPM does not exist yet the honest interim reference is the analytic elastica |
| 65 | ★★★ **`compressive_strength` CARRIES TWO DIFFERENT PHYSICAL QUANTITIES, AND NOTHING TOLD A READER WHICH.** Building the PLASTIC rung of row 64 needed a yield stress, and the catalogue had no such field. It had `compressive_strength`, documented as *"what it takes to CRUSH it"* — and for a ductile METAL that number is not a crushing strength at all but the YIELD, because metals flow rather than crush. Three entries say so in their own notes (`aluminium`: *"compressive_strength = yield"*; `copper`: the same; `iron`: *"compressive yield (ductile metals flow, not crush)"*), while `oak` at 51.3 MPa against 90 MPa in tension and `cast_iron` at 930 MPa against 295 MPa carry genuine crushing strengths. Same field, two questions — the Law II failure, in the DATA rather than in the code | **RESOLVED 2026-08-21 by PROMOTION, not sourcing.** `yield_strength` is now its own field, populated only where the entry already states its `compressive_strength` is a yield: aluminium 276 MPa, copper 69 MPa, iron 370 MPa. No value changed; a fact the entries already asserted was given a name a consumer can act on. ★ Two deliberate gaps: `cast_iron` gets NONE because grey iron is brittle and has no plastic rung to climb (930 vs 295 MPa is exactly what a brittle solid looks like), and `nickel` gets none because its 148 MPa is very close to published annealed yield but the entry never SAYS so — inferring it from the number matching is precisely the pattern-match this catalogue exists to prevent | **THE PLASTIC RUNG IS BUILT** (`flexure::yielding`, `residual_curvature_per_m`, `Assembly::plastic_modulus_v_m3`). `S = ∫|y − y_pna| dA` about the EQUAL-AREA axis, not the centroid — they coincide only for a symmetric section, and at full plasticity the stress block is uniform so it is AREA that must balance. The shape factor `S/Z` is pure geometry and is asserted **exactly 1.5** for a rectangle at four scales, with no tolerance to tune. ★★ The reduction that ties the rungs together: **unloading is always elastic**, so the residual is `κ − M/EI` — SECTION-INDEPENDENT — and below first yield `κ = M/EI` exactly, so an elastic bend leaves precisely NOTHING. Measured on a 3 mm iron nail: 0.000e0 residual at 0.9× first yield, then radius 203 m → 9.1 m → 1.75 m → 0.485 m at 1.05/1.20/1.35/1.45×, and unbounded curvature at `M_p` — which is what a plastic HINGE is, not a solver failure | ★ FLAGGED (Law V): the moment–curvature relation `κ/κ_y = 1/√(3 − 2M/M_y)` is RECTANGLE-SPECIFIC. The shape factor and the residual are general; this curve integrates a stress block over a particular section, and a general form needs the section's own `M(κ)`. Rectangles cover every `Slab` the catalogue builds from, so it is the honest first case rather than a claim of generality. ★ `plastic_modulus_v_m3` REFUSES for non-box shapes and for ROLLED parts — `∫|y| dA` over a rotated rectangle has no elementary form, and approximating it inside a number that looks exact is the failure this refusal avoids. A V-folded blade therefore has an elastic section and no plastic one, which is honest and also not a thing that goes plastic, being grass |
| 66 | ★★★ **ROW 30's ORPHANS HAVE A READER: A ROCK SHATTERS, A TREE SNAPS OR SPLINTERS.** Robin, 2026-08-21: *"a rock should be able to shatter, a tree should snap or splinter, etc."* Row 30 has recorded wood's orthotropic set as catalogued with ZERO readers since it was written, and named the blocker exactly: *"a PART has no grain DIRECTION at all, so even a reader of those fields would not know which way the plank runs."* `Part::roll_rad` removed that blocker, and a catalogue-wide audit independently flagged the same seven properties — `grass.tensile_strength_blade` and the `_perp` sets on `oak` and `pine` — as variants nothing distinguished | **BUILT 2026-08-21** (`flexure::Failure`, `fails`, `orthotropy`, `modulus_across_grain_pa`, `blade_tensile_pa`, `shear_stress_pa`). Five properties moved out of `laws::UNWIRED_MATERIAL_PROPERTIES` into `Material` and are now read: `shear_strength`, `tensile_strength_perp`, `compressive_strength_perp`, `youngs_modulus_perp`, `tensile_strength_blade`. Measured: oak orthotropy **16.4x** with an across-grain modulus of 2.00 GPa against 12.3 along; pine **33.3x**, 0.66 against 8.5; and a grass BLADE's tensile limit is **10,000x** its turf mat's — the same fibre-vs-arrangement split `straw` records for its stem, not an anisotropy | ★★★ **NOTHING DECLARES A FAILURE MODE.** A bent beam carries a bending stress `σ = M·c/I` AND a longitudinal shear `τ = 3V/2A`; each is compared against its own limit and whichever utilisation reaches 1 first decides. Because `σ/τ = 4L/t` for a rectangular cantilever, **the mode depends on the body's PROPORTIONS**: bending governs above `L/t = MoR/(4·τ_limit)` = **1.90 for oak, 2.38 for pine**, so a slender plank SNAPS and a stubby one SPLITS along the grain. Verified at L/t = 0.95 → `Splinters` and L/t = 3.80 → `Snaps`. A body with no across-grain strength has no grain to split along and `Shatters` instead — `damage::disrupt` is the law that makes the pieces, and this is its SECOND trigger alongside atmospheric break-up, rather than a new one | ★★ **THE SHATTER BRANCH IS GATED ON A SYNTHETIC MATERIAL, AND THAT IS THE HONEST PART.** `modulus_of_rupture` is catalogued on oak and pine ONLY, and both have a grain — so **no material in the catalogue can currently reach `Shatters` at all**. The first draft wrote that check against `concrete` inside an `if let`, which SKIPPED SILENTLY and left the path untested while looking tested. It now runs against a grainless clone of oak, with the real oak asserted to snap under the identical overload, so grain is the only difference between the two calls. Rock, concrete and ice are in flight; when their MoR lands the synthetic control retires. ★★★ **MoR SOURCED 2026-08-22 — 2 of 37 became 9 of 37**, so the shatter branch has real materials and the synthetic control has RETIRED: basalt 12.66, granite 18.30, limestone 10.20, sandstone 8.15, concrete 4.96, ice 1.67, cast_iron 475.50 MPa (grey flake-graphite, ASTM A48 Class 40, as cast). `peridotite` and `straw` are honest GAPS and keep no invented number. ★ The verifier's own caveat was heeded over its headline: granite's source value of 20.4 MPa sits ABOVE the upper quartile of Mosch (2008)'s granitoid distribution and would bias a generic granite high by 25-35%, so the better-centred Barre six-orientation mean of 18.3 MPa is carried instead. ★★★ **AND THE EXCLUSION AUDIT KILLED MY OWN GATE'S JUSTIFICATION.** Row 65's `only_things_that_can_be_a_beam_carry_beam_strengths` justified itself with *"a granular bed has no bending strength at all — a heap of sand cannot be a cantilever"*. That is FALSE: **soil-cement has an ASTM standard for exactly this** (D1635, flexural strength of a simple beam under third-point loading) and snow-slab mechanics measures a slab's bending strength. The gate's OUTCOME is right and its REASON was wrong. The criterion that actually decides is the Wood Handbook's own definition — *"modulus of rupture … is not a true stress because the formula by which it is computed is valid only to the elastic limit"* — so MoR is `σ = M·c/I` at the COLLAPSE moment, a fiction that is only SMALL for a material staying nearly elastic to failure. That excludes a ductile metal (which yields extensively, making `Mc/I` a large fiction) for an entirely different reason than it excludes a fluid (no static shear, so no moment and no outer fibre). Phase stays the right PROXY only because every granular entry here is LOOSE; whoever adds a cemented one must widen the gate rather than delete it. ★ OPEN: the `_perp` COMPRESSIVE strength is parsed but still unread — it is what caps wood's bending strength below its tensile (oak MoR 105 vs tensile 90, but pine 59 vs 70), because wood bending fails on the COMPRESSION face |
| 67 | ★★★ **A GRASS BLADE IS BUILT FROM THE SWARD'S STIFFNESS, NOT ITS OWN — AND IT IS 212x TOO SOFT.** `Material::youngs_modulus` for `grass` is **5.0e6 Pa**, which is the turf MAT's modulus; the blade's own **`youngs_modulus_blade` = 1.06e9 Pa** was sourced on 2026-08-17 and **has no reader**, as does **`density_fresh_blade` = 710 kg/m³** against the generic `density` = 1400. So every caller building a blade's `EI` gets a body 212x too floppy and 2x too heavy | **Measured 2026-08-24** while building the de-resolved bending form. With the generic numbers `greenhill_critical_length_m` puts the critical length at **2.78 cm** and the shipped blade is **35 cm — 12.6x past it**: the elastica winds up through tip angles of 420, 560, 3164 rad, i.e. hundreds of complete turns, at every non-zero wind. With the blade's own sourced numbers L_crit is **20.8 cm** and the blade is **1.7x** it — an arch, which is what grass actually looks like. The catalogue was right and unread | **docs/48** — physics built, verified, and wired into one place or none; this is the same pattern as row 58 in the same session. The fix is a reader: a blade's section must ask for the BLADE's modulus and the BLADE's density, and `laws::UNWIRED_MATERIAL_PROPERTIES` must lose both entries when it does. ★ Note the two errors partly cancel in `L_crit` (stiffness up, weight down), which is exactly how a wrong number survives a sanity check | `materials::Material::youngs_modulus`, `granular::contact_from_material`, `laws::catalogue_tests::a_material_id_names_one_material_not_a_member_and_its_bulk`. ★★★ **CLOSED 2026-08-25, and it was WORSE AND MORE GENERAL than this row said.** The rule is **a material id must name ONE material — the member or the bulk, never both**, and it is now a gate. `straw` was the worse offender: `youngs_modulus` held **1.5e5 Pa, the compliance of a LOOSE HAY MASS**, against a 5.67e9 Pa stem — **37,800x**, and CIRCULAR, because a haystack's bulk compliance is exactly what `pile::settle` exists to PRODUCE, so it was being fed back as its own members' contact stiffness. `grass` was 212x (blade) and 1110x (culm). All three flowed through `granular::contact_from_material`, which its own doc calls *"the ONE place where what the matter IS becomes how it collides"*. **Fix:** `youngs_modulus` now carries the MEMBER (grass 1.06e9, straw 5.67e9) with its declared range moved to match; the aggregate compliances survive as `youngs_modulus_sward_aggregate` / `youngs_modulus_loose_aggregate`, declared unwired **on purpose** — Law III says an assembly's compliance is COMPUTED, so they are the targets an emergent answer must reproduce, not inputs. ★ The entries said so themselves the whole time: grass's notes read *"Two stiffnesses coexist: soft soil mat (~MPa) vs stiff strong blades."* The catalogue knew, in prose, and the field went on handing callers the wrong one. ★ `density_fresh_blade` (710 vs generic 1400) is NOT fixed here: 1400 is a true cell-wall density and defensible, so it needs its own look | 647/647 |
| 68 | ★★ **THE LARGE-DEFLECTION SOLVER CANNOT REACH THE DEFLECTIONS GRASS IN WIND ACTUALLY HAS.** `flexure::solve` relaxes a fixed point under-relaxed at `MIX = 0.5`, and that iteration is contractive only while the tip lean stays under **~85% of the arclength** — measured at 85.2%, 85.7% and 88.7% for lengths of 0.25x, 0.5x and 1.0x the Greenhill critical length, so it is a **DEFLECTION** ceiling, not a load one. Expressed as load it collapses fast with length: 202x self weight at 0.25x L_crit, 24.7x at 0.5x, **3.0x at L_crit**. A full-length grass blade converges at NO non-zero wind | **Measured 2026-08-24.** The solver is HONEST about it — it returns `converged: false` rather than asserting, exactly as its doc promises — so this is a capability limit, not a lie. It is nonetheless the regime the whole bending line exists for: Robin, 2026-08-17, on why bending matters — *"wind bends grass blades which, if on fire, can help spread"*. Fire spreads in a gale, and a gale is outside this solver | The method, not the formulation. `dθ/ds = M/EI` is right; fixed-point relaxation is the wrong way to walk it past the fold. Arc-length continuation (step along the SOLUTION curve, not the load) or a Newton solve with the true tangent both reach post-buckling shapes. Until then the limit is stated and gated: `a_de_resolved_patch_bends_like_the_bodies_it_replaces` asserts `converged` on every solve, because its first version did not and silently compared 20,000th iterates of two diverging runs | `flexure::solve`, `flexure::Compliance::tabulate` |
| 69 | ★★ **PARTLY CLOSED 2026-08-29 — and block-stepping is NOT the fix.** ~~THE HONEST STEM STIFFNESS MAKES THE HAYSTACK 194x MORE EXPENSIVE, AND IT NO LONGER FINISHES.~~ ~~THE HAYSTACK HAS THREE ASSEMBLIES AND ZERO TESTS.~~ **That claim was WRONG and is corrected here the day after it was written.** `pile::tests::a_heap_of_dry_blades_does_not_come_to_rest_yet` DOES load `straw` and settle a haystack — it is `#[ignore]`d, so it is absent from the default suite, and a grep for `"straw"` misses it because the material arrives through `Assembly::dominant_material()` rather than a literal. Two ways of not seeing a test, and I reported it as not existing | **Measured 2026-08-25.** Row 67 raised straw's contact stiffness 37,800x, and `pile::settle` sets its own step from it (`dt_spring = 0.1/sqrt(stiffness)`): **dt 2.3494e-4 s -> 1.2084e-6 s, a 194x smaller step** for the same simulated time. The test ran in seconds before and **exceeded 10 minutes** after. So the original row's headline was wrong twice over — the silence was not missing coverage, it was coverage excluded from the default run; and the change did not pass unnoticed, it made the one test that would have noticed unaffordable | ★ This is Law 8's own question — *is this the most physical thing this budget can buy, and does it converge as the budget grows?* A 5.67 GPa stem in an explicit penalty contact is honest and costs 194x. The three candidate answers, in order of honesty: sub-step the contact while the pile integrates at its own rate; move the contact to an implicit/semi-implicit solve, where stiffness stops setting the step; or accept the cost and shrink the member count, which trades a real answer for a smaller one. Do NOT soften the stem back — that is the fudge row 67 just removed. The prize is unchanged: settle the pile, drop a rock on it, and check that the bulk compliance EMERGES into the measured [8.3e4, 3.5e5] Pa band, which would close a declared-not-emergent quantity by computing it | `pile::settle`, `substep`, `aggregate::Aggregate::step_block`, `granular::contact_from_material`. ★★★ **WHAT LANDED, AND WHAT THE MEASUREMENT KILLED.** The row named three candidates; measuring them reordered all three. **(1) The step is now DERIVED, not dialled.** `0.1/√stiffness` appeared in THREE places (`pile` twice, `granular` once) plus a magic `time_scale/960` for SPH and a real CFL condition in `hydrostatic` — five answers to one question. `substep::accurate_dt_s` is the single owner, from the contact's own `ω = √stiffness` and duration `t_c = π/ω`. ★ **The dial was approximately RIGHT**: measured against the analytic restitution it works out to 31.4 steps per contact ≈ **2.2% error**, with error halving per doubling. It was unjustified, not wrong — a distinction worth keeping, and it means there is no easy factor sitting in the timestep. **(2) The block-timestep pattern already existed** in `Aggregate::step_block` (docs/30 stage 3, verified to reproduce the global-dt result) and was welded to point-mass particles. `substep::Schedule` lifts its arithmetic out body-agnostically — for debris, ejecta and smoke as much as rods — pinned term-for-term against the inline version so the extraction cannot drift. **(3) ★★ AND IT WILL NOT SAVE THIS PILE.** Measured contacting fraction: **mean 0.547 over the run, 0.400 at the end**, so over half the members are touching at any moment and pay the stiff step regardless of scheduling — **at most 1.78x**, against the ~100x wanted. A heap is the worst case for block stepping, which pays off when most of a population is FREE | **The remaining affordability fix is row 72, not this row.** The run costs the full `CAP_S = 20 s` only BECAUSE the pile never settles; give rotation a dissipation channel and the gauge terminates early, which is worth more than any scheduling. After that, implicit contact is the only route to a genuinely stiffer step. ★ Also measured: packing read 0.00053 and 0.00046 across a **1.9% timestep change** — 13% apart, so the figure is NOT converged (a scatter of spinning blades is plausibly chaotic) and must not be quoted to two digits |
| 70 | ★★ **CLOSED 2026-08-26.** ~~A FLAT BLADE IS MODELLED AS A ROUND ROD, AND THE AIR CAN TELL.~~ `pile::rod_for` collapses a member to an EQUAL-VOLUME capsule, so the dry grass blade — a ribbon 3 mm wide and ~0.1 mm thick — becomes a cylinder of radius **0.54 mm**. Volume is preserved; **presented area is not**. Broadside, the real ribbon shows ~7x the frontal area of its equal-volume capsule | **Measured 2026-08-26** while wiring real air onto falling rods (row 60 step A3). Terminal speed for this blade: the retired rate fudge gave `g/2` = **4.91 m/s**; real drag on the capsule as modelled gives **7.23 m/s**; real drag on the true 3 mm ribbon gives **~1.85 m/s**. The fudge was not simply too fast or too slow — it sat between two geometries and belonged to neither. ★ I wrote the 1.85 figure into the code as though it described the engine's own rod, and the closed-form check caught it: the correction is in the integrator's comment | Area is what drag integrates, so a shape abstraction that preserves volume and not area is wrong for anything aerodynamic — and hay fluttering down is the canonical case. Options, in order of honesty: give `Rod` a WIDTH as well as a radius (a ribbon is a capsule with two radii, and `Part` already carries `roll_rad` to orient it); or drop the capsule for the blade's real `Shape::Slab` and compute frontal area from the slab. The second is Law I's answer — the matter decides — and it also feeds row 60 step B, since a ribbon's rotational inertia is what makes it flutter rather than fall | `pile::rod_for`, `pile::Rod`, `atmosphere::box_frontal_area_m2`, `atmosphere::FLAT_PLATE_NORMAL_DRAG_CD`. ★★★ **CLOSED, and the row's own headline number was wrong.** It claimed the ribbon shows *"~7x"* the capsule's area; that compared a RANDOMLY ORIENTED capsule against a BROADSIDE ribbon. Like for like: broadside **2.81x** (1.060e-3 vs 3.774e-4 m²), edge-on **0.28x** (1.050e-4 vs 3.774e-4). The real statement is not a single ratio at all — a cylinder is axisymmetric and shows the SAME area whichever way it rolls, while the ribbon swings **10:1** with orientation, and that swing is what fluttering is made of. **Fix:** `atmosphere::box_frontal_area_m2` (exact box projection) + `FLAT_PLATE_NORMAL_DRAG_CD` = 1.98, sourced; `Rod` carries `width_m`, `thickness_m` and a broad-face normal drawn from its own seeded roll. `radius_m` survives as the CONTACT proxy only and says so. **Result: the blade's terminal speed falls 7.23 -> 2.05 m/s**, where real straw drifts, and agreement with the closed form IMPROVED to 0.121%. ★ Also landed: `pile::release_rods`, one owner for the release — a test had begun rebuilding the seeded draw for itself |
| 71 | ★★ **THE SETTLE GAUGE COULD NOT SEE A HEAP TUMBLE IN PLACE — INTRODUCED AND CLOSED THE SAME DAY.** `recohere::SettleGauge` was built when pile members could only translate, so it asks exactly one question: is anything MOVING, by linear speed? Row 60 step B gave rods a rotational degree of freedom, and from that moment a heap could tumble with every centre of mass sitting still and the gauge would call it quiet | **Found 2026-08-26 while landing step B**, by asking what a new degree of freedom does to the instrument that judges the old ones. Worth recording even though it was closed within the hour: a capability added on one side of the engine silently widened a blind spot on the other, and the suite would have stayed green through it | **CLOSED with no new dial**, because the gauge already owns one: a rod turning at ω has a TIP moving at `ω·L/2`, and a tip below the quiescent speed is as motionless as a centre below it. The gauge is shown whichever of the two is larger. ★ The general lesson is the one to keep: **when a body gains a degree of freedom, every gauge that decides whether it is at rest has to be re-asked.** Bending (row 60 step 3) will pose it again — a blade flexing in place is not at rest either, and `SettleGauge` cannot see that yet | `recohere::SettleGauge`, `pile::settle_traced`, `pile::Sample::peak_ang_speed_rads` |
| 72 | ★★★ **ROTATION HAS NO DISSIPATION CHANNEL — A BLADE SPUN UP NEVER SPINS DOWN.** Row 60 step B gave rods angular velocity, but nothing takes it away. `granular::contact_accel` is fed `rod.vel` — the CENTRE OF MASS velocity — where the physical quantity is the velocity **at the contact point**, `v + ω × r`. So a contact can neither damp a spin nor be correctly driven by one, and its `normal_damp` (which carries the material's own restitution) never sees the rotation at all. `atmosphere::drag_accel` likewise returns a force and no torque, so the air cannot slow a turning blade either | **Measured 2026-08-26 by the re-measure.** A 20-blade heap after 20 s: centres of mass nearly STILL at **0.147 m/s**, and tip speed **37.4 m/s** — ω ≈ 214 rad/s. The blades stopped translating and went on spinning. It is worst about the long axis, whose moment is `m(W²+T²)/12` ≈ **3.4e-10 kg·m²** for this blade, so a tiny off-axis impulse produces an enormous angular acceleration and nothing removes it. ★★ **The heap would have been called SETTLED without row 71**, which landed an hour earlier: the rotation-aware gauge is the only reason `quiet` reads false | Feed the contact the POINT velocity `v + ω × r` on both bodies, and apply the resulting impulse at that point — one change that closes the loop in both directions, since the same term that damps a spin is the one that starts it. Air torque is separate and smaller: a blade turning about its own length presents almost nothing to the flow, which is precisely why the contact channel is the one that matters. ★ Until then, **any packing this module reports is a scatter of spinning blades, not a heap** | `granular::contact_accel`, `pile::step_one_rod`, `pile::settle_traced` |
